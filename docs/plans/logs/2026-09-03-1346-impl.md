review session: c622b9f7-2432-4a36-9f7a-b8818fe2e6b3

## 1回目

````text
レビュー完了。修正そのもの（`hasScrollRoom` の走査）は正しく、無限ループは解消されています。P0 はありません。

## P0

## P1
- `docs/CHANGELOG.md:[Unreleased] / 修正` — 「**2 本指スクロールすると**」と書いているが、注入コードは `wheel` を 1 発受けた時点で無条件に `hasScrollRoom(event.target, event.deltaX)` を評価するので、**普通のマウスホイールでの縦スクロールでも同じく固まる**（`deltaX === 0` でも走査自体は回る） / リリース本文とアプリ内通知の唯一の源なので、マウス使いが「自分には関係ない」と読み違える・再発報告の突き合わせがずれる / 「2 本指スクロール」を「スクロール（マウスホイールでも）」に広げる。原因文の「スワイプで戻る / 進むの判定が」はそのままで正しい
- `scripts/verify-phase1.mjs:check('quirks mode の子フレームで wheel を受けてもレンダラが固まらない')` — 子フレームに注入が入っているかを確かめずに `dispatchEvent` しているので、**注入が入っていなければ何も起きずに PASS する**（空振り検査） / CLAUDE.md の「その検査が実際に走ったか」を満たさず、将来 `did-frame-navigate` 側の注入が壊れたときにこの検査は無言で通る。直前の `iframe.html` の検査は別ページなので保険にならない / 子フレームはページと同じワールドに入る（`registry.ts:injectFrame`）ので、`compatMode` の前提チェックと同じ形で `w.__nemoSwipeAttached === true` を `check` にしてから撃つ
- `scripts/verify-phase1.mjs:check('quirks mode の子フレームで wheel を受けてもレンダラが固まらない')` — 時間切れで FAIL を記録したあと、そのまま `ui.ev(navigate …)` → `loaded('/cache.html')` へ進む / 本当に固まっている renderer は同一プロセス・同一オリジンで回り続けるため遷移が commit されず、`loaded` が 15 秒で throw して**以降の phase1 の検査（スーパーリロード・ピン留めドラッグ・ツールバー寸法・main の例外検査まで）が丸ごと走らない**。回帰時に得られる情報が 1 件に減る / 時間切れだったときは navigate せずタブを閉じて作り直す、または後片付けを `try/catch` で包んで残りの検査を続ける
- `VERIFY.md:「2本指スワイプで戻る / 進む」の項目` — phase1 が見ている内容の列挙に quirks mode の検査が入っていない（「隔離ワールド・縦ジェスチャ・iframe の中」のまま） / この列挙が「何が守られているか」の参照先なので、次に走査まわりを触る人が保護の存在に気づけない / 「**DOCTYPE の無い（quirks mode）文書でも固まらないこと**」を同項目に足す

## P2
- `scripts/swipe-gesture.test.mjs:test('注入コード: quirks mode（scrollingElement が body）の文書でも祖先の走査が止まる')` — `new Function(...)` の組み立てが `runInjection` とほぼ同一で 3 箇所目の複製になっている / 注入コードが新しいグローバル（例: `matchMedia`）を使い始めたとき、全複製を直すまで「テストだけ落ちる / テストだけ通る」がぶれる / `runInjection` に祖先ツリーと `getComputedStyle` のフックを渡せる引数を足して合流させる
- `scripts/verify-phase1.mjs:check('quirks mode の子フレームで wheel を受けてもレンダラが固まらない')` — 実機検証が iframe の場合だけで、CHANGELOG が先頭に据えている**トップフレームの quirks ページ**（注入先が隔離ワールド 1729 で経路が別）は撃っていない / DOM は共有なので理屈上は同じ壊れ方だが、注入経路は `executeJavaScriptInIsolatedWorld` と `executeJavaScript` で別物 / `quirks.html` は単体でも配信されているので、そこへ navigate して `page.ev` から同じ `dispatchEvent` を撃つ 1 件を足せば埋まる
- `src/shared/swipe-gesture.js:canScroll` — 先頭で必ず `getComputedStyle(node)` を呼んでから `scrolls || node === document.scrollingElement` を見る / 祖先ぶんだけスタイル計算を強制するので深い DOM ではホイール 1 発ごとの実費が乗る（既存の挙動そのままで、この差分が持ち込んだものではない） / `room <= 1` と `node === document.scrollingElement` を先に判定して `getComputedStyle` を後ろへ回せる。ただし新しい単体テストが `visited` の並びに依存しているので、直すならテスト側も同時に

## Q
- `docs/CHANGELOG.md:[Unreleased]` — この修正を `[Unreleased]` に積んだまま次の機能リリースまで待つか、1.2.8 として単独で切るか / 症状が「タブが固まって閉じる以外に何もできない」で、DOCTYPE 無しの HTML をそのまま描くプレビュー系の埋め込みは実運用で踏み得るため、待つあいだは既存利用者が踏み続ける
- `src/shared/swipe-gesture.js:buildSwipeInjection` — ページへ注入する走査に一律のステップ上限（例: 祖先 200 段で打ち切り）を入れる方針にするか、走査ごとの停止性を都度証明する方針を続けるか / 今回のように注入コードの無限ループはレンダラごと固めてユーザーの手が完全に無くなるため、`vim-scroll.js:scrollableAncestor`（shadow ホストを跨ぐ走査）を含め、今後同種のコードを足すたびに同じリスクを個別判断することになる

````

**対応**: P0 なし（収束）。書き換えで済む P1 を反映: (1) CHANGELOG の「2 本指スクロール」を「スクロール（トラックパッドでもマウスホイールでも）」に広げた（wheel 1 発で走査が回るので指摘どおり）。(2) phase1 の前提 check を「scrollingElement が body **かつ** 子フレームに `__nemoSwipeAttached === true`」に書き換え、注入が無いときの空振りを検出できるようにした（新しい check は足さず既存の前提 check に組み込んだ）。(3) VERIFY.md のスワイプ項目の列挙に「DOCTYPE の無い（quirks mode）文書の上でスクロールしても固まらないこと」を足した。見送り（足す修正・終了報告へ）: P1「時間切れ後に navigate へ進むと残りの phase1 が走らない」はタブ作り直し等の仕組み追加になる / P2「単体テストを runInjection に合流」はリファクタで対象が増える / P2「トップフレームの quirks ページも撃つ」は検査項目の追加 / P2「canScroll の getComputedStyle を後ろへ」は既存挙動で今回の差分外。Q: 「[Unreleased] に積むか 1.2.8 で単独で切るか」はユーザーが「リリースは自分で叩く」と決めているので閉じた（根拠: 会話での決定）/ 「注入する走査に一律のステップ上限を入れる方針」は設計判断で今回の範囲に入れない。
