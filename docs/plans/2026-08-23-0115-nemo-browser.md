# Nemo: 自作ブラウザ（Arc 代替）

## 概要・やりたいこと

Arc から自作ブラウザ「Nemo」へ乗り換える。

Arc の速度や基本的な使い勝手には満足しているが、以下の理由で自作したい:

- **かゆいところに手が届くようにしたい**: 使っていく中で「こういう機能が欲しい」と思ったときに、すぐ自分で実装できる状態にしたい
- **不要な機能を視界から消したい**: Arc の AI 機能などほとんど使っていない機能が UI を占めている
- **PC 移行・複数端末での共有が面倒**: Arc はピン留めアイテムなどが移行できず、端末間の共有も難しい
- **将来的なパーソナライズの土台**: 自分の経歴・好み・個人情報を環境変数やファイル・DB といった永続層に持たせられれば、本当に自分向けの AI 補助機能を載せられる。ただし具体的なアイディアは現時点では無く、**まず「明らかに必要なもの」だけを作り、使いながら育てる**

現時点のゴールは **「Arc 相当の使い勝手を Nemo で再現し、既定ブラウザを Nemo に切り替える」** こと。自作機能はその後に足していく。

## 前提・わかっていること

### 事実確認したこと

#### Arc について

- **Arc は Electron ではない**。Swift のネイティブ UI + Chromium を直接組み込んだアプリ（Vivaldi / Brave と同系統）。だから Chrome 拡張も DevTools も完全互換で動く
- **Arc のデータは素の JSON で読める**（移行スクリプトは機械的に書ける）:
  - `~/Library/Application Support/Arc/StorableSidebar.json`（2.5MB）
    - `sidebar.containers[1].spaces` … スペース定義（2件）
    - `sidebar.containers[1].items` … タブ・フォルダ（111件）。`data.tab.savedURL` / `savedTitle`、`childrenIds` でフォルダ階層、`parentID` で所属
    - `sidebar.containers[1].topAppsContainerIDs` … Favorites（サイドバー上部のアイコングリッド）
  - `~/Library/Application Support/Arc/StorableArchiveItems.json`（1.1MB, 1326件）… アーカイブ済みタブ

#### `electron-chrome-extensions@4.9.0`（npm から取得して LICENSE / README を直接確認）

- **ライセンスは GPL-3.0 と Patron License のデュアル**。npm 上は `SEE LICENSE IN LICENSE.md`、README は "GPL-3. For proprietary use, sponsor me" と明記。**MIT ではない**
- **README の Limitations に明記された制約**:
  - `Chrome extensions are not supported in non-persistent/incognito sessions.` → **incognito セッションに拡張はロードできない**
  - `Usage of Electron's webRequest API will prevent chrome.webRequest listeners from being called.` → **Electron 側で `webRequest` を使うと拡張側の `webRequest` が死ぬ**。広告ブロックを内蔵する場合は宣言的ブロック（DNR）で実装するか、拡張に寄せるかの二択
  - `Service worker preload scripts require Electron's sandbox to be enabled.` → **`sandbox: true` は セキュリティ要件であると同時に拡張の動作要件**
  - `The latest version of Electron is recommended. Minimum support requires Electron v35.0.0-beta.8.` → **動作する組み合わせは自分で固定するしかない**
  - すべての background script が persistent 扱いになる
- 現在の Electron 最新は **43.4.1**。Electron 42 以降で `electron-chrome-extensions` のアイコン・popup が壊れるという未解決報告がある（`samuelmaddock/electron-browser-shell#184`）ため、**「最新版」ではなく本番採用予定の組み合わせで検証する**

#### Electron ネイティブの拡張 API サポート（公式ドキュメントを確認）

- Electron 自身が対応を明記しているのは **`chrome.devtools.*` / `chrome.extension`(部分) / `chrome.management`(部分) / `chrome.runtime`(部分) / `chrome.scripting` / `chrome.storage`(**`local` のみ**) / `chrome.tabs`(`sendMessage`・`reload`・`executeScript`・`query`・`update` のみ) / `chrome.webRequest`** だけ
- **`chrome.declarativeNetRequest` は一覧に無い**
- → 実用的な拡張互換は**ほぼすべて `electron-chrome-extensions` が補っている**。この依存の重さが Phase 0 をゲートにする理由そのもの

#### `electron-chrome-web-store@0.13.0`（MIT。README の API 定義を直接確認）

- CRX3 を自前で剥がす実装は不要
- **`installExtension(extensionId, options)` にバージョン指定パラメータが存在しない**（options は `session` / `extensionsPath` / `loadExtensionOptions` のみ）。**Web Store の最新版しか入らない**
- **`installChromeWebStore({ autoUpdate })` の既定値は `true`**（起動時と5時間ごとに自動更新される）
- 一方で回避手段がある:
  - `allowUnpackedExtensions` … unpacked 拡張のロードを許可する
  - `loadAllExtensions(session, extensionsPath, { allowUnpacked })` … **指定ディレクトリから直接ロードできる**（Web Store を経由しない）
  - `allowlist` / `denylist` … インストール可能な拡張 ID を制限できる
- → **バージョン固定は「Web Store から入れる」のでは成立せず、不変 artifact を自分で保持して直接ロードする**設計が必須

#### 開発環境

- 開発機は個人 PC（Apple Silicon / macOS 15）。Node v24.13.0 / pnpm 10.33.0 / gh 導入済み。**`age` は未インストール**（Brewfile 経由で追加が必要）
- 署名・notarize の基盤は既存の自作アプリのものをそのまま使える（Developer ID Team と notarytool プロファイルは手元の設定を参照する。**public repo には書かない**）

### 決定事項

| 論点 | 決定 |
|---|---|
| アプリ名 | **Nemo**（常用版 `local.nyshk97.nemo` / dev 版 `local.nyshk97.nemo.dev`） |
| 土台 | Electron + `BaseWindow` + タブごとの `WebContentsView` + `electron-chrome-extensions` |
| ライセンス / repo | **本体 repo を public にして GPL-3.0 で配布**。`electron-chrome-extensions` の copyleft に準拠する |
| UI | TypeScript + React。**実装前に DESIGN.md と HTML モックで見た目を確定する** |
| 拡張 | 主要なもの（Bitwarden 等）が動けば OK。動かない小さい拡張はブラウザ本体の機能として作り直す |
| 拡張の取得・更新 | **`autoUpdate: false`**。lock した**不変 artifact を保持して直接ロード**する。更新は明示的な操作でのみ行い、失敗したら旧 artifact に戻す。lock は本体 repo が source of truth で、同期 repo には**写しだけ**置く |
| サイドバー | **スペース概念なし**。Favorites / ピン留め（フォルダ可）/ 一時タブ の3層フラット。**Favorites とピン留めは「枠（定義）」で、タブ実体は押した時点で生まれる**（起動時に作らない）。フォルダは**1階層**。どの行もダブルクリックでリネームできる（`docs/plans/2026-08-24-2014-pins-favorites-rename.md` で作り直した） |
| 自動アーカイブ | 既定 **24時間**。設定で変更・OFF 可（`tabArchiveHours`）。対象は**一時タブだけ**で、アクティブ・音の出ているタブ・ピン留めは触らない。閉じるが消さず、⌘Y のアーカイブから開き直せる |
| セッション | 単一セッション + **拡張なしのシークレットウィンドウ**（Chrome の既定挙動と同じ）。シークレットは**全ウィンドウで1つの非 persist partition**を共有し、最後の1枚を閉じた時点で storage / cache / HTTP 認証 / 権限の記憶 / ダウンロード一覧を消す。次に開くときは別の partition を採る |
| ウィンドウ | 複数ウィンドウ対応、左右分割なし |
| 同期 | private git repo に JSON を置き、**手動** pull / push。全端末で完全に同じ状態 |
| 同期の境界 | **常用データ（Application Support）と git 作業コピー（staging）を分離**し、検証を通してから原子的に import / export する |
| データ形式 | 同期対象（設定・ピン留め・Favorites）= 人間が読める JSON / 履歴・アーカイブ = ローカル SQLite（履歴は FTS5 の trigram で全文検索） |
| シークレット | `age` で暗号化して同期 repo に格納。**ただし利用者が現れる Phase 3 まで着手しない** |
| キーバインド | Arc を踏襲（⌘T コマンドバー / ⌘S サイドバー / ⌘D ピン留め等）。Nemo 独自に ⌘Y ライブラリ / ⌘, 設定 / ⌘⇧P シークレット。設定ファイルで変更可 |
| Arc からの移行 | `StorableSidebar.json` をパースする冪等なスクリプトとして実装 |
| 配布 | notarize + GitHub Release + アプリ内自動更新（electron-updater） |
| Chromium 追従 | Renovate で自動 PR。**拡張互換の smoke test を必須ゲートにし、落ちたら Electron を据え置く** |
| dev 版 | 表示名・bundle id・アイコンを分離。**データも分離**し、常用版からコピーするコマンドを用意 |
| repo 構成 | `nemo`(**public**, GPL-3.0, ソース + Release) / `nemo-config`(private) の**2つ** |
| 移行の進め方 | 段階移行（Arc と併用期間を置き、Phase 2 完了時に既定ブラウザを切り替え） |

> **repo を3つから2つに減らした理由**: 配信 repo を分ける目的は「ソースを private に保ったまま配布 URL を公開する」ことだったが、本体を public にした時点でその理由が消えた。GPL のソース提供義務の観点でも、バイナリとソースが同じ repo にある方が対応が自明になる。既存の自作アプリと運用を揃えたいなら分けたままでもよい。

### 認識しているリスク

1. **Arc のログインセッションは移行できない**。乗り換え時に全サイト再ログインになる（Bitwarden があるので現実的だが、2FA のあるサイトは手間）
2. **Electron と `electron-chrome-extensions` の互換性は壊れやすい**。Electron を上げた瞬間に拡張が死ぬ可能性があるので、last-known-good の組み合わせを記録し、いつでも据え置ける運用にする
3. **拡張のバージョン固定は Web Store 経由では成立しない**。不変 artifact を自分で保持しない限り、2台でバージョンが揃わない・lock 版に戻せない
4. **多数タブのメモリ**。`WebContentsView` はタブごとに実プロセスなので、非アクティブタブの sleep / discard 機構が必要。また **`BaseWindow` を閉じても子 `WebContentsView` の `webContents` は自動破棄されない**ので、明示的に破棄しないとリークする
5. **任意のサイトを開くアプリなので、セキュリティ境界の設計を最初に固める必要がある**。Electron は permission handler を設定しないと権限要求が自動許可され得る
6. **本体 repo が public になるため、個人情報をコードに入れられない**。ブックマーク・履歴・シークレットはすべて `nemo-config`（private）側に置く

## 実装計画

### 事前準備 [人間👨‍💻]

- [ ] Phase 0 用の**テスト用 Bitwarden アカウント**を作る（実 Vault をスパイクに投入しない）

---

### Phase 0: スパイク — 採用可否のゲート [AI🤖]

**目的**: 本体を作り始める前に「この技術構成で行けるか」を確定させる。ここが落ちたら土台の選択からやり直す。

#### 0-1. バージョンを固定する

- [x] Electron / `electron-chrome-extensions` / `electron-chrome-web-store` / Bitwarden の**バージョンを明示的に pin** する（「最新版」で検証しない）
- [x] `docs/compat.md` を作り、**last-known-good の組み合わせ**を記録する（Electron 42 以降の既知の破壊報告を踏まえ、動く組み合わせを見つけたら必ず残す）

#### 0-2. 最小プロジェクト（セキュリティ既定を最初から入れる）

- [x] Electron + TypeScript + React の最小プロジェクトを作る（`electron-vite` ベース、pnpm）
- [x] `BaseWindow` + `WebContentsView` でページを表示できるようにする
- [x] **`sandbox: true` / `contextIsolation: true` / `nodeIntegration: false`**（拡張の service worker preload の動作要件でもある）

#### 0-3. 最小のタブ / ウィンドウ registry（UI なしでよい）

受け入れ基準（0-5）が複数タブ・複数ウィンドウ・popup からのタブ生成を要求するため、Phase 0 の時点で最低限のモデルが要る。ここを Phase 1 に回すと、**拡張の window API 非互換が Phase 1 に入ってから初めて判明する**。

- [x] tab registry / window registry
- [x] `createTab` / `selectTab` / `removeTab`
- [x] `createWindow` / `removeWindow`
- [x] `extensions.addTab()` と active tab の通知
- [x] **popup が生成したタブ / ウィンドウの追跡**

#### 0-4. 拡張のインストールとバージョン固定の方式決定

- [x] ~~`electron-chrome-extensions` + `electron-chrome-web-store` を導入し、**`autoUpdate: false`** で構成する~~ → `electron-chrome-extensions` のみ導入。Web Store 経路は使わないので `electron-chrome-web-store` は依存から外した（ログ > 方針変更）
- [x] `allowlist` で許可する拡張 ID を制限する（lock に載っている ID しかロードしない構造にして、実装として保証した）
- [x] **不変 artifact 方式を確立する**:
  - [x] **Bitwarden 公式 GitHub Release のバージョン付き artifact（`dist-chrome.zip` 等）が使えるか確認する**（使えるなら URL + hash を lock に書くだけで再現可能になり、バイナリを自前で抱えなくて済む）
  - [x] 使えない拡張については、取得した CRX / unpacked を自前の不変 artifact として保持する方式を決める
  - [x] ~~`loadAllExtensions` / `allowUnpackedExtensions` で~~ **lock された artifact を直接ロードする**（`installExtension` で最新版を取りに行かない）→ `loadAllExtensions` はディレクトリを走査して拾うだけで lock との照合ができないため、lock を1件ずつ `session.extensions.loadExtension()` に渡す自前ローダにした
- [x] browser action のアイコンと popup を仮のツールバーに置く

#### 0-5. Bitwarden の受け入れ基準（「一度動いた」では合格にしない）

**実アカウントの Bitwarden で確認済み（2026-08-23）。**
自動検証は `mise run verify` と `mise run verify:ext-update`。手順は `VERIFY.md`。
チェックが付いていない項目は残していない。Phase 0 のゲートに載せられないと判断したものは
取り消し線を引いて移管先を書いてある（理由は ログ > 方針変更）。

- [x] popup が開き、~~テスト用~~ Vault をアンロックできる（実アカウントで確認。※初回ログイン時に popup がスピナーで止まる問題あり → ログ）
- [x] ~~テスト用ログインページ~~ 実サイト（Qiita）のフォームに自動入力できる（popup の項目クリックから）
- [x] **Nemo を再起動しても** Vault 状態と拡張の設定が期待どおり復元される（実機で確認。`chrome.storage.local` の永続化は自走検証済み）
- [x] ~~**service worker を停止・再起動しても** popup と自動入力が動く~~ → **Phase 1-10 に移管**（ログ > 方針変更）
  - Electron に service worker を止める API が無く、idle 停止を待つ検証は非決定的で Phase 0 のゲートに載せられない
  - 起動し直せること（`restartServiceWorkers`）と、SW が落ちても `chrome.storage.local` が残ることは自走検証済み
- [x] **複数タブ・複数ウィンドウで対象タブを取り違えない**（実機で確認）
- [x] **iframe 内のフォーム**でも期待どおり動く（実機で確認）
- [x] popup からの新規タブ / ウィンドウ生成が Nemo のタブモデルに乗る（`chrome.tabs.create` / `chrome.windows.create` を自走検証済み）
- [x] **拡張を更新した後**も動く（更新 → 動作確認 → 旧 artifact へのロールバックまで）
  - `mise run verify:ext-update` で機械検証済み: 実物の Bitwarden artifact で 2026.8.0 → 2026.7.0 → 2026.8.0 と往復し、**各段で起動し直して**「拡張 ID が不変」「`chrome.storage.local` の内容が引き継がれる」ことを確認した（ロールバック後の再読み込みも含む）
  - ~~更新後に実 Vault で自動入力が動くかの目視~~ → **Phase 1 の確認項目に移管**（ログ > 方針変更）
- [x] ~~⌘⌥I で DevTools が開く~~ → **Phase 1-7 に移管**（ログ > 方針変更）
  - DevTools が開くこと自体は IPC 経由で自走検証済み。残っているのは「アクセラレータのキー入力が届くか」だけで、これはキーバインド全体を扱う Phase 1-7 の範囲

#### 0-6. ライセンス整合の確認

- [x] `electron-chrome-extensions` の GPL-3.0 に準拠する形（本体 repo を public / `LICENSE` に GPL-3.0 / 依存ライブラリの notice 同梱）を確定する
- [x] 他に採用するライブラリのライセンスも棚卸しし、copyleft と衝突しないことを確認する

#### 0-7. 判定

- [x] 検証結果をレポートにまとめる（動かなかった `chrome.*` API があれば列挙する）
- [x] 採用する組み合わせを `docs/compat.md` に確定版として記録する

### Phase 0 の判定 [人間👨‍💻]

- [x] スパイクを実機で触って、Bitwarden が実用に足るか判断する → **採用**（2026-08-23）
- [x] ~~**落ちた場合**: Chromium フォークへの方針転換を検討する（判断は別途 `/dig` で行う）~~ → 落ちなかったので不要

> 実アカウントの Vault をアンロックし、実サイト（Qiita）への自動入力・複数タブ / ウィンドウ・iframe まで確認した。
> 見つかった問題（初回ログイン時の popup スピナー / アンロック後のアイコン更新遅延 / 拡張のキーボードショートカット無効）は
> いずれも Nemo 側の実装で潰せる範囲で、土台の選択には影響しない。詳細は `docs/phase0-report.md`。

---

### Phase 1: 素のブラウザ — Arc と併用できる状態にする [AI🤖]

**完了条件**: 「Nemo だけで一日ブラウジングできる」。ただし既定ブラウザはまだ Arc のまま。
この完了条件を満たすため、**ダウンロード・セッション復元・権限ダイアログ・ページ内検索などを Phase 2 から Phase 1 に前倒し**している。

#### 1-0. セキュリティ境界（最初にやる）

- [x] **ブラウザ UI と Web ページを別の `WebContents` として扱う**（UI 側に Web ページを絶対に同居させない）
- [x] すべての Web ページ側 `WebContents` に `sandbox: true` / `contextIsolation: true` / `nodeIntegration: false`
- [x] **`webSecurity` を無効化しない**
- [x] **リモートページ向けの preload には特権 API を一切公開しない**（UI 用 preload と明確に分ける）。**UI 用 preload で公開する API も個別に列挙する**（オブジェクトを丸ごと渡さない）
- [x] IPC ハンドラで **sender・引数・操作対象タブを検証する**（悪意あるページが他タブを操作できないこと）
- [x] **origin 単位の permission request / check handler** を実装する（未設定だと自動許可され得る）
  - 確認なしで許可するのは `fullscreen` / `clipboard-sanitized-write` / `pointerLock` / `keyboardLock` のみ。
    それ以外は origin 単位でダイアログを出し、「今後も同じ扱いにする」を選んだときだけ `permissions.json` に記憶する
- [x] `setWindowOpenHandler` と外部 protocol の allowlist（`mailto:` などを無条件に OS へ渡さない）
  - allowlist に載っていても**初回は必ず確認する**
- [x] **通常ナビゲーションで許可する scheme を定める**
  - 原則 `http:` / `https:` のみ
  - Nemo 内部ページは専用 scheme のみ
  - **`javascript:` / `data:` / `file:` の扱いを明示する**（既定は拒否）→ `DENIED_SCHEMES` に列挙し、1件ずつ回帰テストしている
    （`file:` は 2026-09-02 に「人間の操作が起点のときだけ通す」を追加。`docs/plans/2026-09-02-1812-local-file-open.md`）
  - **リダイレクト後の scheme も検査する**（初回だけ見て通さない）
  - **コマンドバーの入力をそのまま `loadURL()` に渡さない**（正規化と検証を挟む）
  - [x] 許可外 scheme を拒否する回帰テストを置く（`scripts/navigation-policy.test.mjs`）
- [x] **ブラウザ UI を `file://` ではなく制限付きの custom protocol で配信する**
  - [x] `registerSchemesAsPrivileged` で **`standard: true` / `secure: true` / `bypassCSP: false`**
  - [x] **UI に厳格な CSP** を設定する（本番は `script-src 'self'`。dev だけ HMR のぶん緩める）
  - dev も同じ `nemo://ui` origin を通す（main が Vite dev server に中継する）。
    dev だけ `http://localhost` を直接読む作りにすると、origin と CSP が本番と変わって穴が見えない
- [x] **production build で Electron fuses を検査する**（`runAsNode` 等の無効化を確認）→ `scripts/check-package.mjs`
- [x] 証明書エラー・HTTP 認証・renderer crash の扱いを決めて実装する
  - 証明書エラーは既定で拒否。続行はその場限りで記憶しない
  - HTTP 認証はブラウザ UI 側のダイアログで受ける（資格情報はログに出さない）
  - renderer crash は `crashed` / `oom` のときだけ1回自動で読み直す
- [x] 上記を回帰テストとして自動検証できる形にする（`scripts/verify-phase1.mjs`）

#### 1-1. プロジェクト基盤

- [x] **public にする前に、コミット済みの内容から個人情報を落とす**（履歴に残ると消すのが面倒なので push の前にやる）
  - 該当箇所を伏せ字にし、**履歴を作り直してから** public にする方針に決めた（ログ > 方針変更）
  - `git log -p` で全履歴を grep してから push する
- [x] リポジトリ `nemo`(public, GPL-3.0) を作成し、現在の `~/browser` を接続する
  - https://github.com/nyshk97/nemo （履歴は Phase 0 / Phase 1 の2コミットに作り直した）
- [x] `LICENSE`（GPL-3.0）と依存ライブラリの notice を配置する
  - notice は `mise run licenses` が `node_modules` から生成し、パッケージのたびに成果物へ同梱する
- [x] **public repo なので、個人情報・シークレットを絶対にコミットしない**運用を README と `.gitignore` に落とす
- [x] `.mise.toml` に `[tasks]` を定義（`dev` / `build` / `lint` / `test` / `verify` 等。すべて日本語 `description` 付き）
- [x] lint / format / typecheck をセットアップし、CI（GitHub Actions）で回す

**パッケージング（開発起動では露見しない問題を早期に潰す）**

- [x] **packaging tool を確定する** → electron-builder 26.15.3
- [x] ~~**`better-sqlite3` を対象 Electron の ABI 向けに rebuild する**~~ → **rebuild は不要だった**（ログ > 方針変更）
- [x] 必要なら `asarUnpack` に指定する（ネイティブモジュールと preload。拡張は `extraResources` で asar の外）
- [x] 対象 architecture を決める → **arm64 のみ**（使う 2 台とも Apple Silicon。x64 が要るときに足す）
- [x] **`electron-chrome-extensions` の preload script が成果物に含まれることを検査する**（`scripts/check-package.mjs`）
  - `electron-chrome-web-store` は Phase 0 で依存から外したので対象外
- [x] **開発起動ではなく、clean build した `.app` で SQLite と Bitwarden の smoke test を通す**（`mise run verify:packaged`）

#### 1-2. タブとウィンドウの所有モデル

**データモデルを先に明記する**（`WebContents` は一度に1つの `WebContentsView` にしか表示できないため、曖昧なままだと実装が破綻する）:

- Favorites / ピン留めの**定義**は全ウィンドウ共有
- **実体化したタブと一時タブは必ず1つの `windowId` に所属する**
- **ピン留め項目（定義）と、開いているタブ実体は別 ID** として扱う
- 別ウィンドウへの移動は**所有権の移動**として扱う
- 各ウィンドウが自分の active tab を持つ
- タブは `pinnedId` / `favoriteId` の**どちらか一方**にだけ属し、**1 ウィンドウにつき 1 定義 1 タブ**
  （`resolveTabOwnership` が `createTab` の中で保証する。2026-08-24 に追加）

- [x] 上記のデータモデルを型定義に落とし、Phase 0 の registry を本実装に育てる
  - タブ ID を **WebContents.id から独立した `key`（UUID）** に変えた。
    sleep / discard で WebContents を捨てても、ウィンドウを移しても UI から見た ID が変わらない
- [x] `BaseWindow` + タブごとの `WebContentsView`。タブ切り替え・生成・破棄
- [x] **ウィンドウを閉じるときに子 `WebContentsView` の `webContents` を明示的に破棄する**（自動破棄されないため）
- [x] 戻る / 進む / リロード / 停止、ページタイトルと favicon の取得
- [x] リンクのターゲット処理（`target=_blank` / ⌘クリック / 新規ウィンドウ要求）
- [x] **非アクティブタブの sleep / discard**（既定 30 分。設定で変更・OFF 可）
- [x] **renderer crash 時の再読み込み**
- [x] ⌘⌥I で DevTools を開く
- [x] 別ウィンドウへの移動を**所有権の移動**として実装する（WebContents を作り直さない）

#### 1-3. UI 設計の確定（サイドバー実装の前にやる）

- [x] **DESIGN.md を導入する**（`/init-design-md` のテンプレートではなく、Nemo 用に直接書いた）
- [x] **scratchpad に単一 HTML のモックを作り、複数案（A: Arc 忠実 / B: 明るいカード / C: 高密度）をカードで並べる**
  - 擬似タブ・favicon・未読表示・hover・サイドバー開閉アニメーション・DnD・コマンドバーを再現した
- [x] `open` で開き、返答に絶対パスを明記した
- [x] 方向が確定してから React へ移植する → **A 案（Arc 忠実）を採用**

#### 1-4. サイドバー UI（3層）

- [x] データモデル: Favorites / ピン留め（フォルダで入れ子可）/ 一時タブ
  - **2026-08-24 に作り直した**（`docs/plans/2026-08-24-2014-pins-favorites-rename.md`）:
    入れ子は**1階層**まで / Favorites は**専用枠**（タブが `favoriteId` に属し、一時タブ一覧に出ない） /
    ピン・Favorites のタブは**起動時に作らない**（枠を押した時点で生まれ、必ず登録 URL を開く） /
    定義とタブに `customTitle` を持たせて**ダブルクリックでリネーム**できる
- [x] サイドバーのレンダリング（縦タブ、favicon、タイトル、未読表示、sleep 中の薄さ、音の印）
- [x] ドラッグ & ドロップでの並べ替え・フォルダへの出し入れ・層をまたぐ移動
  - 自分自身 / 子孫の中へは動かせないようにしてある（動かすとツリーが消える）
- [x] ⌘D でピン留め、ピン留めタブは閉じてもリストから消えない（Arc の挙動）
  - 2026-08-24: ピン留めと Favorites の排他は**定義ごと移す**（所属だけ付け替えると
    同じ URL が両方の枠に残り、どちらから開いたかで別タブになる）
- [x] 一時タブの行を**ピン留めへドラッグ**して留められる（⌘D と違い、落とした位置に入る）
- [x] 一時タブに見出しを置かず、その先頭に「新しいタブ」行を出す（Arc の並び）
- [x] ⌘S でサイドバー開閉
- [x] Favorites（上部アイコングリッド）の追加・削除・並べ替え
  - 2026-08-24 に**専用枠**へ変更。右クリックでの即削除をやめ、コンテキストメニュー経由にした。
    一時タブをグリッドへドロップして追加できる（空のときも受け皿を出す）
  - favicon は**ページから取れたものだけ**使う。外部の favicon サービスは使わない（CSP と、
    どこを見に行ったかが漏れるのを避ける）。取れないときはホスト名の頭文字で代用する

#### 1-5. アドレスバー / コマンドバー

- [x] ⌘T でコマンドバーを開く（URL 入力 / Google 検索 / **開いているタブへの切り替え** を一つの入力欄で扱う）
- [x] ⌘L で現在タブの URL を編集
- [x] 入力補完（履歴・ピン留め・Favorites・開いているタブを横断）
  - 候補の順番は固定にした（入力のたびに並びが変わると目で追って選べない）
  - **決定先は開き方で決まる**: ⌘T / サイドバーの ＋ は Enter で新規タブ、⌘L は Enter で現在のタブ。
    ⇧Enter はその逆（新規タブモードなら現在のタブ、アドレスモードなら新規タブ）

#### 1-6. 一日使うために必要な基本機能

- [x] **ダウンロード**（保存先の選択・進捗・失敗表示）
- [x] **セッション復元**（正常終了後 / クラッシュ後の両方でタブを復元する）
  - 定期的に書く（終了時にだけ書くとクラッシュで丸ごと失う）
  - 復元直後のタブは **sleep 状態で作る**（数十タブを一斉に立ち上げない）
- [x] **ページ内検索**（⌘F / ⌘G / ⌘⇧G）
- [x] **zoom**（⌘+ / ⌘- / ⌘0）
- [x] **トラックパッドの2本指スワイプで戻る / 進む**（macOS の作法。ログ > 試したこと）
- [x] **キャッシュを無視した再読み込み**（⌘⇧R / 再読み込みボタンの右クリック）
- [x] **フルスクリーン**（⌃⌘F。ページからの全画面要求は permission の自動許可に含めた）
- [x] **外部 protocol の処理**（`mailto:` など。allowlist + 初回確認）
- [x] 権限要求ダイアログ（位置情報・通知・カメラ・マイク）の UI
- [x] HTTP 認証ダイアログ / 証明書エラー画面

#### 1-7. キーバインド

- [x] キーバインド定義を設定ファイルに出し、既定値を Arc 準拠にする
  - 不正な上書き・重複した割り当ては**採用せず理由をログに出す**（黙って既定に戻すと原因が分からない）
- [ ] **アクセラレータのキー入力が実際に届くことを確認する**（**人が実機で見る分**。VERIFY.md「Phase 1 で人が見る分」に手順を書いた）
- [x] メニューバー（macOS 標準の作法に沿ったメニュー構成・日本語ラベル）
  - `globalShortcut` は使わない（背面にいるときも奪ってしまう）。メニュー項目として登録している

#### 1-8. 拡張の組み込み

- [x] Phase 0 の成果を本体に統合する
- [x] browser action のツールバー UI（サイドバー上部のアイコン列 + popup）
- [x] **Vault をアンロックしてもツールバーのアイコンがしばらく古いまま**の問題への対処を入れた
  - `chrome.action.setIcon` 自体は observer 経由で UI に伝わることを確認したので、
    足りていないのは**拡張側がアイコンを描き直すきっかけ**の方だと判断した
  - popup を閉じた直後にアクティブタブの `tab-updated` を1回撃つ（`chrome.tabs.onUpdated` に変換される）
  - **効いたかどうかは実 Vault でないと確認できない**ので、Phase 1 の確認項目に残す
- [ ] ~~**初回ログインで popup がスピナーのまま止まる**（Phase 0 の実機観測 #1）の原因を特定する~~
      → **範囲を絞ったうえで Phase 2 送り**（ログ > 方針変更）
  - **機械検証で潰した仮説**（`mise run verify:ext` の常設チェックにした）:
    popup ↔ service worker の `sendMessage`（両方向）/ 長寿命 port の往復 /
    service worker の書き込みが popup の `storage.onChanged` に届くこと /
    **応答されない `sendMessage` が Chrome と同じに決着すること**（Bitwarden の `sendMessageWithResponse` の形）/
    **popup で WebAssembly が使えること**（manifest の `wasm-unsafe-eval` が効くか）→ **いずれも動く**
  - **実機で範囲が確定した**: 通常のログイン（マスターパスワードのみ）は問題なく通る。
    止まるのは**新規デバイス確認**（`Verify your identity` の認証コード）の経路だけ
  - **ログイン自体は完了している**（同期も含む）。止まっているのは popup の画面遷移だけで、
    **popup を開き直せば Vault が出る**。実害は「新しい端末での初回だけ popup を開き直す」
  - 実 Vault では一度通るともう再現しない。追うには事前準備の
    **テスト用 Bitwarden アカウント**が要る。**Phase 2 で2台目に入れるときに必ず踏む**ので、
    そのときに `mise run dev:popup` で DevTools を開いた状態で再現させて原因を特定する
- [x] 拡張のオプションページ・ショートカットの扱いを決める
  - オプションページ: サイドバー下部の拡張名をクリックで開く（`chrome-extension://` は自分のページのみ許可）
  - ショートカット: `chrome.commands` は未実装のまま（下記）
- [ ] ~~**`chrome.commands` を Nemo 側で実装する**~~ → **Phase 1 の完了条件に含めない**（計画どおり後回し）
- [x] **拡張のタブ / ウィンドウ対応が 1-2 の所有モデルと一致していることを検証する**
  - `TabState` に `webContentsId` / `chromeWindowId` を出し、拡張から見た ID と突き合わせている

#### 1-9. 永続化と診断ログ

- [x] 設定・ピン留め・Favorites を JSON で保存（スキーマとバージョン番号を持たせる）
  - 保存形式は `{ version, data }`。**version を見てから読む**。未来の版は読まない
  - 壊れた JSON は `.broken-<時刻>` に退避してから既定値で作り直す（黙って消さない）
  - 書き込みは一時ファイル + rename（途中で落ちても半端な JSON を残さない）
- [x] 履歴を SQLite に保存（`better-sqlite3`）
- [x] 保存先を決める（`~/Library/Application Support/Nemo/` 配下。dev 版は `Nemo-dev/`）

**診断ログ**（拡張のロード・更新・タブのクラッシュ・同期・自動更新は UI だけでは原因を追えない）

- [x] dev 版 / 常用版それぞれのログファイルを持つ（データディレクトリごと分かれる）
- [x] **安定したイベント名**で記録する（`extension.load_failed` / `tab.crashed` / `navigation.blocked` / `permission.request` など）
- [x] セッション単位のログローテーション（20 世代）
- [x] **URL・フォーム入力値・Vault 情報をログに出さないルール**を決め、逸脱を検出できるようにする
  - 「書く側が気をつける」ではなく**ログの出口で必ず変換する**（`sanitizeDetail`）。
    URL は scheme + host まで、秘密になりうるキーは `[redacted]`、長い文字列は打ち切り
  - 逸脱していないことは `scripts/log-redact.test.mjs` が回帰テストする

#### 1-10. dev 版分離と自走検証の土台

- [x] Debug = dev 版 / Release = 常用版として、**表示名・bundle id・アイコン・データディレクトリ**を分ける
  - 未パッケージの開発起動は**常に dev 版**として動く（常用データを触らせない）
- [x] dev 版アイコンを生成する（右下に DEV リボン）。生成した PNG / icns はコミットする
  - `mise run icons`。SVG を Electron でラスタライズするので、追加のツールを要求しない
- [x] **dev 版でのみ `--remote-debugging-port` を開ける**（常用版では env を渡しても無視して警告を出す）
- [x] CDP 経由で Nemo を操作する検証スクリプトを用意する（`scripts/lib/cdp.mjs` + `verify-phase1.mjs`）
- [x] **拡張互換の smoke test を二段構成で用意する**:
  - [x] **CI 必須（資格情報なし・決定的）**: 自作のテスト用 MV3 拡張（`test-extension/`）で popup / service worker / content script / iframe / tab・window API を検証する（`mise run verify:ext`）。GitHub Actions の job にした
  - [x] **保護 workflow（手動 or Electron 更新時）**: 公式 Bitwarden artifact を取得して `verify-all` を回す workflow を用意した。**資格情報を使う検証（Vault のアンロック・自動入力）は人が実機で行う**
  - [x] **service worker の idle 停止をまたいだ動作確認**（Phase 0 から移管）。`mise run verify:ext-idle`
- [x] `VERIFY.md` を更新し、ここまでの確認手順を記録する

### Phase 1 の確認 [人間👨‍💻]

- [ ] dev 版を数日使ってみて、Arc との差分で致命的に困る点を洗い出す
  - 2026-08-23 に出た分（2本指スワイプ / 再読み込みボタンの大きさとスーパーリロード /
    タブをピン留めへドラッグ / サイドバーの見出し）は**その場で 1-4・1-6 に足して直した**（ログ > 方針変更）
- [ ] 洗い出した点を Phase 2 に反映するか、Phase 3 に回すか判断する
- [ ] **実 Vault の Bitwarden で自動入力が動くことを確認する**（テスト用アカウントでの検証との差分を見る）
- [ ] **拡張を更新した後も実 Vault で自動入力が動く**ことを確認する（Phase 0 から移管。`chrome.storage` が引き継がれることは機械検証済みなので、残りは自動入力の目視のみ）

---

### 配布の実績（2026-08-23）

- **0.1.0 / 0.1.1 をリリースし、常用版を `/Applications` に入れた**（Developer ID 署名 + 公証 + staple）
- **アプリ内更新の通し確認まで済んでいる**: 0.1.0 が入った状態で再起動すると
  30 秒後の自動チェックが 0.1.1 を見つけ、取得 → 終了時に適用 → 再起動後 `0.1.1` で立ち上がった。
  更新後の `.app` も `Notarized Developer ID` として `accepted`

### Phase 2 前の準備 [人間👨‍💻]

- [ ] **Arc を完全に終了する**（起動中は `StorableSidebar.json` が最新でない可能性があるため）
- [ ] Arc のピン留め・Favorites のうち「移行したくないもの」があれば事前に整理しておく
- [ ] **2台目に Nemo を入れるときは `mise run dev:popup`（popup の DevTools つき）で Bitwarden にログインする**。
      新規デバイス確認の popup が止まる不具合（1-8）は**このタイミングでしか再現しない**ので、
      止まった瞬間の Console / Network を取る。取り逃すと次の機会は次の端末まで無い

---

### Phase 2: 常用移行 [AI🤖]

**完了条件**: 既定ブラウザを Nemo に切り替え、Arc をアンインストール可能な状態にする。

#### 2-1. 設定同期

**同期の境界を先に決める**（conflict marker が入った JSON をアプリが読むと壊れるため）:

- **常用データ** = `Application Support` 内の JSON。アプリが読み書きするのは常にこちらだけ
- **staging** = `nemo-config` の git 作業コピー。アプリは直接読まない
- **pull**: staging を更新 → **競合なし・スキーマ正常を検証** → 常用データへ**原子的に import**
- **push**: 常用データの snapshot を clean な staging へ export → commit → push
- **競合中は import / push を停止する**（git 側で解決するまでアプリは触らない）

- [x] リポジトリ `nemo-config`(private) を作成する
- [x] 同期対象の JSON スキーマを確定する（設定 / ピン留め / Favorites / キーバインド / 拡張の lock）
- [x] 上記の境界どおりに pull / push を実装する。**手動トリガー**でよい
- [x] スキーマ検証（バージョン・必須項目）を import の前段に置く
- [x] **import 前のバックアップと rollback** を用意する
- [x] 競合検出時に「git で解決してください」と案内し、CLI で解決できる形にする
- [x] 「新しい Mac でコマンド1発で環境が揃う」導線を用意し、README に書く

#### 2-2. Arc からの移行

- [x] `StorableSidebar.json` をパースして Nemo のデータモデルに変換するスクリプトを書く
  - Favorites ← `topAppsContainerIDs`
  - ピン留め・フォルダ階層 ← `items` の `childrenIds` / `parentID`
  - **スペースは無視してフラット化する**（複数スペースがある場合はピン留めを統合する）
- [ ] ~~`StorableArchiveItems.json` からアーカイブ履歴を取り込む（任意）~~ → **見送り**（ログ > 方針変更）
- [x] 冪等に実行できるようにし、リポジトリにコミットする（使い捨てスクリプトにしない）

#### 2-3. 拡張の運用

- [x] ~~`ext:sync`~~ → `ext:fetch` … **lock された artifact をインストールする**（Web Store の最新版を取りに行かない）
- [x] `ext:update` … **更新操作のときだけ**新しい artifact を取得し、lock を更新する
- [x] lock JSON に **extension ID / version / 取得元 URL / hash** を記録する
- [x] **一時ディレクトリで検証してから原子的に入れ替える**
- [x] **更新に失敗したら旧 artifact に戻す**
- [x] **起動時に manifest 上の ID / version と lock を照合する**
- [x] `ext:outdated` … 新しいバージョンが出ているかだけを確認する
- [x] ~~拡張の lock を `nemo-config` で管理し~~ → **写しを置いて突き合わせるだけ**にした（ログ > 方針変更）。
      2台でバージョンが揃っているかは `mise run config:status` が出す

#### 2-4. 残りの機能

- [x] 一時タブの自動アーカイブ（既定 24時間、設定で変更・OFF 可）
- [x] アーカイブの検索 UI（消えたタブを掘り返せること）
- [x] 履歴の検索 UI（SQLite の全文検索）
- [x] **シークレットウィンドウ（拡張なし）**。Bitwarden が使えないことを UI 上で明示する
- [x] 設定画面

#### 2-5. 既定ブラウザ対応

- [x] `Info.plist` に `http` / `https` のハンドラを登録する
- [x] `setAsDefaultProtocolClient` と、**設定できたかの結果確認**
- [x] **`open-url` を `ready` より前に購読する**（早期に購読しないと起動時 URL を取りこぼす）
- [x] 起動完了前に届いた URL を queue して、準備完了後に処理する
- [x] **起動済み / 未起動の両経路**を処理する
- [x] single-instance 制御
- [x] 外部アプリ（Slack・メール等）から URL を開く E2E 検証

#### 2-6. 配布・更新

- [x] `docs/CHANGELOG.md` を作り、`[Unreleased]` 運用を始める
- [x] ビルド → 署名 → notarize → staple → GitHub Release → 自動更新 feed 公開までを `mise run release` の1コマンドにする
  - 対話を置かない / preflight で全部検査する / 失敗時は bump commit を trap で巻き戻す
  - **dev 版には更新 feed の URL を入れない**（入れると dev が常用版のビルドで置き換わる）
  - canonical な成果物名には全検証を通ったものだけを置く
- [x] アプリ内自動更新（electron-updater）を組み込む
- [x] バージョン表示の導線を2つ置く（起動して0クリックで見える場所 + About パネル）
- [x] Renovate を設定する。ただし:
  - **Electron の更新 PR では 1-10 の CI smoke test を required status check にする**
  - **Bitwarden での最終確認は保護 workflow で行う**
  - **落ちたら Electron を据え置けるようにする**（自動マージしない / pin を維持する）
  - 通った組み合わせは `docs/compat.md` の last-known-good を更新する

### 動作確認 [人間👨‍💻]

- [ ] **GitHub 側の設定**（AI からは触らない）:
      Renovate の GitHub App を `nemo` に入れる /
      branch protection で `CI / lint typecheck unit test`・`CI / extension smoke (macOS)`・
      `CI / package (macOS)` を required status check にする
- [ ] 2台目の Mac（会社貸与 PC）に Nemo をインストールし、同期リポジトリから設定が復元されることを確認する
- [ ] `mise run ext:fetch` で Bitwarden が**同じバージョンで**入ることを確認する
- [ ] 既定ブラウザを Nemo に切り替える
- [ ] しばらく常用して問題なければ Arc をアンインストールする（ゴミ箱移動では完結しないので、ログイン項目・ヘルパーの残骸まで確認する）

---

### Phase 3: 育てる [AI🤖]（継続・優先度は使いながら決める）

MVP には**入れない**と決めたもの。使ってみて必要になった順に着手する。

- [ ] **シークレット保管基盤（age）+ ベーシック認証などの自動入力**
  - `age` を Brewfile 経由で導入する（`brew install` は直接叩かない）
  - age 鍵ペアを生成し、秘密鍵を Keychain に置き、手元のバックアップ先に控える（`git check-ignore -v` で除外を確認するまでやる）
  - シークレットを age で暗号化して `nemo-config` に格納する
  - 新しい Mac での鍵 import 手順を README に書く
  - **保管基盤と最初の利用者（自動入力）をセットで作る**
- [ ] サイト別のカスタム CSS / JS（Arc の Boosts 相当。設定同期に乗せる）
- [ ] URL ルーティング（特定パターンを既存のピン留めタブで開く / 別アプリに逃がす）
- [ ] 広告・トラッカーブロックの内蔵
  - **注意1**: Electron の `webRequest` を使うと拡張側の `chrome.webRequest` が呼ばれなくなる
  - **注意2**: **`chrome.declarativeNetRequest` は Electron 公式の対応 API 一覧に無い**（`electron-chrome-extensions` が補うかどうかも未確認）。**DNR が使えるかをスパイクで確認し、不可なら拡張に寄せる**
- [ ] シークレットウィンドウで拡張を使いたくなった場合の擬似シークレット方式（終了時に cookie / cache / storage / service worker / 拡張の状態を完全破棄する永続 partition。**消え残りがないことの自動検証が必須**）
- [ ] AI 補助機能（自分の経歴・好みを永続層に持たせたパーソナライズ）

## ログ

### 試したこと・わかったこと

- `electron-chrome-extensions@4.9.0` を npm から取得して LICENSE / README を直接確認した。ライセンスは GPL-3.0 + Patron License のデュアルで MIT ではない。README の Limitations に incognito 非対応・`webRequest` の相互排他・sandbox 必須が明記されている
- `electron-chrome-web-store@0.13.0`（MIT）の API 定義を確認した。**`installExtension` にバージョン指定がなく、`installChromeWebStore` の `autoUpdate` 既定値は `true`**。一方 `loadAllExtensions` / `allowUnpackedExtensions` / `allowlist` があるので、Web Store を経由せず不変 artifact を直接ロードする方式が取れる

- Electron 公式ドキュメントの対応 Chrome 拡張 API 一覧を確認した。`chrome.storage` は `local` のみ、`chrome.tabs` は5メソッドのみという極端に狭い範囲で、**`chrome.declarativeNetRequest` は含まれていない**。実用的な互換はほぼ全て `electron-chrome-extensions` に依存している

#### Phase 2 で分かったこと（2026-08-23）

- **Arc の `items` / `spaces` / `topAppsContainerIDs` は `[id, オブジェクト, id, オブジェクト, ...]` の
  交互並び**。オブジェクトだけ拾うと「どの順で並んでいるか」が失われ、
  文字列だけ拾うと中身が無い。両方を別々に読む必要がある。
  実データは 224 要素 = 112 アイテムで、`spaces` も同じ形（4 要素 = 2 スペース）

- **SQLite の FTS5 は既定の tokenizer（unicode61）だと日本語のタイトルが1語になる**。
  「議事録」で「今週の議事録」が引けない。`tokenize='trigram'` にすると
  3文字以上の部分一致が引ける。better-sqlite3 の同梱 SQLite（3.53.4）で
  trigram が使えることを実際に確かめてから採用した。3文字未満は LIKE に落とす

- **`open -a <app> <url>` は LaunchServices 経由でしか届かない**。
  バイナリを直接 spawn したインスタンスに URL を渡そうとすると
  `_LSOpenURLsWithCompletionHandler() failed ... error -600` になる。
  アプリは動いているのに届かないので原因が分かりにくい。**アプリ側も `open` で起動する**。
  古いインスタンスが残っていると単一インスタンス制御で新しい方が即終了し、やはり -600 になる。
  `open` は env を渡せないので、使い捨てプロファイルで試すときは
  `launchctl setenv NEMO_USER_DATA_DIR ...`（後で必ず消す）

- **`open-url` の queue はログファイルが開く前に起きる**。`openLogFile()` は
  `app.whenReady` の後にしか呼べないので、未起動から開かれた経路の
  `open_url.queued` が**ファイルに残らなかった**（コンソールにしか出ない）。
  ログ側にファイルを開く前の行を溜める buffer を足して、開いた時点で書き出すようにした

- **シークレットウィンドウの UI も `view=sidebar` を持つ**ので、
  自走検証が `connectUi` で素直に先頭を拾うと**シークレット側を操作してしまう**。
  そこで作ったタブはセッションに保存されないため、後続の「再起動をまたぐ復元」が落ちた。
  `connectUi` は既定で `private=1` を除外するようにした

#### Phase 1 の dev 常用で分かったこと（2026-08-23）

- **2本指スワイプで戻る / 進むは、main プロセス側だけでは作れない**。Electron は Chromium の
  overscroll history navigation を公開しておらず、`BaseWindow` の `swipe` は**3本指**にしか出ない。
  `webContents.on('input-event')` も **Electron 41 では `type` と `modifiers` しか渡さない**
  （`mouseWheel` でも `deltaX` は `undefined`。実測して確定した）。
  そこで**判定コードを隔離ワールドへ注入**し、ページの `wheel` から判定して `history.back()` を呼ぶ形にした。
  ワールドが分かれているのでページからは覗けず、ページ側 preload を持たない方針も崩さない
- **ページが切り替わった直後には「1つ前のページで払った指の慣性」が流れ込む**。
  素直に数え始めると *戻った直後にもう1ページ戻る*。判定の状態を **`locked` から始めて**、
  指が離れて途切れたら数え始めるようにした（自走検証で踏んで直した）
- **横スクロールの端で「判定済み」にしてしまうと、端に着いても履歴が動かない**（レビュー指摘）。
  ただし「余地があるなら手前で return」にすると、今度は**遷移直後の慣性の抑止が横スクロール領域を
  通り抜けただけで解ける**（続くレビュー指摘）。判定を `feedSwipe` に寄せ、
  「その場所をまだ横スクロールできるか」を引数で渡す形にして、
  **「指が離れたのを見たか（armed）」と「このイベント列では判定しない（locked）」を別に持つ**ようにした。
  兼用にすると必ずどちらかの穴が開く。横スクロール中に**戻してよいのは累積値だけ**で、
  `armed` も `locked` も戻さない（戻すと縦だと判断したジェスチャの抑止まで解ける）
- **`WebFrameMain` には隔離ワールドで実行する API が無い**（`executeJavaScript` だけ）。
  `wheel` は iframe の境界を越えないので子フレームにも入れる必要があり、
  子フレームぶんはページと同じワールドに入れている。渡すのは
  「`wheel` を見て `history.back()` を呼ぶ」だけで、ページが元から持っている能力の範囲
- **ドラッグの落とし先は「抜いてから挿す」ぶんの補正が要る**（レビュー指摘）。
  補正しないと、同じ階層で上から動かしたときだけ1つ後ろに入り、ドロップ線の見た目とも食い違う。
  `movePinned` の `index` を「動かす前のツリーで見た位置」と定義し直して内部で補正した
- **キャッシュ無視の再読み込みは、メインリソースでは差が出ない**（普通の再読み込みでも再検証が走る）。
  差が出るのは**キャッシュ済みのサブリソース**なので、検証用ページは長い `max-age` を付けた
  サブリソースを読み、サーバ側で取りに来た回数を数える形にした

- **拡張の popup が画面左に見切れて操作できなかった**（Bitwarden）。`electron-chrome-extensions` の `PopupView.updatePosition` は既定で**アンカーの右端に popup の右端を合わせる**（= 左へ伸びる）ため、サイドバーが左端にある Nemo では画面外へ出る。`alignment` に `right` を含めると左端合わせ（右へ伸びる）になる。あわせて**ライブラリ側に画面内へ収める処理が無い**ことも確認したので、work area へ押し戻すガードを main に置いた
- **popup のスピナーは「新規デバイス確認の経路だけ」だと実機で確定した**。ログアウトしてマスターパスワードで入り直すと問題なく通る。`loginSuccessHandlerService.run()` は成功したログインすべてで通るので、**犯人はその手前の `logInNewDeviceVerification` 側**（またはその後の画面遷移）に絞られた。ログインも同期も完了していて popup を開き直せば Vault が出るため、実害は初回の1回だけ
- **popup がスピナーのまま止まる件は、メッセージング層の問題ではなかった**。popup ↔ service worker の `sendMessage`（両方向）・長寿命 port の往復・`storage.onChanged` の伝播・**応答されない `sendMessage` の決着**（Bitwarden の `sendMessageWithResponse` は `new Promise((r) => chrome.runtime.sendMessage(msg, r))` なので、callback が呼ばれないと永久に待つ）・**popup での WebAssembly**（Bitwarden の SDK は WASM で、manifest に `'wasm-unsafe-eval'` を宣言している）を全部試したが**すべて期待どおり動く**。7項目とも `mise run verify:ext` の常設チェックにした
- 回帰テストは**ツールバーのボタンを実際にクリックする経路**で書く必要がある。`chrome.action.openPopup()` はウィンドウ右上の擬似アンカー（`anchorRect: { x: width - 64, y: 0 }`）を使い `alignment` を渡さないので、既存の popup テストでは位置の不具合を検出できなかった

#### Phase 0 スパイクで分かったこと（2026-08-23）

- **Vault をアンロックしてもツールバーのアイコンがすぐ切り替わらない**（ロックアイコンのまま。しばらくして badge「2」付きの通常アイコンになる）。`<browser-action-list>` は `crx://extension-icon/...?t=<iconModified>` でキャッシュ回避しているので、アイコン URL の問題ではなく、**Bitwarden がアイコンを更新するきっかけにしているイベントが届いていない**線が濃い。実害は見た目だけなので Phase 1-8 に送った

- **「更新しても拡張の設定が飛ばない」は機械検証できる**。使い捨てのデータディレクトリで `chrome.storage.local` に印を書き、版を張り替えて起動し直して印が残っているかを見ればいい（`mise run verify:ext-update`）。実 Vault を使わずに設計の肝を毎回確かめられる

- **`ext:update` が「lock の派生フィールドを1つ消し忘れる」バグを踏んだ**。版を張り替えるとき `sha256` は空に戻したが `treeSha256` を戻し忘れ、旧版の hash と新版の実体を突き合わせて必ず失敗した。**更新経路は手で回すまで誰も通らない**ので、偽の拡張（鍵を生成して ID を決め、zip をキャッシュに直置き）で「初回 → 更新 → ロールバック」を回す密閉テストを置いた。ネットワークもリポジトリ本体の lock も使わない。バグを戻すと確かに2件落ちることまで確認した
- 失敗したときの状態は壊れていなかった（ツリー hash の照合が原子的な入れ替えより前にあり、`writeLock` はループの後なので、lock も展開済みディレクトリも無傷のまま止まる）
- **`ext:rollback` は git 依存なので、まだ1つもコミットが無いリポジトリでは使えない**。git から復元できないときは `mise run ext:update <version>` を案内するようにした

- **`chrome.commands` が実質未実装**。`getAll()` は manifest の6件を返すが `shortcut` はすべて空文字で、`electron-chrome-extensions` のソースには `globalShortcut` も `commands.onCommand` の dispatch も1つも無い。`onCommand.addListener` は呼べてしまうので「登録できたのに永久に発火しない」形になる。**パスワードマネージャはキーボードで使うものなので、Nemo 側での実装が事実上必須**（Phase 1-8 に追加）
- **実アカウントの Bitwarden で自動入力が動いた**（Qiita のログインフォームに popup の項目クリックからメールアドレスとパスワードが入る）。Vault のアンロックと再起動後の復元も確認できた
- **初回ログインで 2FA コードを送ると popup がスピナーのまま止まる**。ただし**ログイン自体は成功している**（再起動するとアンロック画面が出る）。popup ↔ service worker のメッセージングが疑わしい（`Unchecked runtime.lastError: Could not establish connection.` が出続けている）

- **`electron-chrome-extensions` には「バックグラウンドタブ」の概念が無い**。`store.addTab()` が `tab-added` を emit → `TabsAPI.observeTab` → `onActivated` → `store.setActiveTab` → `impl.selectTab` と流れ、**追加したタブを無条件でアクティブにする**。`chrome.tabs.create({ active: false })` に対して「こちらが selectTab を呼ばない」だけでは足りず、addTab 直後に元のアクティブタブへ戻す必要がある。自動テストを書いて初めて分かった（`window.open` の検証だけでは素通りする）
- **アーカイブの sha256 だけでは拡張の完全性を保証できない**。展開後の JS を書き換えても通ってしまうため、lock に「展開・key 注入まで済んだツリー全体の hash」を持たせ、**ロード前に照合して一致しなければ `loadExtension` を呼ばない**構成にした。Bitwarden のツリーで実測 60ms なので起動時に毎回回せる
- **`localhost:8787` は URL としては scheme `localhost:` に見える**。コマンドバーの入力検証を素直に書くと弾かれてローカル開発が詰む。`host:port` を先に救う必要がある（ナビゲーション判定のユニットテストを書いて発見した）
- `sandbox: true` の preload を CJS で出す件に加えて、**ブラウザ UI の CSP は dev と本番で分ける**必要がある。Vite の HMR は inline script と ws 接続を使うため、本番の `script-src 'self'` のままでは dev server 経由で描画できない

- 採用した組み合わせは Electron **41.10.6**（Chromium 146）+ `electron-chrome-extensions` **4.9.0** + Bitwarden **2026.8.0**。42 系は既知の破壊報告があるため上げていない。`docs/compat.md` に記録した
- **Bitwarden の公式 GitHub Release にバージョン付き artifact があった**（`bitwarden/clients` の `dist-chrome-<version>.zip`）。URL + sha256 を lock に書くだけで再現でき、バイナリを自前で抱える必要は無い
- **GitHub Release の zip には `manifest.key` が無い**。そのままロードすると unpacked 拡張として扱われ、拡張 ID がロード元パスから導出される。Nemo の展開先はバージョンを含むため、**版を上げるたびに ID が変わって `chrome.storage`（= Vault の設定）が失われる**。Web Store の CRX から公開鍵を一度だけ取り出して lock に持ち、展開時に `manifest.json` へ注入することで ID を固定した。2026.8.0 → 2026.7.0 → 2026.8.0 の往復で ID が不変なことを実測で確認した
- **`chrome.declarativeNetRequest` は名前空間ごと存在しない**ことを実測で確定した（Phase 3 の広告ブロック内蔵は DNR ではできない）。`chrome.sidePanel` も名前空間はあるが `setOptions` が無い。一方 `chrome.storage` は `local` / `session` / `sync` すべて動き、`contextMenus` / `webNavigation` / `notifications` / `privacy` も利用できた
- **service worker を止める API が Electron に無い**（`startWorkerForScope` はあるが stop が無い）。「SW を停止してから popup を開く」試験は idle 停止を待つしかないので、UI に再起動ボタンを置いて代替した
- `sandbox: true` の preload は **ESM をロードできない**ため preload だけ CJS で出す必要がある。また `electron-chrome-extensions/browser-action` は preload にバンドルしないと動かない（sandbox 下では `node_modules` を require できない）
- ブラウザ UI とページを**別セッション**（`persist:nemo-ui` / `persist:nemo`）に置いた。同居させると `<all_urls>` の content script がブラウザ UI 自身に注入されうる。`<browser-action-list>` は `partition` 属性で別セッションの拡張を参照できるので分離しても成立する


#### Phase 1 で分かったこと（2026-08-23）

- **`better-sqlite3` は Electron 向けの rebuild が要らなかった**。13.0.3 の prebuild は Node-API なので Electron 41 でそのまま動く。しかも `electron-rebuild` を走らせても `binding.gyp` が prebuild を検出してビルドをスキップするため `build/Release/*.node` は生成されず、**「rebuild したつもり」になりやすい**。実際に必要だったのは asar の外に出すことだけで、それは `mise run verify:packaged` が .app を起動して確認する
- **`findInPage(text, { findNext: false })` は結果イベントが返ってこない**。`findNext` を省略（既定値 false）すれば返る。ドキュメント上は同じはずで、Electron 41 の癖。「ページ内検索が 0 件のまま」から辿って、素の Electron で二分して確定させた
- **Chromium の拡張ローダーは asar の中を読めない**。`fs` は asar を透過的に読むので `manifest.json` の存在チェックは通り、`loadExtension` で初めて落ちるという分かりにくい壊れ方をする。lock された artifact は `extraResources` で asar の外に置いた
- **fuses を書き換えると Electron 本体の署名が無効になり、macOS が起動時に SIGKILL する**（出力も残らない）。配布用の署名をしないビルドでも ad-hoc 署名が要る
- **`@electron/fuses` の `getCurrentFuseWire` は '0'/'1' ではなく文字コード（48/49）を返す**。素朴に文字列比較すると全部 false になり、**false を期待している fuse だけたまたま PASS する**という質の悪い誤判定になっていた（実際に一度そうなった）
- **`chrome.runtime.onInstalled` が発火しない**。自作テスト拡張で確認した。初回セットアップをここに置く拡張は動かない
- **権限要求は「アクティブなタブから」でないとダイアログまで届かない**。背景タブからの `getCurrentPosition()` は Chromium 側で保留され、`setPermissionRequestHandler` まで来ない。自走検証で権限を試すときは対象タブを必ずアクティブにする
- **同じ `.app` が起動したままだと、パッケージ版の検証が新しいプロセスを立てられない**。`mise run verify:packaged` は先に見つけて理由を出すようにした
- **古い Nemo インスタンスが CDP ポートを掴んだままだと、検証が古い方に繋がる**。`pkill -f "scripts/dev.mjs"` では electron-vite の**孫プロセス**の Electron が残る。dev server が死んだ古いインスタンスに繋がって 502 が返り、「custom protocol の中継が壊れている」と1時間誤診した。`mise run dev` は同じポートを掴んでいるインスタンスがあれば起動を拒否し、`dev.mjs` はプロセスグループごと停止するようにした
- **dev server への中継はヘッダを丸ごと転送すると失敗する**。`Host` / `Origin` をそのまま渡すと `net.fetch` が `ERR_FAILED` になる。必要なヘッダだけ通し、GET / HEAD には body を付けない
- **タブ ID を WebContents.id にしたままでは sleep / discard が作れない**。sleep で WebContents を捨てると ID が変わり、UI もセッション復元も追随できない。Nemo 側の ID（`key`）を WebContents から独立させ、`chrome.tabs` との対応は別フィールド（`webContentsId`）で持つ形にした
- **拡張の `removeTab` はタブを閉じる副作用がある**。ウィンドウ間でタブを移すときに `extensions.removeTab()` → `impl.removeTab` → Nemo の `removeTab` と呼び返され、移そうとしたタブが閉じられる。移動中だけ無視するガードを置いた

### 方針変更

#### 配布・更新（2026-08-23）

- **`.app` を公証しただけでは dmg は「未署名」のまま**だった。0.1.0 を配ってから
  `spctl -a -t open` が `rejected: no usable signature` を返して分かった
  （electron-builder の `dmg.sign` は既定 false で、`afterSign` の staple は .app にしか効かない）。
  `dmg.sign: true` と `afterAllArtifactBuild` での dmg 公証を足し、
  **配る前に `stapler validate` で裏を取る**ようにして 0.1.1 で直した。
  検証は「ローカルの成果物」ではなく**配った後にダウンロードしたもの**に対して行う

- **`dist/` を掃除せずに資産を集めていて、0.1.0 の Release に 0.0.0 の dmg が並んだ**
  （署名の動作確認で作った分が残っていた）。ビルド前に `dist/<channel>` を消し、
  資産集めでも今回のバージョン以外が混ざっていたら**配る前に失敗させる**ようにした（回帰テストつき）

- **Phase 2-6（配布・更新）を Phase 2 の頭に前倒しした**。dev 版で一日使える状態になったので、
  常用版を実機に入れて使い始めるほうが Phase 2 の判断材料（Arc との差分）が早く集まる。
  Renovate だけは Phase 2 の残りと一緒に置いた（依存更新は常用が始まってからでよい）

- **`publish` を `electron-builder.yml` に書かず、stable のときだけ CLI で渡す形にした**。
  当初は yml に書いて dev で `--config.publish=null` で打ち消すつもりだったが、
  **electron-builder は "null" という名前の publisher を探しに行って落ちる**。
  さらに **`publish` を一切書かなくても git remote から GitHub を推測して `app-update.yml` を
  埋め込む**ことが分かったので、`afterPack` で dev から取り除く形にした
  （dmg にも残らないよう署名より前で消す）。埋め込みの有無は `check-package` が成果物で検査する

- **常用版の `verify:packaged` は CDP を使わず診断ログで判定する**。常用版は remote debugging を
  開かない設計（開けると拡張の service worker 経由でアンロック済み Vault に手が届く）なので、
  CDP 前提のままでは stable の成果物を一度も起動確認せずに配ることになっていた。
  `app.initialized` と `extension.loaded` をログで待つ経路を足した

- **Team ID と notarytool のプロファイル名は `.release.local.json`（gitignore）に置いた**。
  public repo に書かないという計画の前提を守りつつ、リリースを1コマンドに保つため。
  証明書が1つしかないマシンでは自動解決に落ちる

#### Phase 2（2026-08-23）

- **拡張の lock を `nemo-config` の管理下に置くのをやめた**。lock はアプリ本体に同梱されて
  配られる（`extraResources`）ので、同期リポジトリを source of truth にすると
  **同じ値を2か所で持つ**ことになる。同期には**写しだけ**を置き、
  `mise run config:status` が「2台で版が揃っているか」を突き合わせる形にした。
  計画の狙い（2台でバージョンが揃うことを確認する）はこれで満たせる

- **Arc のアーカイブ（`StorableArchiveItems.json`）の取り込みは見送った**。
  1326 件あるが、Nemo 側のアーカイブは「Nemo で開いていた一時タブ」を掘り返すためのもので、
  Arc の履歴を混ぜると**掘り返したいものが埋もれる**。必要になったら Phase 3 で足す

- **設定画面はファイルの「窓」に留めた**。キーバインドまで画面で編集できるようにすると、
  `settings.json` と同期リポジトリを両方から書くことになり、
  「どちらが正しいか」が状況依存になる。画面から触るのは頻度の高い数項目だけにして、
  残りは**今どうなっているかを見せるだけ**にした

- **シークレットウィンドウは非 persist の partition にした**（計画どおり素の方式）。
  終了時に消す擬似シークレット方式は Phase 3 のまま

- **シークレットのセッションは全ウィンドウで1つ共有する**（Chrome と同じ。レビュー指摘 5 の対処）。
  当初はウィンドウごとに `nemo-private-<n>` を作っていたが、
  **タブを別のシークレット窓へ移せない**（partition が違って `moveTabToWindow` が拒否する）。
  2枚目でログインし直す羽目になるうえ、⌘⇧N が「空の窓だけ増える」という壊れ方をした。
  併せて、**最後のシークレット窓が閉じたら中身を明示的に消す**ようにした。
  Electron は `session.fromPartition` の返り値を内部でキャッシュするので、
  「参照が消えれば勝手に消える」に任せると、UI に出している
  「閉じると跡形もなく消える」が Chromium の都合に依存してしまう

- **シークレットの「今後も同じ扱い」を常用プロファイルに書かない**（レビュー指摘）。
  権限ダイアログの記憶は**既定で ON** なので、シークレットで一度許可しただけの origin が
  `permissions.json` に残り、通常ウィンドウでも黙って自動許可される経路になっていた。
  権限の記憶に scope を持たせ、シークレットは partition ごとの**メモリ上のストア**に置く。
  外部 protocol の記憶も同じ扱いにした

- **シークレットの後始末で消し忘れていたもの**（レビュー指摘）:
  `clearStorageData` / `clearCache` だけでは **HTTP 認証キャッシュが残る**（別 API の
  `clearAuthCache` が要る）。ダウンロード一覧も全ウィンドウ共通なので、
  閉じた後もファイル名と保存先が通常ウィンドウから見えていた（**ファイル自体は残す**が
  一覧のメタデータは消す）

- **シークレットの partition は「世代」で採る**（レビュー指摘）。消去は非同期なので、
  同じ partition を即座に貼り直すと **遅れて終わった消去が、新しいウィンドウで
  書いたばかりの cookie まで消す**。閉じた時点で番号を進め、次のシークレットは
  別の partition を使う。こうすると消している最中に開き直しても干渉しない

- **push が「差分なし」で帰ると、送り損ねた commit が取り残される**（レビュー指摘）。
  commit だけ済んで push が失敗した回の後、次は worktree が clean なので
  何もせず base だけ進み、以後ずっと送られない。差分の有無に関わらず
  「origin に無い commit があれば送る」に変え、**origin に無い commit を base にしない**

- **自走検証で「押したつもり」を作らない**。権限ダイアログの許可ボタンは
  「許可する」なのに `textContent === '許可'` で探していて、**押せていないのに先へ進み**
  「今後もが効かない」という誤った FAIL を出した。押せたかどうかを check に入れた

- **ダウンロードの一覧は scope で絞る**（レビュー指摘）。scope を「終了時に消す」ためだけに使っていて、
  `listDownloads()` は全 scope を返し、しかも全ウィンドウに同じ一覧を配っていた。
  シークレット窓を開いている間、通常窓からファイル名・ホスト・保存先が見え、
  Finder 表示もキャンセルもできる状態だった。**一覧も操作も呼び出し元ウィンドウの scope で絞る**
  （一覧に出さなくても id を知っていれば叩けるので、操作系も必ず突き合わせる）

- **シークレットのセッションは1つを使い回す**（レビュー指摘。世代ごとに作る方式をやめた）。
  `clearStorageData` はデータを消すだけで **Session 自体は破棄しない**ので、
  世代ごとに partition を作ると開閉のたびに Session とハンドラが積み上がる。
  消去の Promise を保持し、**次のシークレットウィンドウは消去が終わってから開く**形にした
  （`openPrivateWindow()` を通す）。消している最中に開いて閉じられた分は、
  消去の完了時にもう一度判定して拾う

- **「一覧に出ない」の検証は両側に1件ずつ作ってから見る**。片方が空だと
  「出ていない」が偶然成立して、絞れていなくても PASS してしまう
  （実際、最初に書いた検証は通常側が 0 件で意味を成していなかった）

- **シークレットの消去に失敗したら、次を開かない**（レビュー指摘）。失敗をログだけにして
  `privateClearing` を落としていたので、**cookie も Basic 認証も残ったセッションで次が開いた**。
  さらに `Promise.all` は最初の失敗で返るため、残りの消去が開き直した後まで走り続け、
  新しく書いた状態を消す競合も再発していた。`allSettled` で全部の完了を待ち、
  1件でも失敗したら次に開くときにやり直す（3回まで）。それでも駄目なら**開かずにエラーを出す**

- **ダウンロードの件数上限も scope ごとに掛ける**（レビュー指摘）。一覧の表示は分けたのに
  上限判定だけ全 scope を混ぜていたので、通常側 49 件の状態でシークレットから 50 件落とすと
  **通常側の古い履歴が押し出されて消えた**（実測で 24 件が巻き添え）。
  判定を `shared/download-cap.js` に出してユニットテストを付けた

- **ダウンロードの上限は「終わった時」にも掛ける**（レビュー指摘）。上限判定は
  「終わっていないものは落とさない」ので、長く走っている1件は超過中も保護される。
  開始時にしか掛けていなかったため、**それが完了した後も上限を超えたまま残り続けた**

- **「起動中か」の判定をプロファイル単位にした**。product 名だけで見ていたので、
  常用の Nemo を開いているだけで**使い捨てディレクトリ相手の操作まで止まり**、
  ユニットテストが実アプリの起動状態で落ちた（同じ HEAD で通る / 落ちるが変わる）。
  ヘルパープロセスが持つ `--user-data-dir=<パス>` で狙ったプロファイルだけを見る。
  既定のディレクトリを見ているときだけ product 名も手掛かりに使う
  （main プロセスはパスを持たないため）

- **`--user-data-dir` の照合は引数まるごとで行う**（レビュー指摘）。`includes` だと
  **`.../Nemo` が `.../Nemo-dev` に前方一致する**ので、Nemo Dev を開いているだけで
  常用側の pull / push / restore が「起動中」と誤判定された。
  パスの直後が空白か行末であることまで見る。データディレクトリのパスには
  空白（`Application Support`）が入るので、`ps` の出力は原理的に曖昧なところがある。
  実在する組み合わせ（`Nemo` / `Nemo-dev`）を回帰テストに固定した

- **シークレットのタブは拡張の API に一切渡さない**。`moveTabToWindow` の
  `extensions.removeTab / addTab` を guard し忘れていて、
  `Invalid WebContents argument. Its session must match ...` が
  **main の例外ハンドラに落ちるだけ**（移動自体は成功する）という気づきにくい壊れ方をしていた。
  自走検証が「main の例外がログに残っていないこと」を見ているおかげで見つかった

- **一時タブの自動アーカイブは `removeTab` に寄せた**。当初は sweep 側で
  「アーカイブに入れてから閉じる」と書いたが、`removeTab` にも
  「閉じた一時タブをアーカイブする」経路があり、**理由（auto / closed）が上書きされて**
  どちらで片付いたか分からなくなった。`removeTab(win, key, { archiveReason })` の
  1経路にまとめた

#### Phase 1（2026-08-23）

- **オーバーレイの状態購読を、バーの中から常時マウントされる `Overlay` に上げた**（レビュー指摘）。
  バーの中で `useWindowState()` を呼ぶと、**開いた瞬間は取得前で `null`**（IPC の往復がある）。
  ⌘L が現在の URL ではなく空欄で開き、後から状態が届いても入力は埋まらない。
  「開いた直後の入力値が現在の URL であること」を検証に足し、
  わざと元の作りに戻すと FAIL することを確認した（PASS するだけのテストにしない）

- **コマンドバーの決定先を「開き方」で決めるようにした**。当初は入力元で分けず
  「Enter は現在のタブ / ⇧Enter は新規タブ」に統一していたが、⌘T とサイドバーの ＋ が
  **新規タブと名乗っているのに今のタブを上書きする**ため、使うと事故になる（実際に踏んだ）。
  オーバーレイの種類を `command-bar`（新規タブ）と `address-bar`（現在のタブ）に分け、
  ⌘T / ＋ は前者、⌘L は後者で開く。⇧Enter は既定の逆。
  「どちらのモードで開いたか」を main が持つ kind で表すことで、
  コマンド通知の到着順に依存せずに決まるようにした

- **拡張 popup のスピナー問題を Phase 2 に送った**。当初「原因は Phase 1 で潰す」としていたが、
  範囲が**新規デバイス確認の初回1回だけ**（しかも popup を開き直せば済む）と分かったこと、
  実 Vault では一度通ると再現しないこと、追うにはテスト用アカウントが要ることから、
  **2台目に入れる Phase 2 で必ず再現するタイミング**に寄せるほうが確実だと判断した

- **自走検証の起動完了判定を、アプリ側の明示的な合図に変えた**（レビュー指摘）。
  起動時のタブは UI のロード完了後に作られるので、「UI の target が出た」だけで読み始めると
  registry が空に見え、**同じ HEAD で PASS と FAIL が入れ替わった**。
  `getAppStatus().ready`（起動時のウィンドウとタブが揃ったか）を出し、
  検証側は必ずこれを待つ。合図が「タブが揃ってから」立つことも回帰テストにした。
  あわせて、空の registry を読んだときに例外で落ちていた箇所を、理由を出して終わる形に直した
- **UI の準備待ちで積んだ処理が、破棄済みウィンドウで走らないようにした**。
  走ると `Object has been destroyed` で main プロセスが落ち、エラーダイアログが出る。
  あわせて `uncaughtException` を握って診断ログに残し（ブラウザごと止めない）、
  **自走検証は毎回「ログに例外が1件も無いこと」を確認する**（握ったまま気づかない、を防ぐ）
- **レビュー指摘の3件を反映した**:
  - **ブラウザ UI の WebContents を `nemo://ui/` から出さないようにした**。UI の preload は
    `window.nemo`（タブ操作・ナビゲーション）を公開しているので、外部ページへ遷移すると
    **そのページに特権 API が渡る**。ガードを外して実測したところ、遷移先の `https://example.com` で
    `typeof window.nemo === 'object'` になることを確認した（実在する穴だった）。
    ナビゲーション自体を拒否したうえで、**IPC 側でも送信元フレームの origin を検証する**二重の防御にした
  - **ピン留めの解除を単一 API（`unpinEverywhere`）に寄せた**。定義は全ウィンドウ共有なのに
    操作元ウィンドウのタブしか紐付けを外しておらず、フォルダを消したときは子孫のぶんも外れなかった。
    紐付いたままのタブはサイドバーのどの層にも出ず、セッションにも古い ID が残るので再起動でも直らない。
    `unpin` が「消えた ID（子孫含む）」を返す形に変え、解除経路（サイドバー / メニュー / ⌘D）を1本化した。
    あわせて**復元時に存在しない定義への紐付けを持ち込ませない**ようにした
  - **オーバーレイの準備完了を待たずにダイアログを送ると取りこぼす**問題を塞いだ。
    取りこぼすと権限・認証の callback が未解決のまま残り、ページが止まる。
    main 側は読み込み完了時に現在の状態を送り直し、UI 側は購読に加えて**マウント時に取りに行く**

- **タブ ID を `WebContents.id` から独立した `key`（UUID）に変えた**。sleep / discard とウィンドウ間移動をまたいで不変にするため。`chrome.tabs` との対応は `webContentsId` / `chromeWindowId` として別に出し、拡張のモデルと Nemo の所有モデルが一致していることを検証できるようにした
- **`better-sqlite3` の「Electron 更新のたびに rebuild が要る」を取り下げた**（ログ > Phase 1 で分かったこと）。更新フローに入れるのは rebuild ではなく **パッケージ成果物の起動確認**（`mise run verify:packaged`）にした
- **dev でも本番と同じ `nemo://ui` origin を通すことにした**。dev だけ `http://localhost:5173` を直接読む作りにすると origin と CSP が本番と変わり、UI の穴が dev では見えない。main が Vite dev server に中継する
- **ダイアログをネイティブ（`dialog.showMessageBox`）ではなくブラウザ UI に出すことにした**。見た目を揃えられるうえ、**自走検証が CDP から答えられる**。ネイティブだと自動検証が「人が押すまで止まる」形になり、権限・認証・証明書の経路がテストできない
- **セッション復元は「復元直後のタブを sleep 状態で作る」形にした**。数十タブを一斉に立ち上げない。選んだ時点で読み込む
- **`chrome.commands` は Phase 1 に入れないまま維持した**（計画どおり）。拡張のキーボードショートカットは使っていないため
- **対象 architecture を arm64 のみに決めた**。使う 2 台とも Apple Silicon なので、universal は成果物が倍になるだけ。x64 が要るときに足す
- **計画ファイルは repo に残し、個人情報だけ伏せ字にして履歴を作り直してから public にすることにした**。plan を private 側に隔離すると、実装と計画が別リポジトリに分かれて追いにくくなるため

#### Phase 0（2026-08-23）

- **回帰テストの後始末を `t.after()` に移した**。「SIGTERM を握り潰す子」や「`kill` を差し替えた子」を意図的に作るテストで、復元処理が assert の後ろにあった。つまり**回帰を検出したときに限って**子が生き残り、テストランナーが終われなくなる（実際に孤児プロセスが残っていた）。バグを戻した状態で走らせ、3件 fail しても孤児が残らず完走することを確認した

- **後片付けの可否を「停止処理が成功したか」で判断していたのが誤り**だった。追跡配列を `splice` で空にしてから停止していたため、途中の停止に失敗しても最後の後片付けは空配列を見て成功扱いになり、使用中の一時ディレクトリを消した。**起動した子は追跡配列から外さず、可否は毎回 `spawned.filter(isChildAlive)` で計算する**形に変えた。危険な書き方が戻っていないかをソースレベルの回帰テストでも固定した
- **`lstat` の全例外を「存在しない」扱いにしていた**（EACCES でも起動中判定を素通りした）。`ENOENT` のときだけ fail-open にした
- **`Promise.race([p, sleep(ms)])` はタイマーを残す**。exit が先に決着してもタイマーが生きているぶんプロセスが終われず、ユニットテスト全体が10秒待たされていた（中身は1.2秒）。`clearTimeout` する `raceTimeout()` に集約した

- **stale マーカーの掃除が無関係なファイルを消せる状態だった**。`.nemo-run` が symlink（例: `.nemo-run -> .`）なら、`package.json` や `extensions.lock.json` が「不正なマーカー」として削除される。マーカー置き場を `lstat` して**通常のディレクトリでなければ検証ごと失敗**させ、対象を `<pid>.json` の**通常ファイル**に限定し、削除は**ファイル名の PID が死んでいるときだけ**にした
- **SIGKILL 後も終了を確認せずに進んでいた**。3秒待って諦めたあと後片付けに入るので「確実に死んでいる」保証になっていなかった。確認できなければ投げて、**一時ディレクトリを消さずに場所を表示する**ようにした
- **検証ハーネス自体にテストを置いた**（`scripts/harness.test.mjs`）。本体より先にハーネスのほうが危険な操作（再帰削除・プロセス停止・lock の書き換え）を持っていたのに、テストが無かった

- **起動中 Nemo の検出を `ps` からマーカーファイルに変更**。`electron-vite dev` は `Electron .` として起動するのでコマンドラインに `out/main/index.js` が出ず、検出をすり抜けて `verify:ext-update` が実行できてしまった。アプリ自身が `.nemo-run/<pid>.json` を書き、検証側は生存 PID を見て stale を掃除する方式にした（`ps` は補助）
- **パス封じ込めを字句比較から symlink 検査つきに変更**。`extensions/<正しい形式の id>` が外部ディレクトリへの symlink なら、`path.resolve` 後の比較は通り抜けたうえで `readdir` がリンク先を列挙し、再帰削除で外を消せた。`safeJoin()` が base を realpath したうえで base から下の各段を `lstat` する形にし、canary 付きの回帰テストを置いた（macOS の一時ディレクトリは `/var -> /private/var` を経由するので、**base より上の symlink は許容**しないと何も通らない点に注意）
- **子プロセスの停止を固定 sleep から exit 待ちに変更**（`stopChild()`）。SIGTERM 後 1.5 秒で次へ進んでいたため、同じ CDP ポートの再利用や、まだ使っている一時ディレクトリの削除と競合しうる状態だった

- **受け入れ基準 0-5 のうち3項目を Phase 1 に移管した**（未チェックのまま放置しない）:
  - `service worker を停止・再起動しても動く` → Phase 1-10。Electron に SW を止める API が無く、idle 停止を待つ検証は非決定的で、採用可否のゲートに載せられないため。起動し直せることと `chrome.storage.local` が残ることは自走検証済み
  - `⌘⌥I で DevTools が開く` → Phase 1-7。DevTools が開くこと自体は IPC 経由で検証済みで、残るのは「アクセラレータのキー入力が届くか」だけ。キーバインド全体を扱う工程に寄せた
  - `更新後に実 Vault で自動入力が動くか` → Phase 1 の確認。`chrome.storage` が引き継がれることは `verify:ext-update` で機械検証したので、残りは目視のみ
- **検証ハーネスが実リポジトリの拡張を差し替えていた**（`verify:ext-update` が userData だけ一時化して lock / extensions / cache は本物を使っていた）。稼働中の Nemo の拡張を巻き込んだので、**lock・extensions・cache まで一時領域に複製して完全に隔離**し、あわせて**Nemo が起動中なら検証系を実行拒否**するようにした
- **`mise run verify` が別プロセスを検証して PASS しうる状態だった**。固定ポート（9333 / 8787）を使い、`verify-spike` にエンドポイントを渡しておらず、起動したページサーバの早期終了も見ていなかった。実際に既存の 8787 サーバを検証して全 PASS した。**毎回空きポートを採番 → `NEMO_CDP` / `NEMO_TEST_PAGES` で明示的に伝播 → 子プロセスの生存と「自分が起動したサーバか」を確認**する形に直した
- **壊れた lock でリポジトリを再帰削除できる状態だった**（`id: ".."` なら削除対象がリポジトリルートになる）。拡張 ID / version / `unpackedRoot` / `source.url` を検証し、削除・展開のパスがすべて `path.resolve` 後に所定のディレクトリ配下であることを確認するようにした。検証は `src/shared/ext-lock.js` に置いて main プロセスとスクリプトで共有する（実装が二重にあったのが遠因）

- **拡張の完全性検証を「アーカイブの hash」から「展開後ツリーの hash」に変更**（レビュー指摘）。ロード前に照合し、不一致ならロードしない。あわせてロード後の ID / version 不一致では `removeExtension()` で必ず外す
- **ナビゲーションで許可する scheme を絞った**（レビュー指摘）。通常ページは `http:` / `https:` と `about:blank`（厳密一致）のみ。`chrome-extension://` はロード済み拡張の ID に一致する場合に拡張自身の経路でだけ許可し、`devtools:` は `loadURL` 経由では一切許可しない。拡張から渡される URL（`chrome.tabs.create` / `chrome.windows.create`）も同じ検証を通す
- **ログに URL のパス・クエリ・フラグメントを出さないようにした**（レビュー指摘）。`redactUrl()` で `scheme://host` まで落とす
- **許可判定を Electron 非依存の `src/shared/navigation-policy.js` に切り出し、`node --test` の回帰テストを置いた**。計画 1-0 の「許可外 scheme を拒否する回帰テスト」を Phase 0 の時点で満たしている
- **`.mise.toml` を Phase 1-1 から前倒しで導入した**。`mise run dev` で「拡張の照合 → テストページのサーバ → remote debugging つきで起動」まで一発にする。起動方法が分からず手動確認に入れない、という状態を解消するため。remote debugging は `NEMO_REMOTE_DEBUGGING_PORT` が明示されていて packaged でないときだけ開く

- **`electron-chrome-web-store` を依存から外した**。Web Store 経由のインストールは lock からの再現ができず、Nemo の「不変 artifact だけをロードする」方針と矛盾する。`installChromeWebStore` を呼ばない以上このパッケージは使い道が無いので、依存ツリーを正直に保つために削除した（`chrome.webstorePrivate` が要るなら Phase 1-8 で戻す）
- **`loadAllExtensions` を使わず自前ローダにした**。`loadAllExtensions` はディレクトリを走査して見つけたものを全部読むだけで、lock との照合ができない。lock を1件ずつ `session.extensions.loadExtension()` に渡す形にして、**ID / version の照合と allowlist を実装として保証**した
- **拡張のロールバックは「lock を git で戻して再 materialize」に決めた**。旧 artifact は `.ext-cache/` に残るのでネットワーク無しで戻せる。`--rollback` のような別コマンドは作らない（lock が唯一の真実であるほうが状態が減る）

- **本体 repo を private → public（GPL-3.0）に変更**。`electron-chrome-extensions` が GPL-3.0 であり、public な Release からバイナリを配布するとソース提供義務が発生するため。あわせて**配信 repo を分ける理由（ソースを private に保つ）が消えたので、repo を3つから2つに減らした**
- **シークレットウィンドウは拡張なしに確定**。Electron が incognito セッションへの拡張ロードに対応していないため。Chrome も既定でシークレット時に拡張を無効化するので、使い心地としては Chrome と同じになる
- **ダウンロード・セッション復元・権限ダイアログ・ページ内検索などを Phase 2 → Phase 1 に前倒し**。Phase 1 の完了条件「一日 Nemo だけで使える」と機能範囲が一致していなかったため
- **拡張のバージョン固定を「lock JSON + Web Store インストール」から「不変 artifact の直接ロード」に変更**。`installExtension` にバージョン指定がなく、lock からの復元ができないため。あわせて `autoUpdate: false` を明示
- **age のシークレット保管基盤を Phase 2 → Phase 3 に移動**。Phase 2 には利用者がおらず、最初の利用者（ベーシック認証の自動入力）が Phase 3 にあるため。常用移行までの距離が縮む
- **パッケージング工程を Phase 1-1 に明示的に追加**。`better-sqlite3` はネイティブモジュールで Electron の ABI 向け rebuild が必要、両拡張ライブラリは preload の同梱確認が必要、といった「開発起動では露見しない」問題があるため。clean build した `.app` での smoke test を完了条件に含めた
- **通常ナビゲーションの scheme allowlist を Phase 1-0 に追加**。popup と外部 protocol だけ制限しても、コマンドバーからの `javascript:` / `file:` やリダイレクト後の scheme が素通りするため
- **Phase 3 の広告ブロックから DNR の断定を削除**。Electron 公式の対応 API 一覧に `chrome.declarativeNetRequest` が無いことを確認したので、「スパイクで確認し、不可なら拡張に寄せる」に修正した
