review session: 49939ad8-b213-44ec-b7ac-8aa8f110d7fb

## 1回目

````text
静的レビューのみ実施（`node --test` の実行は許可されず、テスト・typecheck・prettier は未実行）。

## P0

## P1
- `src/shared/webauthn-shim.js:roamingPossible` — 早期拒否の条件が「`allowCredentials` が空 / 無し」か「全エントリの transports が internal・hybrid だけ」に限られ、**transports を書かないサイトは素通しになる**。/ Microsoft 系は `allowCredentials` に transports を付けない実装が実在し、そこを踏むと今回直したはずの login.live.com / Teams の一部フローが依然 5 分（timeout 無指定時）宙吊りのままになる。「実地で確定」した要求の中身がコードにもテストにも残っていないので、次に壊れても誰も気づけない。/ 実際に固まった要求の `publicKey`（`allowCredentials` の有無・各 `transports`・`userVerification`・`timeout`）を CDP で採取してコメントに記録し、その形をそのままテストの fixture にする。transports 無しが来ていたなら判定側も見直す。
- `scripts/webauthn-shim.test.mjs` — シムの**配達経路（preload バンドル → main world 注入）を見る検査が無い**。/ `permissions-query-shim` は `scripts/verify-phase1.mjs` が isolated world の素の値と main world の値を並べて「配られていない」と「例外で素通しした」を切り分け、`VERIFY.md` にも判定基準がある。今回はユニットテストだけなので、`extension-shim.cjs` の同梱漏れ・chunk 分割・`executeInMainWorld` の失敗は**全部 PASS のまま出荷**される（症状は「また固まる」）。/ `verify-phase1.mjs` に probe を 1 件足す（main world で `credentials.get({publicKey:{challenge:new Uint8Array(32)}})` が即 `NotAllowedError`、`evIsolated` 側は pending のまま＝差分で断言）。`VERIFY.md` にも「素の Electron は宙吊り、シム後は即 NotAllowedError」を書く。
- `src/shared/webauthn-shim.js:callWithTimeout` — timeout で reject しても **native の要求は cancel せず pending のまま残す**。/ 以後その frame の WebAuthn は "A request is already pending" で全滅し、サイトの「もう一度試す」ボタンが別のエラーで死ぬ。コメントは「ページは先に進める」と割り切っているが、cancel する手段（`AbortSignal`）が実際にある以上、放置する理由が無い。/ `AbortController` を自前で持ち、`args[0].signal` があれば `AbortSignal.any([site, own])` に差し替えて（options は浅いコピー）timeout 時に `abort()` する。abort が pending 枠を解放するかは実機で確認する。
- `docs/CHANGELOG.md:[Unreleased] 修正` — 「Bitwarden が保管庫に無い・キャンセルでブラウザへ戻したときに固まらなくなった」が **`create` では成り立たない**。/ `create` の早期拒否は `authenticatorAttachment === 'platform'` のときだけで、attachment 無指定（GitHub 等、パスキー登録の多数派）は素通し → Bitwarden キャンセル後に最大 5 分（timeout 無指定時）固まる。ユーザー向けの唯一の source なので、直っていないケースを直ったと読ませてしまう。/ 「サインイン（get）のパスキー要求」に限定して書き、登録（create）は platform 指定のときだけ即エラーになると明記する。

## P2
- `src/shared/webauthn-shim.js:installWebAuthnShim` — `nativeIsUVPAA()` を要求のたびに await する。/ 判定結果は frame の寿命中に変わらないうえ、万一 Electron 側で isUVPAA が解決しない環境に当たると**シムが全 WebAuthn を宙吊りにする**（今より悪化する）。/ install 時に 1 回だけ呼んで promise をキャッシュし、`catch(() => null)` で素通し側に倒す。
- `src/shared/webauthn-shim.js:wrap` — `answerable(publicKey)` を try/catch していない。/ ファイルの方針は「判定できないときは素通し」なのに、getter が投げるオプション（Proxy・凍結前の細工）だと判定の例外がそのままページへ出て、native とも Chrome とも違うエラーになる。/ `answerable` の呼び出しを try/catch で包み、失敗は `true`（素通し）に倒す。
- `src/shared/webauthn-shim.js:clampTimeout` — 「Chromium の AdjustTimeout と同じ（無指定は 5 分）」というコメント。/ Chromium の `AdjustTimeout` は未指定時に上限側（10 分）を返す実装で、5 分の根拠が確認できない。数字自体は害が小さいが、根拠付きコメントが間違っていると次の人が信じて判断を誤る。/ 出典を確認して数値かコメントのどちらかを直す（Nemo は表示 UI が無いので、Chrome に合わせず意図的に短くするなら「合わせていない」と書く）。
- `src/shared/webauthn-shim.js:installWebAuthnShim` — 早期拒否のゲートが isUVPAA だけ。/ 将来 `app.configureWebAuthn({ enableHybridTransport })` を入れても isUVPAA は false のままなので、hybrid（スマホ QR）が使えるようになったのに hybrid-only の要求を拒否し続ける。/ hybrid の可否は別フラグ（設定 or `getClientCapabilities`）で持てるようにコメントに残すか、ゲートを分ける。
- `src/main/page-shim.ts:registerPageShim` — doc コメントが「http/https 分岐 = `permissions.query` の読み替え」のまま。/ 同じファイルを配るのでシークレットウィンドウにも WebAuthn シムが入っているが、コメントからはそれが読めない。/ 1 行足す。
- `docs/compat.md` — Electron 41 の「modal な WebAuthn 要求は timeout を過ぎても解決も拒否もされない」という実測が、シムのコメントにしか無い。/ compat.md は Electron の版上げ判断の土台で、ここに無いと 42 以降で直ったかを誰も確認しない。/ 「動かない / 使えない」表か Phase 1 の癖に、実測日と CDP での確認手順込みで 1 項目足す。

## Q
- `src/shared/webauthn-shim.js:roamingPossible` — USB セキュリティキーで答えられる要求を素通しする設計だが、**Nemo（Electron 41、`app.configureWebAuthn` 未設定）で USB FIDO が実際に完了するか**が未確認。手元に USB キーがあるか、無い場合「未確認のまま素通し」を続けるか、確認できるまで全 modal 要求を即拒否に倒すか。/ もし USB も宙吊りなら、素通し分岐はただの「最大 10 分固まる経路」で、CHANGELOG の「USB は従来どおり通る」も嘘になる。
- `src/shared/webauthn-shim.js:clampTimeout` — timeout 無指定の素通し要求で **5 分ページが固まる**のを許容するか、Nemo は待ちを表示する UI が無い以上 Chrome より短く（例: 30 秒）倒すか。/ 決めないと「固まらないブラウザにした」という今回の主張が、登録フローでは体感的に成立しないままになる。
- リリース方針 — このシム（拒否で逃がす）で当面固定するか、`app.configureWebAuthn` / プラットフォーム認証器対応を次の作業として積むか。/ 前者なら compat.md と VERIFY.md に「パスキー非対応」を明文化する必要があり、後者ならシムの退き方（isUVPAA true で自動的に素通し）が唯一の出口として妥当か先に決めておく必要がある。

````

**対応**: P0 なし → 収束。反映した P1/P2: `callWithTimeout` に AbortController を持たせ timeout で native を abort（サイトの `signal` は `AbortSignal.any` で合成、合成できない環境はサイトの signal を優先して abort は諦める）/ isUVPAA は install 時に同期で 1 回だけ呼んで結果をキャッシュ、失敗は null / `answerable` を try/catch で包み失敗は素通し / timeout 無指定の既定を 60 秒に変え、コメントを「Chromium は未指定で上限 10 分。Nemo は待ちの UI が無いので意図的に短い」に修正 / hybrid の将来のゲートをコメントに残す / `page-shim.ts` の doc に WebAuthn シムを追記 / CHANGELOG を「サインイン要求（get）は即エラー、登録（create）は platform 指定だけ即エラー、通した要求は timeout で打ち切り、USB は未確認」に書き換え / compat.md に Electron 41 の宙吊りと再確認手順を追記。見送り: `verify-phase1.mjs` への配達経路の probe 追加（検査を足す修正なので終了報告に回す）/ MS の実要求の採取（本人のアカウントが要る。終了報告で依頼）。Q: USB 未確認 → 「拒否しない」側を維持しコメント・CHANGELOG に未確認と明記（根拠: 拒否に倒すと USB キー利用者の唯一の経路を塞ぐ。timeout で有限）/ 既定 timeout → 60 秒に決定（根拠: 待ちの UI が無い。サイト指定は尊重）/ リリース方針 → シムで当面固定、プラットフォーム認証器対応は別作業（根拠: isUVPAA true で自動的に素通しになる出口が既にある。compat.md に明文化）
