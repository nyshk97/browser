review session: 31560272-178b-4245-811f-30daa230ee2d

## 1回目

````text
レビュー結果です。plan の主張はコードで裏取りしました（`openFavorite` の冪等性・IPC ハンドラが同期・`useDelayedClick` の利用箇所・`Sidebar.tsx` / `InlineRename.tsx` が OWNERS 未登録）はいずれも正しく、Phase 1 の設計自体に異論はありません。問題は Phase 2 の前提とドキュメントの範囲です。

## P0
- 前提・わかっていること > コード調査で確認済みの事実 — 「**Favorites のダブルクリックを見る検査は現状無い**」が誤り。`scripts/verify-pins.mjs` の Favorites 節に「閉じている Favorite のダブルクリックで編集に入る」「そのときタブは増えない（Favorites 側でも単クリックの遅延が効いている）」があり、**今回消す挙動を積極的に固定している** / Phase 1 を入れた瞬間にこの 2 件が FAIL するだけでなく、直後の「Favorites のインライン編集が定義に反映される」が dblclick で開いた `.fav-edit .rename` に `value` を流し込む作りなので、入力欄が無くなると `setter.call(null, …)` で例外になりスイートがそこで止まる（＝以降の検査が走らない） / Phase 2 の 1 番目を「新規追加」ではなく**既存ブロックの書き換え**に変える。dblclick の 2 件を反転（編集に入らない・タブがちょうど 1 つ増える）し、インライン編集の反映を見る検査は右クリック →「名前を変更」で入力欄を開いてから値を入れる形に付け替える。ブロック上の「ピン行と同じ規則がグリッド側にも効いているか」のコメントも書き換える
- Phase 2 > 1 — 「遅延撤去の決定打」を `settle()` 相当の待ちで見る設計だと**修正前でも PASS する**。`settle()` は 250ms で `CLICK_DELAY_MS` と同値、さらに `state()` の CDP 往復が乗るので、遅延が残っていてもタブは既にできている（`waitFor` の既定 `interval` も 200ms で粗い） / 決定打のはずの検査が旧実装を落とせず、Phase 2 の「修正前の FAIL を確認」で必ず作り直しになる / クリック直後に `waitFor(ui, 'window.nemo.getWindowState().then(s => s.tabs.length > N ? "ok" : "")', { timeoutMs: 3000, interval: 30 })` でタブ増加を待ち、`Date.now()` の差分を測って `elapsed < CLICK_DELAY_MS` を check する（detail に `${elapsed}ms` を出す。旧実装なら 250ms 超で FAIL）

## P1
- Phase 2 > 1 — 対象セルを `.fav.closed` で拾い続けると狙いがずれる。Favorites 節は定義が 2 件あり、1 回目のダブルクリックで先頭セルが開いて `.closed` が外れるため、後続の右クリック検査は**別のセル**に当たる / 「開いた側で右クリックのリネームが出る」を見たつもりで別セルを見ることになり、壊れても PASS しうる / `shared().favorites[0].id` を取って `.fav[data-id="…"]` で固定する
- Phase 2 > 1 — 新しい検査は**タブを 1 つ開いたまま**終わる。ブロック末尾の後片付けが `resetDefinitions()` だけなので、定義だけ消えたタブが一時タブとして残る / ファイル冒頭が警告している「1 個でも残すとサイドバーの先頭行がずれる（実際に踏んだ）」の再発。今は次ブロックが `closeEphemeralTabs()` を呼ぶので運良く助かっているだけ / ブロックの最後で開いたタブを `closeTab` するか `closeEphemeralTabs()` を呼ぶ
- Phase 3 > 1 — 置換後の文（「ピン留め行・一時タブ行はダブルクリックで名前を変えられる」）だと**フォルダ行が誤りのまま残る**。フォルダは 2026-08-24 の変更で既に右クリックのみ（`docs/CHANGELOG.md:245`、`verify-pins.mjs` のフォルダ検査） / 今回の書き換えで「どの行も」の誤りを直すのに、同じ行の別の誤りを温存することになる / 「ピン留め行・一時タブ行はダブルクリック。**フォルダと Favorites は右クリックの「名前を変更」だけ**」に揃える
- Phase 3 > 3 — VERIFY.md の更新先が pins 節だけになっている。手動確認の「4. **ダブルクリックでのリネーム**（実マウスでの手触り…）」も対象を書き分けないと、人間が Favorites をダブルクリックして「リネームが壊れた」と報告する / 4 番の項目に「Favorites は対象外（右クリックから）」を足す
- Phase 2 > 2 — 「修正前の FAIL を確認」で**何件 FAIL するはず**かが書かれていない。CLAUDE.md が警戒している「速く PASS（検査 0 件）」と区別できない / 反転した 2 件＋遅延の決定打 1 件＝ FAIL 3 件、と期待値を先に書いてから回す

## P2
- Phase 3 > 3 — VERIFY.md の「フォルダのダブルクリックでリネームに入らないこと」は実体が `verify-pins.mjs` にあるのに **Phase 1 の項目リスト**（`verify-phase1.mjs` の節）に紛れている。今回 Favorites の同種項目を pins 節に足すついでに、フォルダの項目も pins 節へ移すと以後の対応付けが崩れない
- Phase 1 > 1 — `onClick` が `if (tab) … else …` の分岐を失って 1 行になるので、`tab` を使う箇所がクラス名・メニュー・アイコンだけになる。コメントは「なぜ遅らせないか」より「なぜ分岐が要らないか（`openFavorite` が開く / 選ぶを吸収する）」の方が実装に近い

## Q
- 決定事項（/dig-lite で確定） > 対象範囲 — 今回でサイドバーのリネーム導線が「フォルダ・Favorites＝右クリックのみ / ピン留め行・一時タブ行＝ダブルクリック可」の 2 系統に割れる。これを**恒久的な仕様として維持する**のか、いずれピン留め行・一時タブ行も右クリックのみへ揃える途中なのかが未決 / 決まらないと `docs/operations.md` の書き方（恒久ルールとして書くか暫定として書くか）と、ピン留め行に残る 250ms 遅延（＝閉じているピンは押しても 0.25 秒開かない体感差）を今後どうするかの判断が保留になる

````

**対応**: P0 2 件を反映（Phase 2 を「新規追加」から既存ブロックの書き換えに変更し、dblclick 2 件の反転・インライン編集検査の右クリック経由への付け替え・コメント書き換えを明記 / 遅延撤去の決定打を `waitFor` + 経過時間 `elapsed < CLICK_DELAY_MS` で見る形に変更し、`settle()` では旧実装でも PASS することを前提に記載）。P1 は全件反映（対象セルを `data-id` で固定 / ブロック末尾でタブを閉じる / operations.md の置換文にフォルダを含める / VERIFY.md 手動確認 4 番に Favorites 除外を足す / 修正前 FAIL の期待件数 3 件を明記）。P2 も反映（VERIFY.md のフォルダ項目を pins 節へ移す / onClick コメントの観点を「なぜ分岐が要らないか」に変更）。Q（リネーム導線の 2 系統化）は自分で決定: /dig-lite で「Favorites だけ」を明示的に選んでいるので、現状の規則としてそのまま書き、ピン留め行・一時タブ行の統一とピン行の 250ms 遅延は今回の範囲に入れないと決定表に追記。

## 2回目

````text
再読しました。前回の P0 2 件・P1 5 件・P2 2 件はいずれも計画に反映されており、決定表の追記（リネーム導線の 2 系統化）も筋が通っています。前提節に検査の実状（既存 2 件が旧挙動を固定・`setter.call(null, …)` で止まる・`settle()` は 250ms で同値）が書かれたので、実装者が同じ罠を踏み直す余地はほぼ消えました。残りは新しく書いた検査手順の詰めだけです。

## P0

## P1
- Phase 2 > 1（遅延撤去の決定打） — `waitFor` の `timeoutMs` と、待ちきれなかったときの扱いが書かれていない / 旧実装（遅延あり）ではタブが 250ms 超で出るので、`timeoutMs` を短く取ると `waitFor` が例外を投げて**スイートがその場で落ちる**。Phase 2 > 2 の「期待する FAIL は 3 件」を観測できず、手順が成立しない / `timeoutMs` は 3000ms 程度を明示し、`catch` して `check('単クリックは遅延しない', false, '時間内にタブが増えなかった')` に落とす（throw させない）
- Phase 2 > 1（ダブルクリックの 2 件を反転） — 撃ってから読むまでの待ちが手順から落ちている / 既存ブロックにあった `await sleep(CLICK_DELAY_WAIT_MS)` を外すと、①遅れて出てくる入力欄（＝退行）を「出ていない」と誤判定する ②タブ生成が状態に反映される前に数えて「0 増」で偽 FAIL する / 反転後も `CLICK_DELAY_WAIT_MS` 待ってから `.fav-edit .rename` の有無とタブ数を読む、と明記する
- Phase 2 > 1（遅延撤去の決定打） — 「別の閉じているセル」の特定方法が未指定 / 前段で先頭セルが開いているので今は `.fav.closed` でも当たるが、それは「先頭が開いた直後」という暗黙の順序に依存する（同じ節の並び順や件数を触った瞬間に別セルへ滑る。1 つ上の bullet で `data-id` 固定にした理由と同じ） / `shared().favorites[1].id` を取って `.fav[data-id="…"]` で固定する

## P2
- Phase 2 > 1（遅延撤去の決定打） — `t0` をどこで取るかが書かれておらず、クリックを撃つ `ui.ev` の往復が `elapsed` に乗りうる / 閾値 250ms に対して余裕はあるので実害は薄いが、遅いマシンでの偽 FAIL を避けるならクリックの eval が返った直後を `t0` にする（またはページ内で `performance.now()` を返して差分を取る）
- Phase 2 > 4 — 「変わらず PASS」の対象を 3 件の列挙で確かめる形になっている / ブロックを書き換えるので Favorites 節の**件数自体**が変わる。CLAUDE.md が警戒する「速く PASS（検査が減っている）」は列挙では捕まらない / 報告に書き換え前後の総件数（`N 件 → M 件`）も併記する

## Q

````

**対応**: 収束（P0 なし）。P1 3 件を反映（`waitFor` の `timeoutMs` 3000ms と時間切れ時の `catch` → check FAIL 化 / 反転後も `CLICK_DELAY_WAIT_MS` 待ってから読むと明記 / 決定打の対象セルを `favorites[1].id` で固定）。P2 2 件も反映（`t0` はクリックの eval が返った直後 / 報告に書き換え前後の総件数を併記）。いずれも既存項目の書き換えで、検査の追加はしていない。
