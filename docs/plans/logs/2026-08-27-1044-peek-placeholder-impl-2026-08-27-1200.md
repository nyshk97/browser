codex session: 01a0410c-3ecc-71e2-bb27-ae4f7c42a8fe

## 1回目

````text
## P0
- `scripts/test-server.mjs:server`（plan `Phase 1 > ステップ 1`）— ゲート保持中のレスポンスを `req.on('close')` で削除している / Node 16以降、このイベントはクライアント切断ではなくリクエスト本文の受信完了時に発火するため、GET 到着直後に `gateHeld` から消え、release が応答を解放できない。[Node HTTP docs](https://nodejs.org/api/http.html#event-close) / `res.on('close')` を使い、`!res.writableEnded` の場合だけ保持一覧から削除する
- `src/main/registry.ts:waitForPeekDocument`（plan `Phase 2 > ステップ 1`）— `did-fail-load` をフレーム判定なしの `once` で購読している / iframe などサブフレームの読み込み失敗でも待機が解除され、トップレベルの `dom-ready` 前に空の Peek View が露出する / `isMainFrame` を確認する `on` リスナーに変更し、トップレベルの失敗・`dom-ready`・timeout のいずれかで全リスナーを解除する
- `scripts/verify-peek.mjs:プレースホルダー検証ブロック`（plan `Phase 3 > ステップ 1`）— `Page.captureScreenshot` の結果を保存するだけで、plan にある中央・面の内側・角丸の外側のピクセル判定をしていない / DOM・computed style・hit test が正しくても、実際の描画色や被覆・合成の問題を検出できず、着手済みステップが未実装 / PNG をデコードし、実画像の中央・境界内側・角外側を色許容差つきで検査する
- `scripts/verify-peek.mjs:R1 selfclose 検証ブロック`（plan `Phase 3 > ステップ 4`）— 「dom-ready が来ない経路」を完了扱いしているが、対象ページは DOM 構築後1.5秒待ってから `window.close()` している / 通常の `dom-ready` で待機解除済みなので、早期 close や timeout の取りこぼしを一切検査していない / inline script で `dom-ready` 前に閉じる専用 endpoint、またはゲート中の WindowProxy を opener から即 close するケースを追加し、Peek の除去とタイマー後の再出現がないことを確認する

## P1

## P2

## Q

````

**対応**: P0 4件すべてに対応した。①test-server のゲート: `req.on('close')` の主張（Node 16 以降は本文受信完了で発火し、GET が即座に握りから外れる）は **Node 24 では再現しなかった**（最小再現で実測。保留した GET は解放されて body が届く）が、版に依存しない形が正しいので `res.on('close')` + `!res.writableEnded` ガードへ変更 ②`waitForPeekDocument` の `did-fail-load` を `isMainFrame` 判定つきの `on` に変え、`stop()` で全リスナーを解除する形に整理 ③`verify-peek.mjs` にピクセル判定を追加（`Emulation.setDefaultBackgroundColorOverride` で透過のまま撮り、`sips` で BMP にして中央・内側の縁・角の3点を見る。PNG デコーダを持ち込まない）。実行したら `#1b1b20` が `#1c1c1f` として読めたので、各チャンネル ±4 の許容差を入れた ④タイムアウト経路の検査を追加（ゲートを解放せず 10 秒待ち、View が出ることと `peek.revealed` の `reason=timeout` がログに出ることを見る）。

## 2回目

````text
## P0
- `scripts/verify-peek.mjs:samplePlaceholderPixels`（plan `Phase 3 > ステップ 1`）— BMP の RGB だけを読み、alpha を捨てている / 実際の成果物では中央が期待 RGB のまま alpha=139 だが検査は PASS するため、祖先の opacity やフェードで半透明・不可視でも「正しい色」と誤判定する / 撮影前にアニメーションを完了させ、alpha も読み取って中央・内側が十分不透明であることを検査するか、既知の不透明色へ合成して最終ピクセルを比較する
- `scripts/verify-peek.mjs:R1 selfclose 検証ブロック`（plan `Phase 3 > ステップ 4`）— timeout 経路は追加されたが、完了条件にある「即 `window.close()` して `dom-ready` が来ない経路」は依然として未検査 / 現在の selfclose ページは DOM 構築後1.5秒待つため、通常の `dom-ready` で待機解除済み / inline script で `dom-ready` 前に閉じる専用 endpoint、またはゲート中の WindowProxy を opener から即座に閉じるケースを追加し、Peek が除去され、8秒後にも再出現しないことを確認する

## P1
- `scripts/verify-peek.mjs:samplePlaceholderPixels` — 固定名の BMP を消さず、`spawnSync('/usr/bin/sips', …)` の終了状態も確認していない / 以前の BMP が残っている状態で今回の変換が失敗すると、古い画像を読んで検査が PASS する / 実行固有の一時ディレクトリを使うか変換前に出力を削除し、`status === 0` と今回生成されたファイルの存在を確認する

## P2

## Q
````

**対応**: P0 2件・P1 1件すべて反映した。①ピクセル判定が alpha を捨てている件: 透過のまま撮るのをやめ、`Emulation.setDefaultBackgroundColorOverride` で地を指定して合成させてから撮る形にし、撮る前に `document.getAnimations()` の完了（暗幕のフェード）を待つようにした。**この検査が本当に半透明を拾うかを故意の壊しで確認した**（`.peek-placeholder` に `opacity: 0.5` を入れてビルド → `#1b1b20` が `#141419` になり FAIL。戻して PASS）。実測の結果はコメントに書いた ②`dom-ready` 前に閉じる経路を追加（ゲートで止めた Peek を中身が来る前に `closePeek` し、その後 10 秒待って「幽霊の Peek が生え直さない」「main の未捕捉例外が無い」を見る。待ち時間は直後の保険タイムアウト検査と共用している） ③`sips` の後始末: 変換前に BMP を消し、`status === 0` と生成の有無を確認するようにした。

## 3回目

````text
## P0
- `scripts/verify-peek.mjs:earlyId（plan Phase 3 > ステップ 4）` — ゲート到達と `visible === false` を確認せずに閉じているため、待機が8秒を超えるとタイムアウト表示済みの Peek を閉じても検査が通る。また、事後のタブ件数では破棄済み Peek のタイマー発火を検出できない / `dom-ready` 前に閉じる経路とタイマー解除を検証したことにならない / 8秒より短い期限で `arrived >= 1` と非表示を必須アサートし、Peek のキーを保存して、待機後にそのキーの `peek.revealed` ログが存在しないことも確認する

## P1
- `scripts/verify-peek.mjs:samplePlaceholderPixels` — アニメーション待機の `Promise.race` は1.5秒のタイムアウトも正常完了として扱う / 描画が遅延・スロットリングされた環境ではフェード途中を撮影して偽FAILになり得る / 完了したかを戻り値で区別してタイムアウト時は明示的に検査失敗とするか、対象アニメーションを `finish()` してから撮影する
- `scripts/verify-peek.mjs:samplePlaceholderPixels` — 背景色オーバーライドの解除が正常経路にしかなく、スクリーンショット取得や書き込みが例外になると設定が残る / 後続検査の描画条件が変わり、二次的な失敗を発生させる / `Emulation.setDefaultBackgroundColorOverride({})` を `finally` で必ず実行する

## P2

## Q
````

**対応**: P0 1件・P1 2件すべて反映した。①早期 close の前提を厳しくした: 閉じる前に「Peek があり、かつ `visible === false`（＝まだ中身待ち）」を必須アサートにし（保険のタイムアウトで既に表示済みだと何も検査していないのに通ってしまうため）、閉じた Peek の key を控えて、待機後にその key の `peek.revealed` がログに出ていないことを見るようにした（タブ本数では破棄済み Peek のタイマー発火を検出できない） ②アニメーション待ちを `Promise.race` の待機から `finish()` での確定に変更（待ち切れずに途中を撮って偽 FAIL になる余地を消した） ③`Emulation.setDefaultBackgroundColorOverride({})` の解除を `finally` へ移した。あわせて検査名の「白地に合成しても」という文言を実測に合わせて直した（実際の合成先は白ではない）。

## 4回目

````text
## P0

## P1

## P2

## Q
````

**対応**: 収束。
