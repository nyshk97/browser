review session: b5e49dea-2652-4c0d-87a8-fd09e0ea813d

# polish-impl review log

対象: リロードボタン右クリックの「通常の再読み込み / ハード再読み込み」メニュー化（Nemo）

## 1回目

````text
レビューしました。差分は 5 ファイル（+ VERIFY / CHANGELOG）で、⟳ 右クリックを native メニュー化し、⌘⇧R のスリープタブ対応を足すもの。型・lint はサンドボックスで実行できなかったので静的読みのみです（`Menu.popup({ window: BaseWindow })` は `src/main/context-menu.ts:attachContextMenu` と同形なので型は通るはず）。

## P0

## P1
- `src/main/menu.ts:runCommandForWindow` / `src/main/ipc.ts:reloadTab` — スリープ中の ⌘⇧R を `selectTab` だけで済ませ、コメントに「materialize が最初から読み直すのでキャッシュ無視と同等」と書いているが、`src/main/registry.ts:materialize` は `wc.loadURL(resolved)` で普通に HTTP キャッシュを使う。つまりハード再読み込みが黙って通常読み込みに落ちる / CHANGELOG の「⌘⇧R が無反応だった → 起こし直し自体が読み直しになる」も同じ誤りをユーザーに約束している（キャッシュ済みサブリソースは取り直されない）。直すなら、起こした後に最初のコミットを待って `reloadIgnoringCache()` を撃つか、`materialize` に「キャッシュを無視して読む」経路（`loadURL(url, { extraHeaders: 'pragma: no-cache\n' })` 等）を足す。実装を変えないなら、コメントと CHANGELOG から「キャッシュ無視と同等」を落として「起こすだけ（キャッシュは効く）」と正しく書く。
- `src/main/ipc.ts:registerIpcHandlers`（`nemo:reload-menu`）— 同じ操作に 2 つの名前がある。popup は「ハード再読み込み」、アプリの表示メニューは `src/shared/keybindings.js` の `reload-ignoring-cache` = 「キャッシュを無視して再読み込み」で、popup 側にアクセラレータ表示も無いため両者が同一だと分からない。ラベルは `COMMANDS` の label を引いて 1 か所に寄せる（必要なら `accelerator: 'CmdOrCtrl+Shift+R'` を表示用に付ける。付ける場合はメニュー非表示時に発火しないことを実機で確認）。
- `src/main/ipc.ts:registerIpcHandlers`（`nemo:reload-menu`）— 新チャンネルに自動検査もログも無い。`scripts/verify-phase1.mjs` にはスーパーリロードの恒久検査があり、直前のコミットでも「verify-phase1 に恒久検査」を足す運用にしているのに、この経路は preload 登録漏れ・ハンドラ名タイポが誰にも気付かれない（右クリックが無反応になるだけ）。VERIFY.md に書いた「不正 key で invoke して `tab does not belong` を見る」を実際に verify-phase1 へ追加し、`src/main/context-menu.ts:attachContextMenu` の `context_menu.open` に倣って popup 時に 1 行 `log()` を残す。

## P2
- `src/main/ipc.ts:registerIpcHandlers`（`run`）— click 遅延ガードが `win.isDestroyed` のみ。`src/main/call-coordinator.ts` や `src/main/registry.ts` は baseWindow がネイティブに閉じた場合を拾うため `win.isDestroyed || win.baseWindow.isDestroyed()` の 2 段で書いており、ここもそれに揃えると `materialize` の `window has been destroyed` throw を踏まない。
- `src/renderer/components/Toolbar.tsx:Toolbar` — tooltip が「再読み込み」だけになり、右クリックにメニューがあることがどこにも出ない。旧 tooltip は挙動が誤解を生んだが、「再読み込み（右クリックでメニュー）」程度は残す価値がある。
- `scripts/verify-phase1.mjs:hits` — 直前のコメントが「スーパーリロード（右クリック / ⌘⇧R）」のままで、右クリック＝即キャッシュ無視という古い仕様を指している。検査自体は `window.nemo.reload(..., { ignoreCache: true })` 直呼びなので有効、文言だけ更新。

## Q
- `src/renderer/components/Toolbar.tsx:Toolbar` — 読み込み中（ボタンが `×`＝停止）の右クリックでも再読み込みメニューを出すかどうかが未決。今は出る（停止ボタンを右クリックすると「通常/ハード再読み込み」が並ぶ）。決めないと、押し間違いで読み込み中のページが再スタートする挙動が仕様なのか事故なのか判断できない。

````

**対応**: P0 なしで収束。P1-1: 実装は変えず、menu.ts のコメントと CHANGELOG から「キャッシュ無視と同等」の誤った約束を削除（materialize は素の loadURL でキャッシュを使うことを registry.ts:647 で確認）。P1-2: keybindings.js のラベルを「ハード再読み込み」に改名し、popup は COMMANDS から引いて 1 か所に寄せた（アクセラレータ表示は実機確認が要るため見送り）。P1-3: log('reload_menu.open') は追加、verify-phase1 への検査追加は「足す修正」なのでループ中は見送り・終了報告へ。P2-1: baseWindow.isDestroyed() の 2 段ガードに変更。P2-2: tooltip を「再読み込み（右クリックでメニュー）」に。P2-3: verify-phase1 のコメント更新（あわせて DESIGN.md の同趣旨の古い記述も更新）。Q: 読み込み中の右クリックでもメニューを出す、を仕様として自分で決定（固まったページからの脱出口として有用・破壊的でない）。
