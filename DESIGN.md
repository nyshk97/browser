# Nemo — DESIGN.md

Nemo のブラウザ UI（サイドバー・コマンドバー・各種オーバーレイ）の見た目の決めごと。
実装で迷ったらここを見る。ここに無いものは「Arc に寄せる」で判断する。

## 方針

- **Arc 忠実**（モックの A 案）。乗り換え時の違和感を最小にすることを最優先にする
- ブラウザ UI は**サイドバー**（枠とタブの一覧）と**ツールバー**（アドレスバーとナビゲーション）の2つだけ
- アドレスバーは**ページ領域の上端**に置く。ページの上に浮かせて被せることはしない
- 装飾より情報の読み取りやすさ。影・グラデーションは使わず、面の明度差と細い区切り線で階層を作る

## カラートークン

ダーク固定（Phase 1 ではライトテーマを作らない）。

| トークン            | 値                      | 用途                                           |
| ------------------- | ----------------------- | ---------------------------------------------- |
| `--nemo-sidebar`    | `#1b1b20`               | サイドバーの地                                 |
| `--nemo-page-bg`    | `#0e0e11`               | ページ領域の下地（読み込み中に見える）         |
| `--nemo-border`     | `rgba(255,255,255,.08)` | 区切り線                                       |
| `--nemo-ink`        | `#e8e8ee`               | 主テキスト                                     |
| `--nemo-ink-dim`    | `#8b8b98`               | 副テキスト・ラベル                             |
| `--nemo-surface`    | `#26262d`               | アドレスバー・入力欄の面                       |
| `--nemo-surface-hi` | `#33333d`               | 選択中のタブ・hover した Favorite              |
| `--nemo-hover`      | `rgba(255,255,255,.06)` | hover の面                                     |
| `--nemo-accent`     | `#5b9dff`               | 未読ドット・選択中の候補・進行中のダウンロード |
| `--nemo-danger`     | `#ff6b6b`               | 失敗・証明書エラー                             |
| `--nemo-ok`         | `#3fb950`               | Approved の状態バッジ（Live Folder）           |
| `--nemo-warn`       | `#d29922`               | 要修正の状態バッジ（Live Folder）              |

## サイズ

| 値                     | 用途                                              |
| ---------------------- | ------------------------------------------------- |
| サイドバー幅 260px     | `SIDEBAR_WIDTH`（main 側の定数と一致させる）      |
| ツールバー高さ 40px    | `TOOLBAR_HEIGHT`（`TRAFFIC_LIGHT_INSET` と辻褄を合わせる） |
| 行の高さ 40px          | タブ・ピン留めの1行                               |
| favicon 16px           | 角丸 4px                                          |
| フォルダのアイコン 16px | フラットな塗り・`--nemo-folder`（水色）。**開閉は形で示し、キャレットは置かない** |
| Favorites グリッド 5列 | 正方形（約 43px）・角丸 8px・gap 6px・アイコン 18px |
| 角丸 7px               | 行・ボタン                                        |
| 角丸 11px              | コマンドバー・ダイアログ                          |
| フォント 13px          | 既定。ラベルは 10px・大文字。letter-spacing は層の見出しが .06em / Live Folder の小見出しが .09em |

信号機ボタンぶんの余白は上端 14px / 左 14px（`trafficLightPosition` と一致させる）。
上端は**ツールバーの中央**に来る値にする（ボタンは 12px なので `(40 - 12) / 2`）。
サイドバー上部の掴みしろ（`.drag-strip`）もツールバーと同じ 40px にして、
信号機とアドレスバーが同じ行に並ぶようにする。

行の中の閉じる（×）は **26px の正方形**。文字を大きくするだけでは押しやすくならないので、
当たり判定ごと広げる。

## ツールバー（ページ領域の上端）

サイドバーとは**別の WebContentsView**（`?view=toolbar`）で、サイドバーの右・ページの上に敷く。
小窓は持たない（小窓は専用の上部バー）。左から順に固定する。

1. サイドバーの開閉（⌘S。**サイドバーを隠してもここは残る**＝戻す導線が消えない）
2. 戻る / 進む / 再読み込み（右クリックで「通常の再読み込み / ハード再読み込み」のメニュー）
3. **アドレスバー**（上限 620px。クリックで編集、Enter で決定、Esc で取消）
4. 拡張（`<browser-action-list>`）/ ダウンロード / 履歴 / 新規タブ

- 編集していない間は**ホストだけを白く**し、残りは沈める。https なら鍵（インライン SVG）を左に置く
- ⌘L は**今までどおりコマンドバー**（`address-bar`）を開く。ツールバーの入力欄はクリック専用
- 空きは `-webkit-app-region: drag`。信号機と同じ行なので、掴んでウィンドウを動かせる
- サイドバーを隠している間は左端まで伸びるので、信号機ぶんの余白（82px）を左に空ける
- 拡張の popup は**この View のクライアント座標**を基準に置かれる。サイドバー幅ぶん
  左にずれるので、main 側（`extensions.ts`）で足し戻す

## 3層の並び

サイドバーの中身。上から順に固定する。層をまたいだ並べ替えはできるが、層自体の順番は変えない。

1. **Live Folder** … GitHub の PR（下の節）。**定義を持たない自動生成の層**で、
   ここだけ並べ替えもドラッグもできない。手で並べる層の上にまとめて置く
2. **Favorites** … 5列アイコングリッド。**ピン留めと同じ「枠」**で、押すとその枠のタブが開く。
   **`messages` と `tools` の 2 セクション**に分かれ、この順で並ぶ。追加・Arc からの取り込みは
   すべて `tools` に入り、`messages` への振り分けはドラッグか右クリックで手で行う。
   `tools` が空のときはラベルごと畳む（`messages` は空でも受け皿を出す）
3. **ピン留め（bookmarks）** … フォルダで1階層だけまとめられる。閉じてもリストから消えない
4. **今日のタブ** … 一時タブ。閉じると消える。先頭に **New Tab** の行を置く。
   左右に並べた 2 本は**結合行**として 1 行にまとまる（下の「分割ビュー」）

層の見出しは**薄い小さなラベル**（`messages` / `tools` / `bookmarks` を CSS の `uppercase` で大文字化。
10px・`--nemo-ink-dim`・不透明度 .7）で揃え、Live Folder の小見出しより一段軽くする。**tools と bookmarks の間に線は引かない**
（ラベルと余白だけで区切る）。線（1px の `--nemo-border`）は Live Folder の下と一時タブの上だけ。
一時タブには見出しを置かない。

**⌘1〜9 は Favorites の通し番号**（`messages` → `tools` の順、グリッドの見た目と同じ）。
10 個目からは番号が付かない。⌘ を押し続けている間だけタイルの右上に番号を出す
（押した瞬間には出さない —— ⌘L / ⌘T のたびに光らせない）。
すでにその Favorite のタブがアクティブなら**直前のタブへ戻る**（Slack を見てすぐ作業に戻る、が同じキー 2 回で済む）。
一時タブの番号選択（旧 ⌘1〜9）は無い。

枠（Favorites / ピン留め）は **favicon の URL を定義に持つ**。タブを開いていなくてもアイコンで
見分けられ、履歴を消しても残る（外部の favicon サービスは使わない）。

Favorites とピン留めは**枠（定義）とタブ実体が別物**。枠は永続で、タブは押した時点で生まれる
（起動時にはタブ実体を1つも作らない＝枠が何十個あっても起動が重くならない）。
枠に属するタブは**一時タブの一覧には出さない**（出すと同じタブが2回並ぶ）。

同じ URL が Favorites とピン留めの両方に並ぶことはない。⌘D でピン留めしたら
その URL の Favorite 定義は**消えてピン留めに移る**（逆も同じ）。

## 名前を変える

ピン留め・Favorites・一時タブは、行を**ダブルクリック**すると
その場で名前を書き換えられる（右クリックメニューの「名前を変更」も同じ）。

- **フォルダだけは右クリックからしか変えられない**。ダブルクリックは開閉と取り合いになり、
  「名前を変えようとしただけで2回開閉する」ことになるので、フォルダには付けない
- 確定は Enter と blur、取消は Esc
- **空にして確定すると解除**され、実タイトルに戻る
- 付けた名前はページ遷移では変わらない。再起動をまたいで残る
- **閉じている枠は、単クリックの反応を 250ms 待ってから開く**。待たないと、名前を変えようと
  ダブルクリックしただけでタブが生まれて読み込みが走る（＝遅延ロードの意味が消える）。
  既に開いている行の選択とフォルダの開閉は待たせない（通常操作が重くなるだけ）

## 状態の見せ方

ピン留めの行と Favorites のセルは**同じ規則**で状態を重ねる（Favorites は狭いので、
未読ドットと ♪ はセルの角に置く）。

| 状態           | 見せ方                                              |
| -------------- | --------------------------------------------------- |
| 選択中         | 行の背景を `--nemo-surface-hi`、文字を `#fff`       |
| hover          | 行の背景を `--nemo-hover`、右端に × を薄く出す      |
| 読み込み中     | favicon の位置にスピナー（1.6px・`--nemo-accent`）  |
| 未読           | 右端に 6px の `--nemo-accent` ドット                |
| sleep 中       | 文字と favicon の不透明度を .45 に落とす            |
| 枠が閉じている | 不透明度を .62 に落とす（まだタブ実体が無い）       |
| 音が鳴っている | 右端に ♪                                            |
| クラッシュ     | 行に `--nemo-danger` の左バー、クリックで再読み込み |

## Live Folder（GitHub の Pull Request）

サイドバーに**自分に関係する PR が勝手に並び、勝手に消える**セクション。
位置は**最上段**（上の「3層の並び」の1番目。Favorites より上）。

親見出し（"PULL REQUESTS"）は**置かない**。小見出し2つだけにして階層を1つ減らし、
区切り線に挟まれた一塊として読ませる。

### 小見出し

- `REVIEW REQUESTED`（自分にレビュー依頼が来ている）と `CREATED`（自分が作った）の2つ
- 既存の層の見出しと同じ作法（10px・大文字・letter-spacing .09em・`--nemo-ink-dim`）。
  大文字は `text-transform` で作る
- **右端はバケットに割り当てられた件数**（`items.length`。重複除外後の値で、折りたたみ状態に
  依存せず、DOM 上の行数でも検索の取得件数 `returned` でもない）。畳んでいても出す
- グループが 0 件ならその小見出しごと出さない
- **クリックでそのバケットだけ開閉する**（2つは独立）。**起動のたびに両方折りたたみ**から始まり、
  永続化しない。開いても再取得はしない
- 先頭に開閉を示す小さな矢印 `›`（開いたら 90° 回転）。ホバーは `.row` と同じ薄い背景
- **畳んでいる間に未読があれば件数の右に未読ドット**（行と同じ `--nemo-accent`）。開いたら行側だけに出す
- 右クリックはセクションメニュー（開閉しない）

### 行

24px の丸アイコン + 2行テキスト。

| 部分   | 中身                                                          |
| ------ | ------------------------------------------------------------- |
| アイコン | GitHub マーク。右下に状態バッジ（縁は地の色で 2px 抜く）      |
| 1行目  | PR タイトル（1行で省略）                                       |
| 2行目  | **文脈で変える**。レビュー依頼は著者名、自分の PR はリポジトリ名 |
| 右端   | 未読ドット（既存タブと同じ `--nemo-accent`）                   |

2行目を文脈で変えるのは、自分の PR に自分の名前を並べても情報が無いため。
右端に「済」などのチップは置かない（状態はバッジだけで出す）。

**未読は「見えたら落ちる」**。行のクリックに限らず、コマンドバー・履歴・⌘数字・タブ切替・
**分割の相方・Peek** —— その URL が画面に出た時点で既読にする（判定は `visibleTabKeys`）。
Peek で一瞬覗いただけでも既読になるが、経路ごとに規則を分けるより一つの述語で済ませる。

### 状態バッジ

**`isDraft` が最優先**。draft はレビューを受け付けない状態なので、
そこに古い approval のチェックを出すと「もう通っている」と誤読される。

| 状態                | バッジ                          |
| ------------------- | ------------------------------- |
| `draft`             | グレーの鉛筆（`--nemo-ink-dim`）|
| `approved`          | 緑のチェック（`--nemo-ok`）     |
| `changes-requested` | 黄の「…」（`--nemo-warn`）      |
| `waiting`           | **バッジ無し**                  |

### 末尾の状態行

10px・`--nemo-ink-dim` の1行。左に「いつのデータか」、右に回転矢印。
**失敗しても一覧を空にしない方針なので、この行が無いと古い一覧を最新だと誤読する。**

| 状況         | 出すもの                                                     |
| ------------ | ------------------------------------------------------------ |
| 通常         | `Updated 3m ago` + `Refresh`（1分ごとに数字を詰める）        |
| 取得中       | `Refreshing…` + 矢印を回す                                    |
| `transient`  | `--nemo-danger` で `Couldn't refresh · showing 19:12` + `Retry`。行は `opacity: .55` |
| `rate-limit` | `Rate limited · retrying in 12m`。**`Retry` は出さない**（押しても投げないので嘘になる） |
| `auth`       | この行ではなく `Reconnect GitHub` の1行に置き換わる           |

打ち切り（検索が 100 件で切られた）は、状態行の**下に別の1行**として
`First 100 of 137 fetched for CREATED` と検索ごとに出す。
バケットに割り当てられた件数と検索ヒット数は別の母集団なので、小見出しの数と同じ表記に混ぜない。
小見出しを畳んでいる間はその打ち切り行も隠す（重複除外で小見出しが無いバケットの分は隠さない。
開く手段が無いため）。

更新ボタンはセクション全体で1つだけ。`⌘R` はページ再読み込みで埋まっているので
キーボードは割り当てない。

### 出す / 出さない

上から順に1つだけ選ぶ（`liveFolderView()` の1つの述語に閉じる）。

| 条件                       | 出すもの                                       |
| -------------------------- | ---------------------------------------------- |
| シークレットウィンドウ     | 何も出さない（データごと渡さない）             |
| 設定で無効                 | 何も出さない                                   |
| トークン未設定             | `Connect GitHub` の1行だけ                     |
| `auth`                     | `Reconnect GitHub` の1行だけ                   |
| 失敗・キャッシュあり       | 一覧（薄く） + 状態行                          |
| 失敗・キャッシュなし       | 状態行だけ                                     |
| 成功・0 件                 | `No open pull requests` + 状態行               |
| 成功・1 件以上             | 一覧 + 状態行                                  |

**0 件でもセクションごと消さない**（消すと手動更新の導線まで消える）。

**分割に入っているタブは結合行が優先**され、一時タブの層から消えない。
分割したページが PR の URL へ遷移した瞬間に結合行ごと消えると、
画面には分割が出ているのにサイドバーから解除する導線が無くなるため
（Live Folder 側の行は従来どおり出て「開いている」表示になる）。

## アニメーション

- サイドバー開閉: 幅を 260px ↔ 0 に `.26s cubic-bezier(.2,.8,.2,1)`
- hover: `.12s`
- コマンドバー: 背景を `rgba(0,0,0,.45)` + `backdrop-filter: blur(2px)`、`.12s` でフェード

長いアニメーションは入れない（毎日何百回も触るので、待たされる感覚が出る）。

## コマンドバー（⌘T / ⌘L）

Arc のコマンドバーに寄せる。1行 = **favicon + タイトル + URL**で、種別バッジは置かない。

| 値                   | 用途                                              |
| -------------------- | ------------------------------------------------- |
| 幅 `min(620px, 72%)` | 縦位置は**箱の中心が画面の中心よりわずかに上（46%）**   |
| 角丸 14px            | **コマンドバー専用**（`--nemo-radius-lg` は 11px のまま。上げるとダイアログ・パネルまで丸くなる） |
| 入力欄 16px          | 左に**選択中の候補の favicon**を置く（何を開こうとしているかが一目で分かる） |
| 行の高さ 40px        | 角丸 8px・左右 6px のインセット                   |
| 選択行               | `--nemo-accent` の**塗りつぶし**・文字は白        |

**候補の並びは固定**（`suggest.ts`）。入力のたびに順番が変わると目で追って選べない。
Arc は文脈次第で履歴が最上位に来るが、**そこは真似しない**。

1. 入力内容をそのまま検索 / 開く（常に先頭・必ず1件出す）
2. 開いているタブ → 3. ピン留め → 4. Favorites → 5. 履歴

候補の上下移動は **↑↓ と ⌃P / ⌃N** の両方で行える（macOS の入力欄はこの2つを既定で
行移動として食うので、実装側で必ず既定動作を止める）。

縦位置は `padding-top: max(24px, calc(46vh - 140px))` で決める。**箱の上端を固定して
候補は下へ伸ばす** —— 中央揃えにすると、入力するたびに箱の高さが変わってバーが上下に跳ねる。
引いている 140px は「候補が数件出た状態の高さの半分」で、**あえて定数にしている**
（実際の高さに追随させると同じ跳ねが起きる）。候補リストの上限 `min(360px, 45vh)` も
vh を混ぜてあり、最小ウィンドウ（480px）でも下がはみ出さない。

**下部の説明行は置かない**。代わりに**選択行の右端**に「Enter を押したら何が起きるか」を出し、
⇧ を押している間だけ行き先を反転させる。

| 候補           | 右端（⇧なし）                              | ⇧を押している間 |
| -------------- | ------------------------------------------ | --------------- |
| 開いているタブ | `タブへ切り替え`                           | 変わらない      |
| 検索           | ⌘T `新規タブで検索` / ⌘L `このタブで検索`  | 逆              |
| URL・ピン・Favorites・履歴 | ⌘T `新規タブで開く` / ⌘L `このタブで開く` | 逆 |

タブへの切り替えだけ反転しないのは、決定処理が `select-tab` を ⇧ に関わらず
「そのタブを選ぶ」に倒しているから（文言だけ変えると実挙動と食い違う）。

検索候補の行頭は favicon ではなく**虫眼鏡**にして、副題（検索エンジンの長い URL）は出さない。

## タブスイッチャー（⌃M）

⌘Tab と同じ「押しっぱなしで選ぶ」もの。直近に使ったタブの帯を画面中央に出す。

| 値                | 用途                                                     |
| ----------------- | -------------------------------------------------------- |
| カード 104px 幅   | favicon 28px（角丸 6px）＋タイトル 11.5px の2段           |
| 帯の角丸 14px     | コマンドバーと同じ器（`.switch-strip`）                   |
| 選択中            | `--nemo-surface-hi` の面＋`--nemo-accent` の 2px 内枠     |
| 並ぶ数            | **5 件**。それ以上は出さない                              |

- 上下は**中央揃え**。コマンドバーと違って高さが変わらないので跳ねない
- 選択枠は border ではなく `inset` の `box-shadow` で描く。border を付け外しすると帯が波打つ
- タイトルは 2 行で打ち切る（高さが揃わないと帯が崩れる）
- **見えているのは押している 1〜2 秒だけ**なので、情報は favicon とタイトルに絞る。
  URL・バッジ・キーヒントは置かない

操作は次のとおり。**並びもハイライト位置も main が持ち、renderer は表示だけ**する
（renderer 側に状態を置くと、⌃ を離した瞬間の確定と食い違う）。

| 操作                     | 結果                                     |
| ------------------------ | ---------------------------------------- |
| ⌃M                       | 帯を出して**直前のタブ**を指す           |
| ⌃ を押したまま ⌃M        | 1つ先へ。末尾まで行ったら先頭へ戻る      |
| ⌃ を離す                 | **確定**（ハイライトしているタブへ移る） |
| ← → ↑ ↓                  | ハイライトを動かす                       |
| Enter                    | 確定                                     |
| Esc・背景クリック        | 取消（タブは変わらない）                 |
| カードのクリック         | そのタブへ確定                           |
| ウィンドウが背面へ回る   | 取消（keyUp がもう届かないため）         |

**時間では確定しない**。押しっぱなしの最中に時間切れで切り替わるのは「離した瞬間に確定」と
食い違う（選んでいる途中で目的地が変わる）。keyUp を取り逃す条件は
**ウィンドウの blur** で捕まえ、そこでは確定ではなく取消にする
（見ていない間に勝手にタブが変わる方が困る）。時間で確定するのは、修飾キーを持たない
割り当てに変えたとき（`F5` など、そもそも押しっぱなしにできない場合）だけ。

カードのクリックは**位置ではなくタブの key で送る**。押している最中にタブが閉じると
main 側の並びだけが詰まり、帯の表示は古いままになる。位置で送ると1件ぶんずれて
別のタブへ飛ぶ。

並びは「アクティブタブを先頭に固定し、残りを最後に使った順」。**専用の配列は持たない**
（タブの生成・破棄・ウィンドウ間の移動で並びが壊れないようにするため）。
確定すると選んだタブが先頭に来るので、**1回押して離せば直前のタブと行き来できる**。

ほかのオーバーレイ（コマンドバー・ダイアログ）が出ている間は割り込まない。
逆に、押している最中にダイアログが割り込んできたら帯は畳む。

## 分割ビュー（2 ペイン）

一時タブ 2 本を左右に並べて同時に見る。Arc の Split View に寄せる。

- **対象は野良の一時タブだけ**。ピン留め・Favorites・Live Folder の行は分割に入らない
  （枠に属する行と結合行が二重になるため）。**3 つ以上は並べない**
- **作る導線は D&D だけ**。サイドバーのタブ行を別のタブ行の**中央帯**へドロップする。
  **ドロップ先が左・ドラッグしてきたタブが右**で、作った直後は右にフォーカス
- 分割は**表示モードではなくタブ同士の組**。別のタブを選ぶと両ペインとも隠れ、
  戻ると復帰する（＝ 1 ウィンドウに組が複数ありうる。同時に見えるのは 1 組だけ）
- 再起動をまたいで復元する

| 値 | 用途 |
| --- | --- |
| ペイン間の隔間 8px | `SPLIT_GAP` |
| ページ領域の外周の余白 8px | `SPLIT_INSET`。**分割中だけ**空ける（単独表示は今までどおりベタ塗り） |
| ペインの角丸 0px | `SPLIT_RADIUS`。下の「角丸を持たない理由」を参照 |
| フォーカス枠 2px `--nemo-accent` | `SPLIT_FOCUS_RING`。ペインの外周に出す |
| 結合行の高さ 40px | 通常のタブ行と同じ（`--nemo-row-h`）。器は `--nemo-surface-hi` |
| チップの面 `rgba(255,255,255,.05)` | 器の中に 2 つ。角丸 5px・間隔 3px |
| フォーカス側のチップ | 面を `rgba(255,255,255,.10)` にし、左端に 2px のアクセントバー |

### 角丸を持たない理由

`View.setBorderRadius()` は**その View 自身にしか効かず、子はクリップされない**
（`scripts/spike-split-chrome.mjs` で実測）。ペインは「ツールバー + ページ」の 2 枚なので、
ページだけを丸めると継ぎ目に 10px のえぐれが出る。**その見た目を取るくらいなら角丸を捨てる**。
将来 Electron が子をクリップするようになったら、この値を戻せば器 1 枚で済む。

### サイドバー（結合行）

分割中の 2 本は、**左タブがいた位置に 1 行（結合行）**として出る。右タブは自分の行を持たない。
位置は一時タブの層の中で、通常のタブ行と同じ並びに混ざる。

- チップの中身は通常のタブ行と同じ規則（favicon → 名前 → 未読 / ♪ → ×）。× は 20px
- **そのペアが画面に出ているときだけ**器をアクティブの面にする
- クリックでそのペインへフォーカス、ダブルクリックでその場で名前を変更
- 右クリックは 名前を変更 / ピン留め / Favorites に追加 / **分割を解除** / 閉じる
- 解除すると**その場で 2 行に割れる**（左だったタブが上・右だったタブが下）
- **ドラッグはチップ単位**。ピン留めツリー / Favorites へ落とすと、
  分割が解けてから枠に入る（⌘D も同じ規則）

ドロップの受け皿は**行の中央帯だけ**に出す（上下 8px は将来の並べ替え用に空けてある死に帯）。
受け付けられない相手（自分自身・すでに分割中・ピン留め / Favorites / Live Folder）には
そもそも受け皿を出さない。

### ツールバー

**ペインごとに 1 本**。右ペインのぶんは分割を初めて作るときに生まれる。

| | 左（＝分割していないときの唯一のツールバー） | 右 |
| --- | --- | --- |
| サイドバーの開閉 | 出す | **出さない** |
| 戻る / 進む / リロード / アドレスバー | 出す | 出す |
| 拡張 / ダウンロード / 履歴 / ＋ | 出す | **出さない** |
| ✕（このペインを閉じる） | 分割中だけ出す | 出す |

右を最小構成にするのは、同じ partition の `<browser-action-list>` を 2 枚出すと
popup がどちらの View に属するのか曖昧になり、位置合わせ（`popupAnchorOffset`）が
当てにならなくなるため。サイドバーを隠したときの信号機ぶんの余白も**左だけ**に付ける。
**分割中は左ペインが窓の左端から `SPLIT_INSET`（8px）右に始まる**ので、その余白は
82px ではなく 74px にする（信号機はウィンドウ側に描かれる＝基準は窓の左端）。

**ペイン固有の操作はフォーカスも移す**（戻る / 進む / リロード / アドレスバー / ✕）。
通さないと「左のアドレスバーを触ったのに ⌘W や ⌘F の対象は右のまま」になる。
ウィンドウ共通の操作（サイドバー開閉・拡張・ダウンロード・履歴・＋）では動かさない。

### 閉じ方の違い

| 操作 | 対象 |
| --- | --- |
| ペインの ✕ / チップの × | **そのペインのタブ**（浮いている Peek も一緒に閉じる） |
| ⌘W | **Peek が出ていれば Peek**（既存の規則）。出ていなければフォーカス中ペインのタブ |

どちらでも、片方が閉じれば分割は解けて残った方が全画面になる。

### ほかの決めごと

- **⌃Tab / ⌃M は 2 つのタブのまま数える**（ペイン間のフォーカス移動にも使える。⌘1〜9 は Favorites 用で、
  一時タブの番号選択は無い）。分割を作ると右タブをタブの並びの中で左の直後へ移すので、
  ⌃Tab の並びとサイドバーの見た目が揃う
- 狭いウィンドウでも素直に半分に割る（畳んで片方だけ出す、はしない）
- 自動 sleep / 自動アーカイブは**見えているタブに触らない**。加えて、分割中のタブの寿命は
  **ペアの新しい方**で見る（片側だけ先に閉じられてペアが解けるのを防ぐ）
- **`lastActiveAt` はフォーカスを持っていた側だけ**更新する。両方に同じ値を書くと
  ⌃M の MRU 順で左右が同着になり、「右から別タブへ行って ⌃M」で左に戻ってしまう

## Peek（ウィンドウ内ポップアップ）

ページ内のリンク（`target=_blank` / `window.open`）を、タブとして積む前に
**今見ているページの上に浮かぶポップアップ**で開く。ざっと見て閉じるか、
⌘O で通常のタブに昇格させるかを後から選べる。

| 値                       | 用途                                                       |
| ------------------------ | ---------------------------------------------------------- |
| 暗幕 `rgba(0,0,0,.30)`   | + `backdrop-filter: blur(3px)`。下のページが薄く透けて文脈が残る |
| 角丸 16px                | `WebContentsView.setBorderRadius`（CSS ではない）           |
| 寸法 ページ領域の 91%    | 中央・固定。**リサイズ不可**                                |
| 上下の余白 42px 以上     | ✕ と ⌘O を Peek の**外側**に置くため（`PEEK_TOOL_BAND`）    |
| ボタン 26px・角丸 8px    | ページ領域の右上。`rgba(28,28,34,.72)` + 内枠 1px           |
| 面 `--nemo-sidebar`      | 中身が来るまでの**プレースホルダー**。暗幕の上に同じ矩形・同じ角丸で置く |

- **サイドバーは覆わない**。Peek を開いたままタブを切り替えられる
- 寿命は**タブに紐づく**。別タブへ行くと隠れ、戻ると再表示される（破棄しない）
- 閉じるのは **✕ / Esc / ⌘W** の3つ。**暗幕クリックでは閉じない**
  （テキスト選択のドラッグが Peek の外へはみ出したときの誤爆を避ける）
- ⌘O または ⤢ ボタンで**同じウィンドウの一時タブ（末尾）**になりアクティブに。
  ページは読み直さない
- **⌘クリック（背面タブ）は従来どおり**。検索結果から何本も背面に溜める操作は殺さない
- **踏んだ瞬間に窓の形を出す**。中身のドキュメントが届くまで Peek の View は出さず、
  暗幕の上にプレースホルダーの面だけを置く。「暗くなっただけで何も出ない」時間を作らない
  （採用済みの `WebContents` には背景色を敷けないので、View を後から出す形にしている）

**分割中は、その Peek が属するペインのページ領域の中に収まる**（暗幕もそのペインだけ）。
出るのは**フォーカス中タブのぶんだけ** —— 暗幕はウィンドウに 1 枚しかないので、
相方の Peek はフォーカスを移したときに出る（破棄はしないので行き来しても消えない）。

サイドバーの一覧には**出さない**（親タブが1本出ているように見せる）。
一方で拡張から見た「今いるページ」は Peek を指す（Bitwarden の自動入力が
覗いているログイン画面に効くようにするため）。

## 小窓（Little Nemo）

ターミナルや Slack から踏んだリンクを、**メインウィンドウを前面に出さずに**、
今いる画面の上へ小さなウィンドウで開く。⌘O でメインウィンドウのタブへ移す。

| 値                     | 用途                                                     |
| ---------------------- | -------------------------------------------------------- |
| 寸法 460×560           | **記憶しない**。常に同じ場所に出す                        |
| カスケード +30px       | 2枚目以降はずらす。原則4枚まで                            |
| 上部バー 38px          | 信号機と同じ行。左に 72px の掴みしろ                      |
| 並び                   | 戻る / 進む / リロード → URL → ⌘O                         |
| URL                    | **表示のみ**。クリックでコピー（アドレスバーではない）    |
| ✕                      | 置かない（信号機の赤で閉じる）                            |

- **サイドバーもコマンドバーも持たない**。⌘L も効かない。
  別のページへ行きたくなったらタブに昇格させる、という分け方
- ウィンドウは **NSPanel**（`type: 'panel'`）。アプリを前面に出さずにキーフォーカスを
  受け取れるので、フルスクリーンのターミナルの上に出しても Space が動かない。
  代わりに**全 Space に追従する**（固定はできない）
- タブは常に1つ。⌘W は「ウィンドウを閉じる」に読み替える（空の小窓を残さない）

## 会議の小窓（Meet の通話コントロール）

Google Meet の会議タブから離れているあいだ、**他アプリの上に浮く横長のバー**を出す。
そこから「会議タブへ戻る」「マイクの ON/OFF」「カメラの ON/OFF」ができる。
**映像は出さない**（コントロールだけ）。

| 値 | 用途 |
| --- | --- |
| 寸法 336×60 | サイズは固定。**位置だけ記憶する**（`{ x, y, displayId }`） |
| 既定位置 | 会議タブが載っている display の `workArea` 右下（余白 24px） |
| 角丸 11px | `--nemo-radius-lg`。frameless なので内枠 1px（`--nemo-border`）を自分で描く |
| 外周の余白 14px | **掴みしろ**。ボタンで埋めない |
| ボタン 38px・角丸 7px | 面は `--nemo-surface`、hover で `--nemo-surface-hi`。間隔 8px |
| テキスト | ドメイン名 11.5px（2 段目に経過時間 10.5px・`tabular-nums`） |
| 参加中の点 | 6px の緑（`#4ade80`）をドメイン名の左に。**縮退中は出さない** |

- 並びは **[● ドメイン / 経過時間 →]** …掴みしろ… **[マイク] [カメラ]** の 3 ボタン
- **「会議へ移動」はドメイン名と経過時間ごと1つのボタン**にする。独立した「戻る」ボタンだと
  何に戻るのかがラベルからも形からも読み取れず、履歴の「戻る」と混ざる。
  アイコンは**枠の中へ入る矢印**にして、左向き矢印（履歴を戻る）は使わない
- **退出も ✕ も置かない**。退出は誤爆で会議を抜ける事故を原理的に消すため、
  ✕ は「会議中はいつでも出ている」を崩さないため（＝閉じる手段は持たない）
- **掴みしろを最優先にした間取り**にする。地は全面がドラッグ領域で、
  外周の余白とボタン群のあいだ（`margin-left: auto`）を広く空ける。
  ボタンを詰めると**動かせない小窓**になる
- **切れているとき（ミュート / カメラ OFF）は、面を沈めたままアイコンだけ `--nemo-danger`**
  にして斜線を重ねる。面ごと赤く塗ると、他アプリの上に浮くバーの中でそこだけ主張が強すぎる。
  色だけに頼らないよう斜線を必ず併せる
- **経過時間は renderer 側で `joinedAt` から数える**（毎秒 IPC を撃たない）
- **トグルは楽観更新しない**。Meet 側で弾かれることがあるので、見た目が変わるのは
  main からの push が返ってから
- 地は `-webkit-app-region: drag`、**各ボタンは `no-drag`**（でないと押せない）
- ウィンドウは **NSPanel**（`type: 'panel'`）+ `setAlwaysOnTop(true, 'floating')`。
  小窓（Little Nemo）と同じ理由で、アプリを前面に出さずに押せる。
  `setVisibleOnAllWorkspaces` は**呼ばない**（Dock アイコンが消える）
- 出るのは**会議タブが見えていないとき**（他アプリで作業中も、Nemo で別タブを見ている間も）。
  会議タブへ戻ると隠れるが**破棄はしない**（行き来が頻繁なので作り直すと出るのが遅れる）

### 縮退（状態が読めないとき）

Meet の DOM からマイク / カメラの状態が読めないときは、**戻るボタンだけ**にする。

| | 通常 | 縮退 |
| --- | --- | --- |
| 参加中の点 | 出す | **出さない** |
| 経過時間 | 出す | **出さない**（0:00 で止まって見える誤解を作らない） |
| マイク / カメラ | 出す | **出さない**（状態が分からないまま押させない） |
| 会議へ移動 | 出す | 出す |

一時的に読めなかっただけで縮退へ固定しない（読み込みの途中で普通に起きる）。
正常値が返れば復帰し、**経過時間は 0 に戻さず縮退前から続ける**。

### 出し入れの決めごと

| 状況 | 小窓 |
| --- | --- |
| 会議タブが見える → 見えない | 表示（無ければ生成） |
| 見えない → 見える | **hide のみ**（破棄しない） |
| 対象の会議が終わった・他に会議がある | **retarget**（同じ小窓のまま対象を差し替える） |
| 出せる会議が 0 件 | **destroy**（`WebContentsView` 1 枚で 89MB なので必ず返す） |

- 出るのは**アプリ全体で 1 枚**。複数の会議があれば**最後にアクティブだった会議**を出す
- **設定項目は持たない**

### 背面スロットリングを外す

Chromium は隠れたページで `requestAnimationFrame` を止める。Meet はボタンを押した結果の
反映を rAF 越しに行うので、素のままだと**小窓からミュートを押しても会議タブを前面に戻すまで
実際には切り替わらない**（押した本人は「効いていない」と思って何度も押すことになる）。

会議中の対象タブだけ・会議中だけ `setBackgroundThrottling(false)` にする。
増えるのは CPU だけで、通信量もメモリも増えない。会議が終われば元に戻す。

## 空状態（タブが 1 つも無いとき）

最後のタブを閉じてもウィンドウは残る。そのときページ領域に出すもの。

| 値 | 用途 |
| --- | --- |
| マーク 64px | Lucide の `fish-symbol`（ISC）をインライン SVG で持つ。線 2・角丸端 |
| マークの色 `--nemo-ghost` | `rgba(232,232,238,.14)`。**地に溶けかける濃さ**にする |
| キーの表記 11.5px | `--nemo-ink-dim` を `opacity: .8`。マークより目立たせない |
| 間隔 28px | マークとキーの表記のあいだ |

- 出るのは**マークとキーの表記だけ**。押せるものは置かない。Favorites や履歴を並べると、
  タブが無いだけの画面が「もう一つのホーム画面」になり、サイドバーと役割が二重になる
- キーの表記は**英語**（`⌘T` New Tab / `⌘⇧T` Reopen Closed Tab）
- 専用の WebContentsView（`?view=empty`）をページ領域いっぱいに敷く。
  **タブが 0 本になって初めて作る**（起動時には作らない）
- **タブがあるときは必ず隠す**。ページの上に残るとクリックを丸ごと遮る
- この View は状態を購読しない。出し入れは main の `layout()` だけが決める

## オーバーレイ

ブラウザ UI は複数の WebContentsView に分かれている。

- **sidebar** … 常時表示。サイドバー本体（小窓では代わりに **mini** の上部バー）
- **toolbar** … 常時表示。アドレスバーとナビゲーション（小窓は持たない）
- **overlay** … 必要なときだけ表示。矩形は main 側が決める（`overlayBounds`）
- **peek** … Peek の暗幕と ✕ / ⌘O。Peek を初めて出すときに作る（透明）
- **empty** … タブが 1 つも無いときのページ領域。0 本になって初めて作る
- **call** … 会議の小窓。**別ウィンドウ（NSPanel）の中身がこれ 1 枚だけ**

| 種類                                               | 位置                       | モーダル |
| -------------------------------------------------- | -------------------------- | -------- |
| コマンドバー                                       | 全面（暗転）               | する     |
| タブスイッチャー                                   | 全面（暗転）               | する     |
| 検索バー                                           | 右上 460×68                | しない   |
| ダイアログ（権限 / 認証 / 証明書 / 外部 protocol） | サイドバー右の上部 560×220 | する     |
| ダウンロード                                       | 右上 380×最大460           | しない   |

モーダル以外は**ツールバーの下**（ページ領域の上端から）に置く。
上に重ねるとアドレスバーが隠れ、「今どのページか」が見えないまま答えることになる。

## 書かないもの

- アイコンフォント・外部アセットは使わない（CSP を `'self'` に保つため）。記号は Unicode 文字か
  インライン SVG で描く
- **favicon だけは例外**。サイト自身が申告した favicon の URL（`page-favicon-updated` で届くもの）は
  `<img src>` で表示する。取れないときはホスト名の頭文字で代用する。
  **第三者の favicon サービス（`google.com/s2/favicons` 等）は使わない** —— どこに何を見に行ったかが
  そのサービスに漏れる。CSP は `img-src` にだけ `https:` を許している
- 影は使わない（オーバーレイの `box-shadow` だけ例外）
