review session: b1db612e-3b6a-4515-9224-27764d8736f6

## 1回目

````text
計画と、参照している既存実装（`swipe-gesture.js` / `registry.ts:952` / `verify-switcher.mjs` / `verify-targets.mjs` / `verify-all.mjs`）を突き合わせて読みました。

## P0
- `Phase 3 > 2`（ケース 5） — `Input.dispatchKeyEvent` は `type: 'keyDown'`（や `rawKeyDown`）だけでは**文字を挿入しない**ので、「`input.value` に `G` が入る」は必ず空振り FAIL になる。リポジトリ内の先行例（`verify-switcher.mjs:108`、`verify-live-folder.mjs:670`）はどれもショートカット用で `text` を渡していないため、踏襲すると確実に踏む / 「`preventDefault` していないことの裏取り」という一番大事なアサーションが偽 FAIL になり、実装側を疑って往復する / `{ type: 'keyDown', key: 'G', code: 'KeyG', text: 'G', modifiers: 8, windowsVirtualKeyCode: 71 }` + `keyUp` で撃つ。`text` 付きに変えると `g` 側も挙動が変わるので、ケース 1〜4 も同じ `pressKey` ヘルパーに揃えて撃ち方を 1 つにする。
- `Phase 3 > 3`（`verify-all.mjs` への組み込み位置） — `switcher` の隣に置くと、以降のスイートのキー検証を巻き込んで壊す可能性が高い。`verify-switcher.mjs:276` に「**CDP の合成キーは撃った先へフォーカスが移り、一度ページへ撃つと以降オーバーレイへ撃っても届かない**」と実測が残っている。vim-scroll はタブのページへ大量に撃つスイートなので、後ろに来る `peek`（scrim へ撃つ）や `live-folder`（`ui` へ撃つ）が原因不明で落ちる / 落ちても原因が自分の変更に見えないので、切り分けに一番時間がかかる形の失敗になる / 実行順の**最後**（`live-folder` の後、`restart` の前）に置き、`KNOWN_TARGETS` の並びも同じ位置にする。あわせて、開いたタブは終了時に必ず閉じる。一度はフル `mise run verify` を通して隣接スイートへの影響が無いことを見る。
- `Phase 1 > 2` / `Phase 3 > 2`（ケース 4） — `scrollableAncestor` の条件が「`overflowY` が `auto`/`scroll` かつ room > 1」だけで、`hasScrollRoom`（`swipe-gesture.js:151`）が持っている `node === document.scrollingElement` の特例が落ちている。この結果、activeElement 起点の探索は**通常ページでは必ず null を返す**（`html` の `overflowY` は `visible`）ので、実質「画面中央 → `scrollingElement`」の 2 段でしか決まらない / 「activeElement の祖先を辿る」という仕様の主経路が実装にも検証にも存在しないまま完成扱いになる。中央がモーダル・固定ヘッダ・canvas に当たるサイトで無反応になる経路もそのまま残る / 祖先探索にも `scrollingElement` 特例を入れる。あわせて `pickTarget` を「候補を順に見て、**縦の room がある最初のもの**を採る」と明文化する（room の無い候補で確定して黙って何も起きない、を防ぐ）。

## P1
- `Phase 3 > 1` / `Phase 3 > 2`（ケース 4） — 「内側 div にフォーカスを置いて `G`」は、素の `<div>` が focusable でないため `focus()` が効かず activeElement は `body` のままになる。中央フォールバックが拾って**テストは通るが、検証したかった経路は一度も通らない** / 「内側スクローラで効く」ことの回帰網が中央フォールバックだけになり、祖先探索が壊れても気づけない / `scroll-inner.html` の内側スクローラに `tabindex="-1"` を付けるか、中にフォーカス可能な要素を置く。さらに「内側 div を画面中央から外した状態でも activeElement 経由で効く」ケースを 1 本足すと、2 経路を分離して撃てる。
- `Phase 3 > 2`（ケース 5） — `input.focus()` はその要素をビューポートへスクロールインさせるので、フォーカスを置いた時点で `scrollTop` が動く / 「`G` で動かない」の基準値が汚れ、偽 FAIL か偽 PASS のどちらにも転ぶ / `focus({ preventScroll: true })` を使い、**フォーカス後**に基準の `scrollTop` を読んでから撃つ。
- `Phase 3 > 2`（target の掴み方） — ページ target の選び方と後片付けの決まりが書かれていない。`verify-switcher.mjs:63` が「URL 完全一致で 1 件に絞る・絞れなければ落とす」「ほかの検証が残したウィンドウと衝突するので URL に `?w=<windowId>` を付ける」を明示的に踏んでいる / 踏襲しないと、単独では通るのにフル実行時だけ別ウィンドウのページを掴んで落ちる / `connectWindow` / `connectPage` の型をそのまま持ってきて、URL に `?w=${windowId}` を付ける。
- `Phase 4 > 4` — 「`pnpm verify --changed` を回して逆引きが `vim-scroll` を選ぶことを確認する」は、この時点の作業ツリーでは成立しない。Phase 2 で `src/main/registry.ts` を触っており、`registry.ts` は意図的に `OWNERS` 外（`verify-targets.mjs:88`）＝ `full` に倒れる。新規の `test-pages/*.html` も `OWNERS` に無いので同じ / フル（372s）を 1 回空振りさせたうえで「逆引きが壊れている」と誤読する / 逆引きの確認は `scripts/verify-targets.test.mjs` に `selectVerifyTargets(['src/shared/vim-scroll.js'])` → `subset: ['vim-scroll']` のケースを足す形にする。`--changed` を回すなら「registry.ts があるのでフルに倒れるのが正しい」と期待値を書き換える。
- `Phase 1 > 1` — `feedVimKey` に渡す `at` の時計が決まっていない。`createVimScrollState(performance.now())` と `event.timeStamp` は同じ time origin だが、`swipe-gesture.js` と違って計画に明記が無い / 片方を `Date.now()` で書くと猶予判定が壊れ、しかもユニットテストは相対値で通るので気づけない / `at` は `event.timeStamp` に固定すると JSDoc に書く（`feedSwipe` の記述に揃える）。

## P2
- `Phase 1 > 2` — `isEditable` / `scrollableAncestor` / `pickTarget` は注入文字列の中にしか無く、`pnpm test` の網にかからない。回帰が効くのは `verify-vim-scroll` だけ、という前提をファイル先頭のコメントに残しておくと、後から「テストがあるはず」と誤解しない。
- `Phase 1 > 1` — 新規の `src/shared/*.js` は `// @ts-check` + JSDoc 付き（`swipe-gesture.js` / `timings.js` と同じ体裁）。`pnpm typecheck` に載る前提を Phase 1 側にも書いておく。
- `Phase 2 > 1` — 注入マーカー名（`__nemoVimScrollAttached` など）を swipe と別名で明示する。ワールドが違えば衝突しないが、将来同じワールドに寄せたときに黙って `'already'` を返す形になる。

## Q
- `Phase 3` 全体 — 新スイートを**フル実行に常設するか、`--only` 専用に留めるか**が決まっていない。直前のコミットでフルを 529s → 372s に縮めたばかりで、これは `NEEDS_APP` のアプリ起動込み・smooth の到達待ち・1200ms の固定待ちを持つ / 決めないと「速さのために削った時間を、次の機能で無言で戻す」ことになる。許容する追加秒数の上限を先に決めないと、後から削る作業が発生する。
- `仕様 > なめらかにスクロール` / `動作確認 > 1` — smooth 固定か、長大ページでは即時ジャンプに倒すかが人の手触り待ちになっている / 即時へ倒すと Phase 3 のポーリング前提・アサーションの書き方・`VERIFY.md` の記述がまとめて変わるので、動作確認の後に Phase 3 を書き直す順番になる。先に「まず smooth で作り、倒す判断が出たら検証も直す」と明記するか、最初から即時で作るかを決めておきたい。
- `概要・やりたいこと` — 今後 `j` / `k` / `Ctrl-d` / `Ctrl-u` へ広げる想定があるか / 広げる前提なら `feedVimKey` の戻り値を `'top' | 'bottom'` の 2 値に固定した時点で作り直しになる（ファイル名・スイート名 `vim-scroll` も同様）。「今回きり」と決めるなら固定でよい。

````

**対応**: P0 は全件反映。① `Input.dispatchKeyEvent` に `text` が要る件を「検証まわりで分かっていること」と Phase 3 に書き、撃ち方を `text` 付きの `pressKey` ヘルパー 1 つに揃えると明記。② `verify-all.mjs` への組み込み位置を `switcher` の隣から**実行順の最後**（`live-folder` の後・`restart` の前）へ変更し、CDP の合成キーが後続スイートを壊す理由を添えた。あわせて Phase 4 に「フルを 1 回通す」を足した（`--only` では検出できないため）。③ `scrollableAncestor` から `node === document.scrollingElement` の特例が落ちていた件を仕様セクションと Phase 1 の両方で明記し、`pickTarget` を「縦の room がある最初の候補を採る」と書き換えた。

P1 も全件、書き換えの形で反映。`scroll-inner.html` の内側スクローラに `tabindex="-1"` を付け**画面中央から外す**ことでケース 4 を activeElement 経路だけの検査に変更（新ケースを足すのではなく既存ケースの書き換えで分離した）。ケース 5 は `focus({ preventScroll: true })` + フォーカス後に基準値を読む形に修正。ページ target の掴み方（URL 完全一致・`?w=<windowId>`）を前提と Phase 3 に追記。Phase 4 の `--changed` 確認は `registry.ts` を触るのでフルに倒れるのが正しく、成立しない検査だったため削除。`at` を `event.timeStamp` に固定する旨を Phase 1 に明記。

P2 も全件反映（`// @ts-check` + JSDoc の体裁、注入ヘルパーが `pnpm test` の網にかからない旨のコメント、マーカー名を swipe と別名にすること）。

Q は 3 件とも保留（人間の判断待ち）。

## 2回目

````text
再読しました。前回の P0/P1/P2 はすべて反映されています。追加の指摘のみ。

## P0
- `Phase 3 > 2`（ケース 5） — 撃つ直前のページ位置が決まっておらず、ケース 3 が終わった時点で `scroll-long.html` は**最下部にいる**。そこで `G` を撃って「`scrollTop` が動かない」を見ても、入力欄の除外が壊れていて最下部へ飛ぶ実装でも同じ結果になる / 計画自身がケース 2 で警戒している「最初から最下部なら検査が空振りする」と同じ穴を、除外の検査側で踏む。しかも**通ってしまう**ので気づけない / ケース 5 は先頭（または中間）へ `scrollTo` してから `focus({ preventScroll: true })` → 基準値を読む、の順にし、「基準値が最下部ではないこと」もアサーションの詳細に出す。`input.value` 側だけが根拠にならないよう、両方を non-vacuous にする。

## P1
- `Phase 3 > 3` — 「実行順の最後（`live-folder` の後、`restart` の前）」が実際の並びと合っていない。`live-folder` の後には `http-auth` ブロックがあり（`verify-all.mjs` の `want('http-auth')`）、そこに挟むと最後にならない / 意図（キーを撃つスイートを全部の後ろへ置く）が半分しか効かない。`http-auth` はキーを撃たないので今は無害だが、前提が変われば同じ罠に戻る / 「`http-auth` の後、`restart` の前」に直す。`KNOWN_TARGETS` の並びも `'http-auth'` の後・`'restart'` の前に合わせる。
- `Phase 3 > 2`（終了時にタブを閉じる） — 閉じた結果ウィンドウのタブが 0 になると**ウィンドウごと消える**（`verify-switcher.mjs` の `openThree` が「起動時のタブは最後に畳む（先に全部閉じるとウィンドウごと消える）」として踏んでいる）。vim-scroll は最後に走るので、前のスイートが何枚残しているかに依存する / ウィンドウが消えると後続の `restart` ブロック（`split --restart-write` / `http-auth --restart-write` はアプリが動いている前提）が丸ごと落ち、原因が vim-scroll に見えない / 「他にタブが残っていることを確かめてから閉じる（残っていなければ 1 枚残す）」を手順に書く。
- `Phase 1 > 2` / `Phase 3 > 2` — 注入ヘルパーの一覧に**修飾キーの除外と `isComposing` の除外が無い**（仕様の「効かない条件」にはあるが、実装手順とケース一覧のどちらにも落ちていない）。`feedVimKey` は `key` しか見ないので、この 2 つは注入側にしか置き場がない / ⌘G（Chromium の「次を検索」）で最下部へ飛ぶ誤爆が入っても、ユニットテストにも自走検証にもかからない / ヘルパー一覧に `event.metaKey || event.ctrlKey || event.altKey` と `event.isComposing` の早期 return を明記し、ケースに「`modifiers` 付きで `G` を撃って動かない」を 1 本足す（`pressKey` に `modifiers` を渡せる形にしておく）。
- `仕様 > スクロール対象の決め方` — 祖先探索に `node === document.scrollingElement` 特例を入れたことで、**ルートに数 px の余りがあるページでルートが先に採られる**（`pickTarget` は「room がある最初の候補」を採るので、中央候補まで進まない）。Gmail / Slack 型でも `html` に端数の overflow があるサイトは珍しくなく、その場合 `gg` / `G` が数 px 動いて終わる / 内側スクローラのサイトで無反応に見える、という当初の狙いから外れる挙動が残る / 候補を「activeElement 祖先（`scrolls` のみ）→ 中央要素の祖先（`scrolls` のみ）→ `scrollingElement`」の順にする。通常ページは最後の候補で確実に拾えるので特例を祖先探索に置く必要はなく、先取りも起きない。

## P2
- `Phase 3 > 1`（ケース 4） — 内側スクローラへのフォーカスも `focus({ preventScroll: true })` に揃える。ケース 5 とヘルパーを共通化しておくと、後からページを増やしたときに片方だけ素の `focus()` に戻る事故が減る。
- `Phase 3 > 2` — bfcache 復帰（`did-navigate` に張る理由そのもの。swipe が実際に踏んでいる）が人間の動作確認だけに載っている。同じタブで 2 ページ踏んで `history.back()` → `G` を撃つだけで自走に移せるので、ケースを 1 本足す価値はある。
- `Phase 3 > 2` — `connectWindow` は `verify-switcher.mjs` にしか無い private 関数なので、今回はコピーでよい。3 本目の利用者が出たら `scripts/lib/cdp.mjs` へ寄せる、と書き添えておくと後で迷わない。

## Q
- `Phase 3` 全体 — フル実行に常設するか `--only` 専用に留めるか（アプリ起動込み・smooth の到達待ち・1200ms の固定待ち）。直前に 529s → 372s へ縮めたばかりなので、許容する追加秒数の上限を先に決めないと後から削る作業が出る。
- `仕様 > なめらかにスクロール` / `動作確認 > 1` — smooth 固定か、長大ページでは即時に倒すか。即時へ倒すと Phase 3 のポーリング前提とアサーション、`VERIFY.md` の記述がまとめて変わる。
- `概要・やりたいこと` — 今後 `j` / `k` / `Ctrl-d` へ広げる想定か。広げるなら `feedVimKey` の戻り値を `'top' | 'bottom'` に固定した時点で作り直しになる（ファイル名・スイート名 `vim-scroll` も同様）。

````

**対応**: P0 を反映。ケース 5 は「ケース 3 の直後で最下部にいるまま撃つと、入力欄の除外が壊れていても同じ結果になり通ってしまう」ので、**先に中間位置へ `scrollTo` してから**フォーカスを置く形に書き換え、「基準値が最下部ではないこと」もアサーションに出すと明記した。

P1 は 4 件中 3 件を反映。① 組み込み位置を「`live-folder` の後」→「**`http-auth` の後**」に修正（`verify-all.mjs:360` に `http-auth` ブロックが挟まっていることを実際に確認）。`KNOWN_TARGETS` の並びも同じに直した。② タブを閉じる手順を「他にタブが残っていることを確かめてから閉じる（残っていなければ 1 枚残す）」に書き換えた。③ **祖先探索から `scrollingElement` 特例を外した**（1 回目の P0-3 で入れたものを撤回）。`html` に数 px の余りがあるページでルートが先に採られ、内側スクローラまで辿り着かない指摘が妥当。通常ページは最後の候補で拾えるので特例は不要。

修飾キー / `isComposing` の件は、**注入ヘルパー一覧への早期 return の明記だけ反映**した（仕様セクションに既にある決定を実装手順に落とし直しただけなので書き換え扱い）。**検証ケースの追加は見送り**（ループ中に検査項目を足さない方針）。終了報告に回す。

P2 は 2 件反映（ケース 4 のフォーカスも `focus({ preventScroll: true })` に揃える、`connectWindow` は今回コピーでよい旨）。**bfcache 復帰の自走ケース追加は見送り**（同上、検査項目の追加）。終了報告に回す。

Q は 3 件とも保留（1 回目と同じ内容）。

## 3回目

````text
再読しました。前回の P0 と P1 3 件・P2 2 件は反映を確認。見送り 2 件（検証ケースの追加）は理由が妥当なので繰り返しません。今回の書き換えで新しく見えたものだけ。

## P0
- `仕様 > スクロール対象の決め方` / `Phase 1 > 2` — 祖先探索から `scrollingElement` 特例を外した結果、今度は**逆向きの穴**が開いた。走査は activeElement から `parentElement` を辿るので **`document.body` / `document.documentElement` にも当たる**が、`body { overflow-y: scroll }`（スクロールバー常時表示のためによく使われる）のページでは `getComputedStyle(body).overflowY === 'scroll'` かつ `body.scrollHeight - body.clientHeight > 1` になる一方、overflow の viewport 伝播で**実際に動くのは `scrollingElement` 側**なので `body.scrollTo()` は何もしない / 何もフォーカスしていない初期状態では `document.activeElement` が `body` そのものなので、**そういうサイトでは `gg` / `G` が常に完全に無反応**になる。しかも `scroll-long.html` にその CSS が無ければ自走検証は PASS し、実サイトを触るまで気づけない（人間の動作確認で踏んで作り直し） / 走査で `node === document.body || node === document.documentElement` に当たったら**採らずに候補 3（`document.scrollingElement`）へ譲る**。あわせて `scroll-long.html` の `body` に `overflow-y: scroll` を入れておけば、検査項目を増やさずにケース 1・2 がそのまま回帰になる。

## P1
- `Phase 1 > 2`（`scrollableAncestor`） — 「祖先を辿って」としか書かれておらず、**渡した要素自身を含めるか**が決まっていない。含めないと、ケース 4 でフォーカスを置く内側スクローラ**そのもの**が候補から外れて親から探し始めることになる / ケース 4 が落ちて原因探しの往復が出る（`hasScrollRoom` は `let node = target instanceof Element ? target : ...` と自分自身から始めているので、写し方次第で簡単に食い違う） / `hasScrollRoom` と同じく「渡された要素自身から始める」「`Element` でなければ（`activeElement` が `null` のときも含めて）その候補は空振りとして次へ」と明記する。注入コード内の例外はワールドごと黙って死ぬので、`null` の分岐も落とさない。

## P2
- `Phase 3 > 2`（ケース 3・5） — 「動かない」を見るケースは到達 polling が使えないので、**smooth の最長ぶんを待ってから読む固定待ち**が要る。ケース 3 は 1200ms が書いてあるが、ケース 5 は待ち方が書かれていない。実装時に「撃った直後に読んで PASS」（＝何も待たずに動かないのを確認したつもり）になりやすいので、待ち時間の根拠を 1 行添えておくとよい。

## Q
- `Phase 3` 全体 — フル実行に常設するか `--only` 専用に留めるか。直前に 529s → 372s へ縮めたばかりで、このスイートはアプリ起動込み・smooth の到達待ち・固定待ちを持つ。許容する追加秒数を決めないと、後から削る作業が出る。
- `仕様 > なめらかにスクロール` / `動作確認 > 1` — smooth 固定か、長大ページでは即時に倒すか。即時へ倒すと Phase 3 のポーリング前提・アサーション・`VERIFY.md` の記述がまとめて変わる。
- `概要・やりたいこと` — 今後 `j` / `k` / `Ctrl-d` へ広げる想定か。広げるなら `feedVimKey` の戻り値を `'top' | 'bottom'` に固定した時点で作り直しになる（ファイル名・スイート名 `vim-scroll` も同様）。

````

**対応**: 全件反映（すべて書き換えで済むもの）。

P0: 祖先探索で `document.body` / `document.documentElement` を**採らずに候補 3（`scrollingElement`）へ譲る**と明記した。`body { overflow-y: scroll }` は条件に合致するのに overflow の viewport 伝播で実際に動くのは `scrollingElement` 側、かつ初期状態の `activeElement` は `body` そのものなので、そのままだと該当サイトで常に無反応になる指摘が妥当。仕様セクションを「特例を持ち込むと先取り / `body` を素通しにすると無反応」の両方が見える形に書き直した。あわせて `scroll-long.html` の `body` に `overflow-y: scroll` を入れることにした（**検査項目は増やさず**、既存のケース 1・2 がそのまま回帰になる）。

P1: `scrollableAncestor` を「**渡された要素自身から**始める」「`Element` でなければ（`activeElement` が `null` の場合を含め）空振りとして次の候補へ譲る」と明記。`hasScrollRoom` の始め方に揃えた。

P2: 「動かない」を見るケース（3・5）は到達 polling が使えないので、smooth の最長ぶんを待ってから読む固定待ちが要る旨を 1 行足した。

Q は 3 件とも保留（1・2 回目と同じ内容）。

## 4回目

````text
再読しました。前回の P0・P1・P2 はいずれも仕様セクションと Phase 1 / 3 の両方に落ちていることを確認。新しく見えたものが 1 件だけあります。

## P0

## P1
- `Phase 3 > 1`（`scroll-long.html`） — `body` に `overflow-y: scroll` を足しただけでは、狙った回帰にならない。走査が `body` を拾う条件は「`overflowY` が `auto`/`scroll`」**かつ**「縦の room がある」の両方で、`body` の高さが `auto`（通常の長いページ）だと `body.clientHeight` は中身と同じ高さになり `scrollHeight - clientHeight ≈ 0` になる。room が無いので、`body` を採らない guard を外してもケース 1・2 は素通りで PASS する / 「検査項目を増やさずに回帰になる」と書いてあるのに実際は何も守っていない、という一番たちの悪い形（守っているつもりの空検査）になる。実サイトで踏む形は `body` の高さが**ビューポートに固定されていて中身がはみ出す**ケースで、そこで初めて `scrollHeight > clientHeight` かつ `body.scrollTop` は動かない、が成立する / fixture を `body { height: 100vh; overflow-y: scroll }`（中身はそれより高い）にする。この形なら overflow の viewport 伝播で実際に動くのは `scrollingElement` 側のままなので、ケース 1・2 の測り方は変えずに guard を外すと落ちるようになる。あわせて、検証が読むのは `document.scrollingElement.scrollTop` であることを明記する（`document.body.scrollTop` を読むと常に 0 で、今度は逆に落ちない）。

## P2

## Q
- `Phase 3` 全体 — フル実行に常設するか `--only` 専用に留めるか。直前に 529s → 372s へ縮めたばかりで、このスイートはアプリ起動込み・smooth の到達待ち・固定待ちを持つ。許容する追加秒数を決めないと、後から削る作業が出る。
- `仕様 > なめらかにスクロール` / `動作確認 > 1` — smooth 固定か、長大ページでは即時に倒すか。即時へ倒すと Phase 3 のポーリング前提・アサーション・`VERIFY.md` の記述がまとめて変わる。
- `概要・やりたいこと` — 今後 `j` / `k` / `Ctrl-d` へ広げる想定か。広げるなら `feedVimKey` の戻り値を `'top' | 'bottom'` に固定した時点で作り直しになる（ファイル名・スイート名 `vim-scroll` も同様）。

````

**対応**: P0 が空になったので収束。

収束回の P1 を 1 件反映（書き換えで済むもの）。`scroll-long.html` の fixture を `body { overflow-y: scroll }` から **`body { height: 100vh; overflow-y: scroll }`（中身はそれより高い）** に修正した。`body` の高さが `auto` だと `scrollHeight - clientHeight ≈ 0` で room が無く、`body` を採らない guard を外してもケース 1・2 が素通りで PASS する（＝ 3 回目に「回帰になる」と書いたものが実際には空検査だった）。あわせて、検証が読むのは `document.scrollingElement.scrollTop` であることを明記した（`document.body.scrollTop` は常に 0 なので、そちらを読むと今度は逆に落ちない）。

この差分は再レビューを通していない。

Q は 3 件とも 1〜3 回目と同じ内容で保留。

## Q への回答（人間の決定）

- **フル実行への常設**: **既定から外す**。`--only vim-scroll` と `--changed` でだけ回す。
  `verify-all.mjs:101` の `want` に opt-in 集合を噛ませる。代償（CDP の合成キーが後続を壊す回帰を
  CI で拾えない）は、Phase 4 で一度だけ既定除外を外してフルを通すことで受ける。
- **smooth**: **固定のまま進める**。人間の動作確認で NG なら、そのとき `behavior` と
  Phase 3 の polling・アサーション・`VERIFY.md` をまとめて直し「ログ > 方針変更」に記録する。
- **j / k / Ctrl-d への拡張**: **今回きり**。`feedVimKey` の戻り値は `'top' | 'bottom' | null` に固定。
  j/k は GitHub / Gmail と二重発火するサイトが実在し `preventDefault` しない方針と相性が悪いため、
  設計が未決のまま抽象だけ先行させない。
