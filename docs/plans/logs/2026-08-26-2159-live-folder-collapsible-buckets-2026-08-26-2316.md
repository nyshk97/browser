## 1回目

````text
## P0

## P1

- Phase 1: 開閉 state と小見出しのトグル > ステップ6 — 重複除外後に `items.length === 0` だが `truncation[bucket]` は存在する場合、小見出しが描画されない一方で打ち切り行は初期 `collapsed=true` により永久に隠れる / 両検索に同じPRが含まれるケースで既存の打ち切り情報が失われる / 小見出しが存在しないバケットの打ち切り行は隠さないか、`truncation` があれば0件でも小見出しを描画し、重複だけのfixtureで検査する
- Phase 3: 自走検証の追従 > ステップ7 — `items.length` を「取得済み件数」と定義しているが、これは検索結果を重複除外した後のバケット割当件数であり、実際の取得件数は `truncation.returned` と一致しないことがある / DESIGN.mdやコメントがデータの意味を誤って説明する / 「バケット内の表示対象件数」などDOM状態にも取得処理にも依存しない名称へ統一し、`returned` だけを検索の取得件数と呼ぶ
- Phase 3: 自走検証の追従 > ステップ3 — `mine` の全件既読化を「選ぶ」経路で行うには、現状PR_88のタブが存在しないため `liveFolderOpen` などで実際のGitHubページを開く必要がある / 自走検証に新たな外部通信とタブ・アクティブ状態への副作用が入り、既存の「特定の検査だけGitHubへ接続する」という前提も崩れる / 既読化済みで既に存在するPR_12を一時的に`mine`へ割り当てる制御レスポンス、または全件既読の再起動fixtureで「未読なし」のラベルを検査する

## P2

- Phase 1: 開閉 state と小見出しのトグル > ステップ2 — 新しい外側ラッパーと打ち切り文中の既存ラベルがどちらも `.lf-bucket` を名乗る / 現在は`data-bucket`で区別できても、同じクラスがコンテナとテキストという別の役割を持ちCSSや検査の変更時に衝突しやすい / 外側を `.lf-group` にするか、打ち切り側を `.lf-bucket-name` に改名する

## Q
````

**対応**: P1-1 → 打ち切り行の hidden 条件を「小見出しが描画されている（items.length > 0）かつ折りたたみ中」に限定し、重複除外で mine が空でも打ち切り行が見える検査を追加。P1-2 → 件数の定義を「バケットに割り当てられた件数（重複除外後）」に改め、returned/total と区別する文言に統一。P1-3 → mine の全件既読状態は既読済み PR_12 だけを mine に返す制御レスポンスで作る（実 GitHub 接続を避ける）。P2 は見送り。収束
