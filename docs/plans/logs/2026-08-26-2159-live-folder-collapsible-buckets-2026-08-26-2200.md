## 1回目

````text
## P0

- Phase 3: 自走検証の追従 > 1 — `EXPAND_ALL` を単に行数検査の前で呼ぶと、PR の非同期描画前や設定の再有効化による `LiveFolder` 再マウント後には空振りする / 折りたたまれたまま既存の `ROW_TITLES` 待機がタイムアウトする / `state.items` と小見出しの出現を待ってから展開する非同期ヘルパを作り、main・restart-read・再有効化後で明示的に呼ぶ
- Phase 3: 自走検証の追従 > 2 — 「起動直後に `.lf-sub[aria-expanded="false"]` が2件」は、既存検証の起動直後がトークン未設定の `Connect GitHub` 状態なので必ず0件になる / テストの前提と実際の初期画面が一致しない / `BASE` 取得後、小見出しを展開する前の「一覧初回表示時」として検査するよう条件と配置を明記する

## P1

- Phase 2: CSS > 2 — `.chev` のスタイルだけが計画され、矢印要素を描画する実装項目がない / CSSだけでは矢印は現れない / Phase 1 に `<span className="chev" aria-hidden="true">›</span>` の追加と、`aria-expanded="true"` に連動する回転を明記する
- Phase 3: 自走検証の追従 > 2 — 打ち切り行を折りたたみ時に隠す仕様の検査がない / `collapsed[bucket]` の条件漏れや別バケットとの取り違えを検知できない / `truncation.mine` が存在することを確認したうえで、Created を閉じると `.lf-truncated` が消え、再度開くと1件に戻る検査を追加する
- Phase 3: 自走検証の追従 > 2 — 未読検査が「小見出しのドットが開くと消える」までで、開いた行側へドットが移ることを確認していない / ヘッダーと行の両方からドットを消す誤実装でも通る / 対象バケットを特定し、閉時はヘッダーのみ、開時は該当 `.lf-row .dot` のみになることを前後比較する
- Phase 3: 自走検証の追従 > 2 — 「片方だけ開く」が `aria-expanded` の件数確認だけだと、閉じた側の行まで描画される不具合を見逃す / 独立開閉という本体仕様を十分に検証できない / バケットを識別できる属性またはラッパーを用意し、開いた側の行だけがDOMに存在することを検査する

## P2

- Phase 1: 開閉 state と小見出しのトグル > 6 — 右クリックメニューを維持する仕様に回帰検査がない / 将来イベント処理を変更した際に右クリックで開閉まで起きても気づけない / `contextmenu` 後にメニューが表示され、対象の `aria-expanded` は変化しない検査を追加する

## Q
````

**対応**: P0-1 → expandAll を非同期ヘルパにし、until の述語ごとに呼ぶ形へ。P0-2 → 検査の配置を「一覧初回表示時・expandAll 前」に明記。P1-1 → Phase 1 に chev 要素追加。P1-2/3/4 → 打ち切り・未読の行側移動・data-bucket による独立開閉の検査を追加。P2 は見送り

## 2回目

````text
## P0

## P1

- Phase 3: 自走検証の追従 > 3 — 折りたたみ中も件数を表示する仕様を検査していない / `collapsed` の条件に件数まで巻き込む誤実装でも通る / 初期折りたたみ検査で各 `.count` の値と可視性も確認する
- Phase 3: 自走検証の追従 > 2 — `restart-read` では行数検査のため即座に `expandAll` するため、「起動のたびに折りたたみ」の再起動経路を検査できない / キャッシュ復元時だけ展開される回帰を見逃す / キャッシュの小見出し出現を待ち、展開前に `aria-expanded="false"` と行数0を検査してから `expandAll` する
- 動作確認 > 1 — 矢印の向き・ホバー背景・折りたたみ時の件数配置を実アカウントでの人間確認に委ねており、ローカルの擬似データで外観を確認する工程がない / CSSのセレクタ漏れや回転方向の誤りが自走検証を通る / `BASE` 表示時の折りたたみ前後をスクリーンショットで確認し、少なくとも矢印の `transform` とホバー背景は computed style でも検査する
- Phase 1: 開閉 state と小見出しのトグル > 2 — `aria-expanded` だけで制御対象との関連付けがない / スクリーンリーダーには何を開閉するボタンか伝わりにくい / バケット内容に安定した `id` を付け、ボタンの `aria-controls` から参照する

## P2

## Q
````

**対応**: P1-1 → 件数の値と可視性の検査を追加。P1-2 → restart-read で expandAll 前に折りたたみ検査を追加。P1-3 → chev の transform・hover 背景の computed style 検査とスクショ保存を追加。P1-4 → ラッパーに id を付け aria-controls で参照

## 3回目

````text
## P0

## P1

- Phase 1: 開閉 state と小見出しのトグル > 2 — `aria-controls` がボタン自身を含む祖先ラッパーを指しており、実際に開閉される内容との関係が循環するうえ、別位置の `.lf-truncated` は対象外になる / 支援技術へ誤った制御関係を伝える / ボタンと兄弟の内容コンテナに安定した `id` を付け、そこを参照する構造にする
- Phase 3: 自走検証の追従 > 3 — 初期折りたたみ検査を始めるための待機条件がなく、直前の `waitRequests(1)` はレスポンス反映や小見出しの描画を保証しない / `Connect GitHub` のDOMを検査して間欠的に失敗する / `expandAll` を呼ばずに `.lf-sub` が2件になるまで待つ専用条件を先に置く
- Phase 3: 自走検証の追従 > 3 — 開時の `transform` を厳密な90°行列で検査する一方、矢印には `transition` を付ける計画になっている / クリック直後は `none` と最終行列の中間値になり、タイミング依存で失敗する / `transitionend` または最終行列になるまで待ってから検査する
- Phase 3: 自走検証の追従 > 3 — スクリーンショットの保存先と寿命が決まっていない / `NEMO_USER_DATA_DIR` 配下なら `verify-all.mjs` の後片付けで消え、リポジトリ配下なら検証のたびに作業ツリーを汚す / `/tmp` 配下など明示した成果物ディレクトリへ保存し、実行時に絶対パスを出力する

## P2

- Phase 1: 開閉 state と小見出しのトグル > 7 — 右クリックでは開閉しない仕様に回帰検査がない / イベント処理の変更でコンテキストメニューと開閉が同時発火しても検知できない / `contextmenu` 後にメニューが出て `aria-expanded` は変化しない検査を追加する

## Q
````

**対応**: P1-1 → 内容コンテナ（lf-items）を兄弟に置き aria-controls はそこを指す。打ち切り行もコンテナ内に移動。P1-2 → .lf-sub が2件になるまで待つ専用条件を追加。P1-3 → transform は最終値まで until で待つ。P1-4 → スクショは撮らない方針に変更（既存慣習なし）。P2 は見送り

## 4回目

````text
## P0
- Phase 2: CSS > ステップ2 — `.chev` は通常のインライン `span` のままで、`transform` が適用されない / 矢印が回転せず、計画済みの `matrix(...)` 検査も確実に失敗する / `.chev` に `display: inline-block` または `inline-flex` を追加する
- Phase 3: 自走検証の追従 > ステップ3 — `--restart-read` で小見出し2件を期待しているが、fixture は先頭1000件が `review` で、正規化は有効な200件に達した時点で打ち切るため `mine` は残らない / 実装が正しくても小見出しは1件となり検査が必ず失敗するうえ、末尾の不正URL・不正state検査も実際には正規化経路を踏まない / 上限到達前に有効な `mine` と不正項目を配置し、最終的に両バケット合計200件になる fixture へ組み替える

## P1
- Phase 1: 開閉 state と小見出しのトグル > ステップ2 — 折りたたみ時に `.lf-items` 自体を描画しないため、`aria-controls` が存在しないIDを参照する / 支援技術に壊れた関連付けを公開することになる / `.lf-items` は常時描画し、折りたたみ時は `hidden` を付けるか、中の行だけを非描画にする
- Phase 1: 開閉 state と小見出しのトグル > ステップ6 — `.lf-truncated` を各バケット内へ移すと、現在の「`StatusLine` の下」という表示順が「各バケット内・`StatusLine` の上」に変わり、「`StatusLine` の位置は従来どおり」と両立しない / 実装者によってDOM順と余白設計がぶれる / 現在位置のまま `!collapsed[bucket]` で条件描画する構造に改める
- Phase 1: 開閉 state と小見出しのトグル > ステップ5 — 折りたたみ中の未読情報が装飾用の青ドットだけで、行もDOMから消える / スクリーンリーダーでは未読の存在を認識できない / 小見出しに視覚非表示の「未読あり」を追加するか、意味を含む `aria-label` を設定する
- Phase 3: 自走検証の追従 > ステップ3 — 見た目を変更するのにスクリーンショット検証を明示的に省いている / 計算済みCSSの一致だけでは、矢印・余白・件数・ドットの配置崩れを検出できず、Web UI変更後は自分で撮影確認するというプロジェクト方針にも反する / `Page.captureScreenshot` 等で閉時・片側開時を撮影して目視確認する手順を追加する

## P2

## Q
````

**対応**: P0-1 → chev に display: inline-block。P0-2 → restart-read は「全小見出しが閉」に変更（fixture 確認済み: mine は残らない）。P1-1 → lf-items は常時描画し hidden。P1-2 → 打ち切り行は現在位置のまま条件描画。P1-3 → aria-label に件数と未読。P1-4 → CDP スクショによる自己確認ステップを追加（自走検証には組み込まない）

## 5回目

````text
## P0

## P1
- 実装計画 > Phase 3 — 「開いても再取得しない」という決定を検査していない / トグル実装時に誤って `liveFolderRefresh` を呼んでも全検証が通る / `resetCounters()` 後に各見出しを開閉し、短時間待ってリクエスト数が 0 のままである検査を追加する
- 実装計画 > Phase 3 — 初期折りたたみ状態では `.lf-row` が DOM に存在しないため、同じ場面で `.lf-sub:hover` と `.row:hover` の背景を比較できない / 比較対象を取得できず、外観検査の実装時に手順を再設計することになる / 小見出しの背景を取得後、片方を開いて実在する `.lf-row` をホバーして比較する手順まで明記する
- 実装計画 > Phase 3 — スクリーンショット確認で使う擬似 API サーバーの起動方法がない / `NEMO_GITHUB_TEST_ENDPOINT` は宛先を指定するだけで、既存の擬似サーバーは `verify-live-folder.mjs` 実行中にしか立たないため、記載手順のままでは3状態を固定して撮影できない / 固定レスポンス用モードまたは専用スクリプトを用意し、サーバー起動・環境変数・終了方法を具体的なコマンドとして計画に追加する
- 実装計画 > Phase 3 — `aria-label`、`aria-controls`、キーボード操作の検査がない / 見た目とマウス操作が通っても、Phase 1で明示したアクセシビリティ仕様の欠落を検出できない / ラベルの件数・未読有無、参照先IDの存在、EnterまたはSpaceによる独立開閉を検査する

## P2

## Q
````

**対応**: P1-1 → 再取得しない検査（resetCounters 後に total=0）を追加。P1-2 → hover 比較の順序を明記。P1-3 → 手動撮影をやめ、スクリプトの opt-in 環境変数 NEMO_VERIFY_SHOTS で撮る手順に変更。P1-4 → aria-label / aria-controls / Enter・Space の検査を追加

## 6回目

````text
## P0
- Phase 3: 自走検証の追従 > 3 — 未読検査は `.lf-sub[data-bucket="review"]` を参照する一方、Phase 1 では `data-bucket` を親 `<div>` に付けるため必ず 0 件になる / 検証が実装どおりでも失敗する / `[data-bucket="review"] > .lf-sub .dot` に直すか、`data-bucket` をボタン側へ移して全セレクタを統一する
- Phase 3: 自走検証の追従 > 3 — 最初の `BASE` 取得では `withUnread` により既存キャッシュにない3件すべてが未読になるため、review を開いた際の `.lf-row .dot` は PR_12 だけの1件ではなく2件になる / 計画どおりの期待値が必ず失敗する / PR_12 の仕込み前に既存3件を既読化し、「直前は未読0件」を確認してから PR_12 だけを再登場させる

## P1
- Phase 3: 自走検証の追従 > 3 — ホバー背景は2要素の計算値を比較するだけなので、合成マウスが当たらず両方とも透明でも通る / ホバー実装が壊れていても検査が空振りする / 各操作後に対象が `matches(':hover')` であることと、ホバー前後で背景色が変化したことも検査する
- Phase 1: 開閉 state と小見出しのトグル > 7 — 「右クリックのセクションメニューは従来どおり」が追加検査に含まれていない / `<div>` から `<button>` への変更で右クリック時に開閉やメニュー伝播が壊れても検出できない / 小見出しを右クリックしてメニューが表示され、`aria-expanded` とリクエスト数が変わらないことを追加する

## P2
- Phase 2: CSS > 3 — `.row .dot` と同じ値を `.lf-sub .dot` に複製する計画になっている / 将来どちらかだけ変更され、同じ未読表示なのに見た目がずれる / 色・サイズ・形は `.row .dot, .lf-sub .dot` の共有規則へ寄せ、配置差だけを個別指定する

## Q
````

**対応**: P0-1 → data-bucket はボタン自身に付けることを明記しセレクタ統一。P0-2 → 未読の期待値を liveState から算出する形に変更。P1-1 → :hover 成立と前後の変化を検査。P1-2 → 右クリックの検査を追加。P2 は見送り

## 7回目

````text
## P0
- Phase 3: 自走検証の追従 > ステップ3 — PR 12 の既読化後に両見出しのドットが 0 件になるという期待値が成立しない / 初回 `BASE` では3件すべてが未読になり、PR 12 を既読化しても PR 41 と PR 88 は未読のままなので検証が必ず失敗する / 全PRを明示的に既読化して `liveState` の未読件数が 0 になったことを確認してから両見出しのドット消失を検査するか、PR 12 の既読化では review 側の行ドットが `N → N-1` になることだけを検査する

## P1
- Phase 3: 自走検証の追従 > ステップ5 — `NEMO_VERIFY_SHOTS=<scratchpad>/shots` の保存先を作成する工程がない / 指定例の `shots` ディレクトリが未作成なら最初の書き込みが `ENOENT` で失敗する / 環境変数を読んだ時点で保存先を `mkdirSync(dir, { recursive: true })` し、ディレクトリでない場合は明示的に失敗させる
- Phase 3: 自走検証の追従 > ステップ6 — 永続的なUI仕様を変更するのに `VERIFY.md` しか更新対象になっていない / `DESIGN.md` の Live Folder > 小見出しが開閉・初期状態・未読ドット・矢印を記述しないまま残り、実装後の設計資料と画面が食い違う / `DESIGN.md` に独立開閉、初期折りたたみ、閉時の未読ドット、件数常時表示、矢印の規則を追記する

## P2

## Q
````

**対応**: P0 → 既読化後の期待値を「review 行ドットが N-1」に変更。P1-1 → NEMO_VERIFY_SHOTS の mkdirSync を明記。P1-2 → DESIGN.md 追記のステップを追加。上限到達のため再レビューは未実施

