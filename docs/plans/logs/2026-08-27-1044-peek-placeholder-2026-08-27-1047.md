codex session: 01a040e6-7779-7d83-9b6a-dba5ceeffcbe

## 1回目

````text
## P0
- 実装計画 > Phase 3 — 昇格時の背景色解除が任意になっている / `promotePeek()` は同じ View を通常タブとして再利用し、`setBackgroundColor()` の設定は自動解除されないため、「Peek のみ」という決定に確実に反する。透明なページでは通常タブにも色が露出する / 昇格時に `#00000000` へ戻す処理を必須化する。[Electron View API](https://www.electronjs.org/docs/latest/api/view)

## P1
- 実装計画 > Phase 1 — 未描画状態を作る遅延エンドポイントが Phase 2 まで用意されない / 既存ページが先に描画されると、採用済み View にマゼンタを設定してもページに隠れ、「効かない」と誤判定する / 遅延用エンドポイントの作成を Phase 1 の先頭へ移し、その URL で実測する
- 実装計画 > Phase 2 — 固定 1500ms の遅延に撮影タイミングを依存している / CDP 操作や `screencapture` が遅れるとレスポンス後を撮って検査が不安定になる / レスポンスを検証側が明示的に解放するゲート方式にし、リクエスト到達確認後に撮影してから解除する
- 実装計画 > Phase 2 — Peek 中央の小領域だけをピクセル判定する設計になっている / 中央が期待色でも、面の範囲や角丸が壊れているケースを検出できない / 中央・境界内側・角丸の切り欠き外側をそれぞれサンプリングし、実際の画像寸法と色許容差も出力する
- 実装計画 > Phase 4 — `node scripts/verify-peek.mjs` の単体実行に必要な環境変数が指定されていない / `NEMO_USER_DATA_DIR` が検証プロセスへ渡らないと診断ログ依存の検査が失敗し、別ポートなら検証対象も取り違える / 自前起動時は `NEMO_CDP`・`NEMO_TEST_PAGES`・`NEMO_USER_DATA_DIR` を明記するか、隔離環境を自動構築する `mise run verify:only peek` に統一する

## P2

## Q
````

**対応**: P0（昇格時の背景色解除）を Phase 3 の必須ステップにした。ただし戻す値は `#00000000` と断定せず、Phase 1 の実測で「触っていない通常タブと同じ見え方になるか」を確かめてから決める形にした（page 用 View は `transparent: true` を指定していないので、透明指定が黒くなる可能性がある）。P1 は4件とも採用: ①遅延エンドポイントを Phase 1 の先頭へ移動（未描画を作れないと実測が誤判定になる指摘は妥当） ②固定 sleep をやめ、検証側から解放するゲート方式に ③サンプリングを中央1点から「中央・面の内側・角の外側」の3種に広げ、色だけでなく範囲と角丸まで見る ④`node scripts/verify-peek.mjs` の直叩きを `mise run verify:only peek` に変更（起動・隔離・CDP ポートを verify-all に任せる）。あわせて、検査は verify-peek.mjs に足すこと、`screencapture` が画面収録の権限で通らない場合は自動化を打ち切って人手手順に落とすことを明記した。

## 2回目

````text
## P0

## P1
- 実装計画 > Phase 1 — 背景色を戻した View と「触っていない通常タブ」の比較条件が未指定 / 通常タブのページが不透明なら背景色は隠れ、どの戻し値でも同じに見えて誤判定する / 同じゲート中の未描画ページ、または背景が明示的に透明なテストページを、同じ下地・同じ昇格後状態で比較する
- 実装計画 > Phase 2 — Electron の座標と BMP のピクセル座標を同一尺度として扱っている / `screenX`・View bounds は DIP だが、Retina 環境のスクリーンショットは物理ピクセルになるため、中央や角のサンプル位置がずれる / BMP の実寸と指定した撮影範囲から倍率を求めて座標変換し、倍率・画像寸法・変換後座標も検査出力へ含める
- 実装計画 > Phase 2 — 修正前 FAIL の確認に `git stash` を使う記述が残っている / Phase 2 は Phase 3 より前なので戻す変更はまだなく、実装後に実行すると無関係な作業中変更まで stash する恐れがある / Phase 2 完了時に未修正コードで FAIL を記録し、Phase 3 後に同じ検査を PASS させる順序へ明記し直す

## P2

## Q
````

**対応**: P0 なしで収束。P1 は3件とも採用した。①戻し値の比較条件を「同じゲートで止めた未描画ページ同士」に限定（描画済みの不透明なページ相手では、どの戻し値でも同じに見えて誤判定する） ②Retina の物理ピクセルと Electron の DIP の尺度差を明記し、倍率換算と換算後座標の出力を検査に要求 ③修正前 FAIL の確認を Phase 2 の時点で行う順序に直した（Phase 2 は Phase 3 より前なので `git stash` で戻すものが無く、無関係な作業中変更を巻き込む）。
