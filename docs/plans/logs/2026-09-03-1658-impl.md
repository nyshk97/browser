review session: 1f8a9c7e-fa4d-4d7d-854d-cff085e7c154

## 1回目

````text
レビュー結果です。差分（Sidebar / InlineRename / verify-pins / 3 つのドキュメント）と plan・実装ログを読み、主張はコードで裏取りしました（`openFavorite` は `findFavorite` → `win.normalTabs.find` → `createTab` まで同期、IPC ハンドラ `nemo:open-favorite` も同期なので「2 発目は選ぶだけ」は成立。`useDelayedClick` は `PinnedTree.tsx` に残り、Sidebar 側に `schedule` / `cancel` の残骸なし。`.fav` には `data-id` があるので `data-id` 固定も効く）。なお `mise run typecheck` / `lint` / `verify:only pins` はこの環境では実行を拒否されたため、結果はログの記載（138 件 PASS・修正前 FAIL 3 件）を前提にしています。

## P0
- `DESIGN.md:「名前を変える」` — 「ピン留め・**Favorites**・一時タブは、行を**ダブルクリック**するとその場で名前を書き換えられる」「**フォルダだけは右クリックからしか変えられない**」「**閉じている枠は、単クリックの反応を 250ms 待ってから開く**」が 3 つとも今回の実装と逆のまま残っている / DESIGN.md は冒頭で「実装で迷ったらここを見る」と宣言している UI 規則の正で、2026-08-24 のフォルダ変更ではここ（`フォルダだけは…`）を更新している。今回だけ落ちると、次に Favorites を触る人が「Favorites はダブルクリックでリネーム・閉じている枠は 250ms 待つ」を仕様として読み、`operations.md` / `VERIFY.md` と食い違う / 「ダブルクリックで変えられるのはピン留め・一時タブだけ」「フォルダと Favorites は右クリックのみ」「250ms の遅延は**閉じているピン**だけ（Favorites の枠は即開く）」に書き直す。`InlineRename.tsx` の doc コメントと同じ切り分けに揃う

## P1

## P2
- `scripts/verify-pins.mjs:check('Favorite は右クリックの「名前を変更」で編集に入る')` — 直後の `setter.call(input, …)` と `item.click()` は要素が無いと eval が例外を投げ、`ev` が throw してスイートごと止まる（`const [firstFav, secondFav] = (await shared()).favorites` も定義が 2 件無いと `undefined.id` で同じ形で落ちる） / 右クリック導線が退行したとき、FAIL 1 件ではなく以降の節（`URLを変更…` 以降）が丸ごと走らない。plan が旧コードの欠点として挙げていたのと同じ失敗の形 / eval 側を `if (!input) return 'missing'` のように null 安全にして、判定は後続の check に落とす（フォルダ節も同じ形なので、house style として揃えるなら現状維持でも可）
- `scripts/verify-pins.mjs:CLICK_DELAY_MS` — 実装の `InlineRename.tsx:CLICK_DELAY_MS` をコメントだけの対応で複製している / 実装側の値を変えても検査の閾値（と `CLICK_DELAY_WAIT_MS = 600`）は追随せず、閾値の意味が静かにずれる。今回は 4ms vs 250ms で余裕が大きいので実害は薄い / 将来ここを触るなら、UI 側から値を読む（`window.nemo` 経由で公開する／ユニットテストで両者の一致を見る）ほうが安全

## Q

````

**対応**: P0（DESIGN.md「名前を変える」節が旧仕様のまま）を反映: ダブルクリックで変えられるのはピン留め・一時タブだけ、フォルダと Favorites は右クリックのみ、250ms の遅延は閉じているピンだけで Favorites は即開く、に書き直した。P2 の 2 件は見送り: eval の null 安全化はフォルダ節と同じ house style で、足すと検査の仕組みが増える（終了報告に回す）/ `CLICK_DELAY_MS` の複製は UI からの公開やユニットテストの追加が要るので今回の範囲に入れない（終了報告に回す）。

## 2回目

````text
DESIGN.md の修正を読み直し、他のファイルの差分が前回から変わっていないこと（`git diff --stat` が DESIGN.md 以外で完全に一致）と、リポジトリ全体に旧仕様の記述が残っていないこと（`ダブルクリック` / `250ms` の全文検索）を確認しました。指摘した 3 点はいずれも実装どおりに直っています（ダブルクリックでのリネームはピン留め・一時タブだけ＝`PinnedTree.tsx` / `TabRow.tsx` の `onDoubleClick` と一致、フォルダと Favorites は右クリックのみ、250ms の遅延は `PinnedTree.tsx` の閉じているピンだけ＝`schedule(...)` の残る唯一の呼び出し）。見送った P2 2 件の理由も妥当なので繰り返しません。収束です。

## P0

## P1

## P2
- `docs/plans/2026-09-03-1632-favorites-no-dblclick-rename.md:Phase 3`（plan: Phase 3） — 差分に `DESIGN.md` の書き換えが入ったのに、Phase 3 のステップにも「ログ > 試したこと・わかったこと」にも DESIGN.md が出てこない（列挙は operations.md / CHANGELOG / VERIFY.md の 3 つのまま） / 後から plan と差分を突き合わせる人が「計画外の変更」に見えるし、次に同種の UI 規則を変える人が「DESIGN.md も直す」を計画に入れ損ねる（今回まさに落ちた項目） / Phase 3 に `DESIGN.md` の「名前を変える」節を直す項目を 1 行足してチェック済みにする（レビューで足りた経緯はログ側に 1 行）

## Q

````

**対応**: 収束（P0 なし）。P2 を反映: plan の Phase 3 に DESIGN.md の項目を足してチェック済みにし、ログ > 試したこと に経緯を 1 行追記。
