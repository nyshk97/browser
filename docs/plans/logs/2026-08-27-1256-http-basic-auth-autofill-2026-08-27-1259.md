codex session: 01a04160-063b-7842-a4aa-1eae158c20de

## 1回目

````text
## P0
- 実装計画 > Phase 1 — `convertMultipassPattern` は `https://example.com/` や `example.com/path` も「メタ文字が `.` だけ」と判定し、壊れたホストパターンへ変換する / 任意の正規表現を受け付ける MultiPass の実データを黙って破壊する / 変換対象を厳密な裸のホスト名（必要ならポート付き）に限定し、scheme・path・IP・既存正規表現を素通しするテストを追加する
- 実装計画 > Phase 1 — 「複数マッチしうる正規表現の組を洗い出す」は、backreference や lookaround を含む任意の JavaScript 正規表現では実装方法が定義できない / インポート警告と Settings UI の完成条件を満たせず設計変更が確定する / 具体的な検査 URL に対する複数マッチだけを警告するか、重なり判定をせず旧 priority 順と新しい長さ順が逆転した組を保守的に警告する仕様へ変える
- 実装計画 > Phase 3 — `pendingNavigation` は `did-start-navigation` で更新するだけで、成功・失敗・中断時に消す手順がない / B への失敗した遷移後も A のページから発生した B のサブリソース認証を「遷移中の B」と誤認し、同一オリジン制約を破る / `did-navigate`・main-frame の `did-fail-load`・破棄時に対象 URL を照合して消し、失敗したクロスオリジン遷移後のサブリソースを E2E に追加する

## P1
- 実装計画 > Phase 3 — 自動入力の除外条件に `authInfo.scheme` がなく、現在の Basic / Digest 共通ハンドラでは Digest 等にもルールが適用される / 機能名と資格情報の適用範囲が一致せず、意図しない認証方式で試行される / `scheme === 'basic'` のときだけ自動入力し、それ以外は従来ダイアログへ送る
- 実装計画 > Phase 3 — 試行キーがタブ・host:port・realmだけなので、同じ protection space の並列リクエストも「2回目＝資格情報拒否」と数える / 正しい資格情報でも画像や API の同時ロードでダイアログへ落ちうる / 少なくとも scheme と `details.url` をキーに含め、同一オリジンの認証付きリソースを並列取得する E2E を追加し、WebContents 破棄時にも状態を掃除する
- 実装計画 > Phase 2 — auth cache の消去が削除時だけで、対象セッションも明記されていない / 有効トグルや資格情報編集が現在のセッションに反映されず、`defaultSession` を消しても実際の `persist:nemo` には効かない / 有効状態・pattern・username・passwordを変更または削除したら、`session.fromPartition(PAGE_PARTITION).clearAuthCache()` を await するサービス層へ集約する
- 実装計画 > Phase 5 — ユーザー入力の任意正規表現を main プロセスで直接実行する / catastrophic backtracking を含むパターンを貼ると Settings だけでなくブラウザ全体が停止する / pattern・URL長の上限に加え、安全な構文制限かタイムアウト可能な worker 実行を仕様化する
- 実装計画 > Phase 2 — `importedFrom` を保持する決定に対し、`listRules()` の戻り値から除外されている / 変換履歴をユーザーが後から確認するという保存目的を達成できない / 非機密フィールドとして一覧型と IPC に含め、変換されたルールの詳細に表示する
- 実装計画 > Phase 6 — 暗号化ストアの重要な失敗経路が検証対象にない / `safeStorage` 利用不可、暗号文1件の破損、一覧への password 混入、再起動後の復号が壊れても E2E が通りうる / 暗号化アダプターを差し替え可能にし、保存拒否・1件だけ無効化・秘匿一覧・再読込をストア単位で検査する

## P2
- 実装計画 > Phase 5 — 「表示」で取得したパスワードをいつ再マスクするか決まっていない / Settings を開いたままにすると平文が残り続ける / 一定時間後、Settings を閉じたとき、別ルールを表示したときに再マスクする

## Q

````

**対応**: P0 3件はすべて反映。① `convertMultipassPattern` の変換対象を「裸のホスト名（+任意ポート）に厳密一致」に限定し、scheme付き・パス付き・IP・メタ文字入りを素通しにした（素通しになるケースを回帰テストの本命として明記）。② 「複数マッチしうる正規表現の組の検出」は任意の正規表現では実装方法が定義できないので仕様ごと変更 — インポートは priority が一様でなければ一括警告、Settings の複数マッチはテスターに入れた実 URL に対する照合に格下げ。前提セクションに「特定しない」理由を追加し決定表 #18 も修正。③ `pendingNavigation` の消去契機（`did-navigate` / メインフレームの `did-fail-load` / 破棄）を追加し、失敗したクロスオリジン遷移後のサブリソースを E2E に追加。

P1 は6件すべて反映。`scheme === 'basic'` 限定（決定表 #8 に追記）、`attempts` のキーに `details.url` と scheme を含める（並列 401 の誤カウント。E2E も追加）、`clearAuthCache` を `session.fromPartition(PAGE_PARTITION)` に対して削除以外の変更でも呼ぶ形に集約（決定表 #19 を書き換え）、正規表現の長さ上限（ReDoS で main ごと固まる）、`listRules()` に `importedFrom` を含める、ストアの失敗経路を fixture ベースで検証（暗号文1件破損・password 混入・再起動後の復号・保存拒否）。ただし P1 の「暗号化アダプターを差し替え可能にする」は設計を増やす割に得るものが fixture 方式と変わらないので採らず、検証項目だけを落とした。

P2 の「表示したパスワードの再マスク」も反映（1行で済み、決めておかないと実装時に落ちるため）。

## 2回目

````text
## P0

- Phase 4: ダイアログ（保存と prefill） > ステップ1 — `canSave` の除外条件が暗号化不可・シークレットだけで、プロキシ・非Basic・クロスオリジンでも保存チェックが出る / これらは `resolveCredential` が常に拒否するため、保存しても次回自動入力されないルールができる / 暗号化可・通常タブ・非プロキシ・Basic・同一オリジンを共通の保存適格判定にし、リトライ回数だけを判定から除外する
- Phase 6: 検証 > ステップ3 — fixtureだけでは `safeStorage` 利用不可を作れず、端末鍵依存の正常な暗号文も別マシンへ持ち運べない / 保存拒否テストは常に利用可能なmacOS CIで条件に到達せず、静的fixtureでは正常ルールまで復号失敗して「1件だけ壊れた」を証明できない / 同じ実行環境で2件保存してから1件だけ暗号文を壊す方式にし、利用不可については非パッケージ版限定のテスト用overrideか利用可否判定の小さな差し替え口を設ける

## P1

- /dig で確定した仕様 > #4 — 前提①と決定表は試行キーを `(タブ, host:port, realm)` のまま記載し、Phase 3 は `(wc.id, scheme, details.url)` に変更されている / どちらが正なのか実装者や後続レビューが判断できず、並列401対策が古い仕様へ戻りうる / 前提①と #4 も Phase 3 と同じキーへ更新する
- Phase 3: main の認証ハンドラ > ステップ1 — `did-fail-load` 等で `pendingNavigation` を無条件に消すと、古い遷移の失敗イベントが新しい遷移の pending を消す競合が残る / 素早い連続ナビゲーションで正しい自動入力がダイアログへ退行する / イベントのURLと現在記録しているpendingが一致するときだけ削除し、Bを開始直後にCへ遷移する検査を追加する
- Phase 5: Settings の管理 UI > ステップ1 — pattern・URLの長さ制限だけでは `(a+)+$` のような短い正規表現による catastrophic backtrackingを防げない / 上限内の入力でもmainプロセス全体が停止する / 照合を終了・再生成可能なworkerへ隔離してタイムアウトさせるか、許容する正規表現構文を安全な範囲へ制限する
- Phase 2: ストア > ステップ1 — `JsonStore` の初期化と終了時のflushを `index.ts` へ配線する手順がない / 既存ストアの作法に合わせると未初期化になり、デバウンス中の保存は終了時に失われる / `initHttpAuthStore()` をハンドラ・IPC登録前に呼び、`closeHttpAuthStore()` を `before-quit` に追加する
- Phase 3: main の認証ハンドラ > ステップ2 — `isPrivate` を「引数で受け取る」とだけあり、現在のresolverはwindow IDしか返さず、未所有WebContentsをフォーカス中のwindowへフォールバックする / 認証元とシークレット状態を厳密に結び付けられない / 所有タブを厳密検索して `{ windowId, isPrivate }` を返すresolverを用意し、未所有WebContentsでは自動入力しない
- Phase 5: Settings の管理 UI > ステップ1 — `safeStorage` 利用不可メッセージを出す一方、列挙されたIPCには利用可否を返すものがない / rendererが保存を試す前に状態を表示できない / 一覧レスポンスを `{ rules, storageAvailable }` にするか、専用status IPCを追加する
- Phase 5: Settings の管理 UI > ステップ1 — テスターIPCは「1パターンとURL群」の照合だが、UI要件は保存済み全ルールの複数マッチと勝者表示になっている / renderer側で優先順位ロジックを重複実装するか、IPCをルールごとに繰り返す構造になる / main側で保存済みルールへ `matchRules` を適用し、URLごとのmatched IDsとwinnerを一括で返す形にする

## P2

## Q

- Phase 5: Settings の管理 UI > ステップ3 — パスワードを「一定時間」で再マスクするとあるが時間が未決定 / 実装値とUI検証の待機時間が任意になるため、何秒後に隠すか決める必要がある
````

**対応**: P0 2件を反映。① `canSave` を `resolveCredential` と同じ判定（暗号化可 / 通常タブ / 非プロキシ / Basic / 同一オリジン、リトライ回数だけ除外）に共通化 — 指摘どおり、暗号化とシークレットだけだと「保存しても次回自動入力されないルール」が作れてしまう。② 暗号化ストアの検証を静的 fixture から**実行時生成**に変更（safeStorage は端末鍵なのでコミットした暗号文は別マシンで全件復号失敗し「1件だけ壊れた」を証明できない）。同一実行内で2件保存 → 1件だけ破壊 → 残り1件が生きることを、破壊前に2件読めていた事実込みで確認する形にした。`safeStorage` 利用不可は Phase 2 に「利用可否判定を1か所に閉じ dev 限定で false に倒せる口を設ける」を追加し、使わない場合は未検証と明記する条件にした。

P1 は7件すべて反映。前提①と決定表 #4 の試行キーを Phase 3 と同じ `(wc.id, scheme, details.url)` に更新（仕様本体が古いまま残ると後続レビューが逆戻りさせるため）、`pendingNavigation` の削除をイベント URL と pending の一致時のみに限定（B→C 連続遷移の競合）、正規表現は長さ上限に加えて**ネストした量化子と後方参照を拒否**（`(a+)+$` は6文字なので長さでは防げないという指摘が正しい）、`initHttpAuthStore` / `closeHttpAuthStore` の `index.ts` 配線、`findTabByWebContents`（既存・タブ以外は null を返す厳密版）でシークレット判定を厳密化しタブ以外では自動入力しない、一覧 IPC に `safeStorage` 利用可否を同梱、テスター IPC を「保存済みルール全体に matchRules を当てて URL ごとの matched IDs と winner を返す」形に変更。

P1 の「照合を worker へ隔離してタイムアウトさせる」は、実装量に対して構文制限＋長さ上限で実用上の catastrophic backtracking はほぼ潰せるため採らず、構文制限のみ採用した。

Q（再マスクまでの秒数）は人間の意思決定なので手を付けていない。

## 3回目

````text
## P0

- Phase 3: main の認証ハンドラ > ステップ1 — server-side redirect時に `pendingNavigation` を更新する手順がない / `did-start-navigation` 後の302は別の `did-redirect-navigation` で通知されるため、HTTP→HTTPS→401のような経路ではpendingが旧originのままになり、自動入力も保存も拒否される / main-frameの `did-redirect-navigation` でもpendingを遷移先URLへ更新し、302で別originへ移ってBasic認証されるE2Eを追加する

## P1

- Phase 5: Settings の管理 UI > ステップ1 — ネスト量化子と後方参照の拒否だけでは、`^(a|aa)+$` のような短い重複alternationによる指数的backtrackingが残る / 長さ上限内でもmainプロセスを停止できる / 量化されたグループ内の重複alternationやlookaroundも禁止するなど許容構文を明文化し、共通validatorの拒否・許可例をテストする
- Phase 5: Settings の管理 UI > ステップ1 — 危険パターン検査が「保存時点」としか書かれておらず、MultiPassインポートや既存JSONの正規化から入ったパターンが迂回できる / Settingsで拒否した正規表現が再起動後の認証照合でmainを固めうる / `validateHttpAuthPattern`を1つ作り、編集・インポート・`normalizeRules`・テスターの全入口で使う
- Phase 4: ダイアログ（保存と prefill） > ステップ1 — 新しい共通適格判定の各分岐を検証する項目がない / proxy・Digest・クロスオリジンのどれかが判定から漏れると、再び保存しても使われないルールが作られる / 暗号化不可・シークレット・proxy・非Basic・クロスオリジン・正常系について、`canSave`と自動入力可否を表形式の単体テストで固定する
- Phase 6: 検証 > ステップ2 — auth cacheの検証が削除経路だけで、今回追加した無効化・pattern・username・password編集を通していない / 各IPCがキャッシュ破棄の共通処理を呼び忘れても検出できない / 認証成功後に少なくとも有効トグルと資格情報編集を行い、同一セッションで再チャレンジが発生することを検証する
- Phase 6: 検証 > ステップ3 — Phase 2で利用不可overrideを必須実装する一方、検証側は「使わない場合は未検証」を許している / 実装したテスト口を使わず必須の保存拒否経路を未検証のまま完了できる / dev限定overrideを必ず使用する合格条件にし、未検証ならPhase 6を未完了にする
- Phase 4: ダイアログ（保存と prefill） > ステップ3 — `AuthPrompt`のstateをprompt ID変更時に初期化する手順がない / 認証promptが連続するとReactが同じコンポーネントを再利用し、前のhostのusername・password・save状態が次のhostへ残りうる / `prompt.id`をkeyにして再mountするか、ID変更時にprefillとチェック状態を明示的にresetする
- Phase 5: Settings の管理 UI > ステップ3 — `importedFrom`は一覧IPCに追加されたが、Settingsの表示項目には含まれていない / 変換前パターンを後から追跡するという保持目的をUIから達成できない / 自動変換されたルールに「インポート元」を表示する

## P2

## Q

- Phase 5: Settings の管理 UI > ステップ3 — パスワードを「一定時間」で再マスクするとあるが時間が未決定 / 実装値とUI検証の待機時間が任意になるため、何秒後に隠すか決める必要がある
````

**対応**: P0 1件を反映。メインフレームの `did-redirect-navigation` でも `pendingNavigation` を更新する手順を追加。サーバー側 302 は `did-start-navigation` では通知されないので、`http://x` → `https://x` → 401 という日常的な経路で pending が旧オリジンのまま残り、自動入力も保存も拒否される。

P1 は7件すべて反映。① 危険な正規表現の検査を `validateHttpAuthPattern` として Phase 1 に切り出し、編集 / インポート / `normalizeRules` / テスターの全入口で通す形にした（保存時だけだと迂回経路が残る）。禁止対象を「量化されたグループ内の量化子・alternation・lookaround、および後方参照」に広げた（`^(a|aa)+$` は 9 文字なので長さ上限では防げないという指摘が正しい）。許容構文は validator 側で明文化し拒否例・許可例をテストで固定。② `canSave` と自動入力可否を「暗号化不可 / シークレット / プロキシ / 非 Basic / クロスオリジン / 正常系」の表形式で単体テストに固定。③ auth cache の検証を削除だけでなく有効トグルと資格情報編集にも広げた。④ `safeStorage` 利用不可の検証を「dev 限定の差し替え口を必ず使う」必須条件にし、逃げ道（未検証と明記すれば可）を削除。⑤ `AuthPrompt` を `prompt.id` を key にして再マウント（連続する認証ダイアログで前のホストの入力値が残るバグ）。⑥ `importedFrom` を Settings の表示項目に追加。

Q（再マスクまでの秒数）は前回同様、人間の意思決定なので手を付けていない。

## 4回目

````text
## P0

- Phase 1: 純粋ロジック + 単体テスト > ステップ1 — `convertMultipassPattern` が生成する `^https?://([^/]+\.)?example\.com/` は、外側が `?` で量化され、内側に `+` を含むグループなので、現在のvalidator規則では拒否される / 裸のホスト名から変換したルールがインポート直後または再起動時に全て落ちる / 外側が最大1回の `?`・`{0,1}` はネスト量化子禁止の対象外にするか、この安全な生成形を明示的に許可し、「変換→validate→import→normalize後も残る」結合テストを追加する

## P1

- Phase 6: 検証 > ステップ2 — `did-redirect-navigation` の実装手順は追加されたが、302→Basic認証のE2Eがない / リスナーの付け忘れやmain-frame判定ミスがあっても既存検査は通る / test serverに別originまたはHTTP→HTTPSの302経路を設け、redirect先でダイアログなしに認証できることを検証する
- Phase 1: 純粋ロジック + 単体テスト > ステップ2 — `canSave`と自動入力可否の表形式テストはあるが、対応する本番用の純粋な共通述語が実装項目にない / `resolveCredential`とprompt生成側が別々に条件を書けば、テストが片方しか検証しない状態へ戻る / `evaluateHttpAuthEligibility`のような純粋関数をPhase 1に明記し、両経路がその戻り値を使う形にする
- Phase 3: main の認証ハンドラ > ステップ1 — `resolveCredential` が全ての非自動入力を同じ`null`で返す一方、後段は「拒否された保存済みルール」「ルールなし」「自動入力対象外」を区別する必要がある / `rejected`・prefill・`canSave`を正しく作れず、シークレットで保存ルールを再読込する実装にもなりうる / `autofill`・`rejected`・`prompt`・`ineligible`などのdiscriminated unionを返し、prefillは`rejected`にだけ含める
- Phase 3: main の認証ハンドラ > ステップ2 — strictな`findTabByWebContents`へ切り替えた後、タブ以外で「従来どおりダイアログ」を出すwindow IDの解決方法が書かれていない / strict resolverのnullをそのまま扱うと、既定動作の認証キャンセルになりダイアログが出ない / 自動入力可否にはstrict resolverを使い、手動promptの宛先には既存の`findWindowIdForPageContents`を使う二段構成を明記する
- Phase 5: Settings の管理 UI > ステップ1 — `nemo:save-http-auth-rule`の更新形式が未定義で、パスワードを持たない一覧データからpatternだけ編集したときの暗号文保持方法が分からない / 空パスワードで上書きするか、rendererへ一度パスワードを返す実装になりうる / metadata更新IPCと資格情報更新を分けるか、password省略時は既存暗号文を保持するpatch semanticsを明記する
- Phase 3: main の認証ハンドラ > ステップ1 — `(wc.id, scheme, details.url)`では同じURLへの並列リクエストと誤パスワードの再試行を区別できないのに、検証は「並列401をすべて自動入力」と一般化している / 同一URLを並列取得した場合は仕様どおり2回目でダイアログへ落ちる可能性がある / アカウントロック回避を優先する既存方針を維持するなら、この制約を明記し、並列E2Eは異なるrequest URLであることを固定する

## P2

## Q

- Phase 1: 純粋ロジック + 単体テスト > ステップ1 — MultiPass内にvalidatorで拒否されるルールが1件でもあった場合、全インポートを中止するか、有効なルールだけ取り込んで拒否分を報告するかが未決定 / インポートの原子性・結果表示・回帰テストの期待値が決められない
- Phase 5: Settings の管理 UI > ステップ3 — パスワードを「一定時間」で再マスクするとあるが時間が未決定 / 実装値とUI検証の待機時間が任意になるため、何秒後に隠すか決める必要がある
````

**対応**: P0 1件を反映。前回入れた validator の規則が、変換器が生成する `^https?://([^/]+\.)?example\.com/` 自身を弾く自己矛盾になっていた（外側が `?` の量化グループで内側に `+` を含む）。外側の量化子が最大1回（`?` / `{0,1}`）なら対象外にする例外を追加し、「変換 → validate → import → normalize を通しても落ちずに残る」結合テストを Phase 1 に追加した。

P1 は6件すべて反映。① `evaluateEligibility` を Phase 1 の純粋関数として明記し、`resolveCredential` とダイアログ生成の両方がこの戻り値を使う形にした（表形式テストだけ足して本体の共通述語が無いと、条件が二重実装されて片方しかテストされない状態に戻る）。② `resolveCredential` の戻り値を `autofill` / `rejected` / `no-rule` / `ineligible` の判別可能な union にし、prefill は `rejected` にだけ載せる。③ 宛先の解決を二段にした — 自動入力可否は strict な `findTabByWebContents`、手動ダイアログの宛先は従来の `findWindowIdForPageContents`。strict 版の null をそのまま返すと既存の `if (windowId === null) return` に落ちて認証キャンセルになりダイアログが出ない、という指摘が正しい。④ `nemo:save-http-auth-rule` に patch semantics（password 省略時は既存暗号文を保持）を明記。⑤ 302 で別オリジンへ飛んだ先の 401 の E2E を追加。⑥ 並列 401 の E2E に「リクエスト URL は互いに異なるものにする」という前提を明記し、同一 URL の並列は仕様どおり2回目でダイアログに落ちることを書いた。

Q は2件（インポートで validator に拒否されるルールがあったときの原子性、再マスクまでの秒数）。どちらも人間の意思決定なので手を付けていない。

## 5回目

````text
## P0

- Phase 2: ストア > ステップ1 — `clearAuthCache()`時に`attempts`をリセットする連携がない / 正しい資格情報で一度成功したURLも、Settingsで資格情報を編集して全認証キャッシュを消した後は同じURLの次回401が「2回目＝拒否」と判定され、新しい資格情報を自動入力できない / キャッシュ破棄と全attempts破棄を同じ無効化サービスに集約し、ナビゲーションなしの再fetchでも編集後の資格情報が自動入力される検査を追加する
- Phase 6: 検証 > ステップ3 — 実行時生成に変えても、実`safeStorage`で2件保存・再起動・復号する時点でmacOS Keychainへ触れる / 既存のLive Folder検証では同じ操作で`SecurityAgent`が起動して自走検証が永久停止した実績があり、今回も同じハーネスでは完走できない / 非パッケージ検証時だけ暗号化・復号を行うテスト用crypto backendへ差し替え、実`safeStorage`との結合確認は人間の動作確認へ分離する

## P1

- Phase 6: 検証 > ステップ2 — Phase 5のSettings UIについて、表示・インポート・validatorエラー・テスターの勝者・`importedFrom`・パスワード表示を操作する検査がない / main側のIPCが正しくてもrendererの配線漏れや表示不具合を検出できない / ダミーJSONを使ってSettingsを実操作するUI検査を追加し、再マスク時間決定後は時間経過によるマスクも含める
- Phase 4: ダイアログ（保存と prefill） > ステップ3 — `prompt.id`による再マウントを実際に踏む検査がない / 現在の誤パスワード検査は「promptなし→rejected prompt」なので、同じAuthPromptから別のAuthPromptへ切り替わる経路を通らない / 同一ウィンドウで異なるホストの認証promptを連続表示し、2件目に1件目の入力値とチェック状態が残らないことを検証する
- Phase 1: 純粋ロジック + 単体テスト > ステップ1 — MultiPass JSONとして受け付けるroot形式・各フィールドの型・件数や文字列長の上限が定義されていない / malformed JSONや巨大入力に対する挙動と、現在のobject形式を正しく読めるかの完成条件が決まらない / 現行exportのobject形式と必要フィールドをスキーマ化し、構文エラー・欠損・型違い・上限超過をテストする
- Phase 1: 純粋ロジック + 単体テスト > ステップ1 — priorityの「一様」判定で、欠損・数値`1`・文字列`"1"`をどう扱うか未定義 / MultiPass上では同じ優先度でも不要な警告が出る可能性がある / `Number(priority ?? 1)`のように実効値へ正規化して比較し、欠損・数値・文字列が混在するケースを固定する

## P2

## Q

- Phase 1: 純粋ロジック + 単体テスト > ステップ1 — MultiPass内にvalidatorで拒否されるルールが1件でもあった場合、全インポートを中止するか、有効なルールだけ取り込んで拒否分を報告するかが未決定 / インポートの原子性・結果表示・回帰テストの期待値が決められない
- Phase 5: Settings の管理 UI > ステップ3 — パスワードを「一定時間」で再マスクするとあるが時間が未決定 / 実装値とUI検証の待機時間が任意になるため、何秒後に隠すか決める必要がある
````

**対応**: P0 2件を反映。

① `clearAuthCache()` と `attempts` の連動。資格情報を編集して認証キャッシュを消しても `attempts` が残っていると、同じ URL の次の 401 が「2回目＝拒否」と判定されて新しい資格情報が使われない。Phase 2 の「資格情報が変わったときの後始末」を、キャッシュ破棄と `attempts` 全消しを必ず両方行う集約点として書き直した。

② 自走検証が実 `safeStorage` に触る件。**主張の裏を取ったところ、このリポジトリ自身に記録された既踏の事故だった** — `VERIFY.md:695` と `docs/plans/2026-08-25-1924-github-pr-live-folder.md:730` に「自走検証が PAT を保存した瞬間に `SecurityAgent` が上がり、検証が永久に止まった」とある。私が書いた Phase 6（2件保存 → 再起動 → 復号）はこれをそのまま再現する。既存の回避策（`NEMO_GITHUB_TEST_AUTH=stored-only` / `live-folders/token.ts:73`）に倣い、Phase 2 を「利用可否判定を1か所に閉じる」から「暗号化・復号そのものを dev 限定で差し替え可能にする」に拡張し、Phase 6 は差し替え backend で回す形にした。前提セクションに事故の記録を引用して理由を残し、実 `safeStorage` との結合確認は人間の動作確認（`cat http-auth.json` で平文が無いことを見る。VERIFY.md の PAT と同じ作法）に分離した。

P1 は4件すべて反映。① Settings を実操作する検査を追加（一覧・インポート結果と priority 警告・validator エラー・テスターの勝者・`importedFrom`・パスワード表示。再マスクの時間経過は Q 待ちと明記）。② 異なるホストの認証ダイアログを連続表示して入力値とチェック状態が残らないことの検査を追加。③ MultiPass JSON のスキーマを明記 — **実際のエクスポートはハッシュをキーにしたオブジェクト、ドキュメントの例は配列で、`parse_json` が `for...in` なので両方通る**ことをソースで確認済みなので、Nemo も両方受ける形にした。必須フィールドの型・件数と長さの上限・欠損時の扱いも追加。④ priority の一様判定を `Number(priority ?? 1)` の実効値比較にし、欠損・数値・文字列の混在で不要な警告が出ないことをテストに固定。

Q は2件（インポートで validator に拒否されるルールがあったときの原子性、再マスクまでの秒数）。どちらも人間の意思決定なので手を付けていない。

## 6回目

````text
## P0

## P1

- Phase 2: ストア > ステップ1 — 差し替えbackendの条件が「devビルド限定」で、`!app.isPackaged`とは明記されていない / このリポジトリではパッケージ済みdev版もdev channelなので、環境変数によりKeychainを使わない可逆backendで実パスワードを保存できる / `!app.isPackaged`を必須ゲートにし、パッケージ版では環境変数を無視する検査を追加する
- /dig で確定した仕様 > #19 — 決定表は`clearAuthCache()`だけのままで、Phase 2ではattempts全消しも必須になっている / normativeな仕様と実装手順が再び食い違い、片方だけ実装されうる / #19も両方の後始末へ更新し、storeと`http-auth.ts`の循環importを避けるため、永続化の外側に無効化サービスを置いて両者を呼ぶ
- Phase 6: 検証 > ステップ2 — 資格情報編集後の検査に「ナビゲーションなし」が明記されていない / reloadして再アクセスすると`did-start-navigation`がattemptsを消すため、キャッシュ破棄時のattempts全消しが欠けていてもPASSする / 同じdocumentからfetchを再実行して401を発生させ、新しい資格情報が自動入力されることを確認する
- Phase 6: 検証 > ステップ3 — テストbackendが単に「可逆」としか定義されていない / base64やXORでは暗号文を壊しても復号時に例外にならず、復号失敗ルールだけ無効化する検査へ到達できない / 固定ヘッダーとchecksum等を持ち、改変時に必ず復号エラーになる決定的な形式を定義する
- 動作確認 [人間] > ステップ1 — 「次回自動入力」を同じセッションで確認するとHttpAuthCacheが応答し、保存ルールも実`safeStorage`の復号も一度も通らない / テストbackendしか使わない自走検証と合わせると、本番暗号化経路の読み戻しが未検証になる / 保存後にNemoを完全終了・再起動してアクセスし、`auth.autofilled`ログとダイアログ非表示を確認する
- Phase 1: 純粋ロジック + 単体テスト > ステップ1 — pattern・username・password・件数の「上限」は追加されたが具体値がない / 境界テストとIPCの受理条件を実装者が任意に決めることになる / `MAX_RULES`・`MAX_PATTERN_LENGTH`・`MAX_USERNAME_LENGTH`・`MAX_PASSWORD_LENGTH`を数値で定義し、上限ちょうどと上限超過を検査する
- Phase 6: 検証 > ステップ2 — 再マスク検査が時間経過ごとQ待ちになっているが、Settingsを閉じたときと別ルールを表示したときの再マスクは既に決定済み / timer以外の2経路が未検証のままになる / この2経路は現在のUI検査に追加し、時間経過だけをQ決定後に足す

## P2

## Q

- Phase 1: 純粋ロジック + 単体テスト > ステップ1 — MultiPass内にvalidatorで拒否されるルールが1件でもあった場合、全インポートを中止するか、有効なルールだけ取り込んで拒否分を報告するかが未決定 / インポートの原子性・結果表示・回帰テストの期待値が決められない
- Phase 5: Settings の管理 UI > ステップ3 — パスワードを「一定時間」で再マスクするとあるが時間が未決定 / 実装値とUI検証の待機時間が任意になるため、何秒後に隠すか決める必要がある
````

**対応**: P0 が空になったので収束。P1 は7件中6件を反映した。

① 差し替え backend のゲートを `!app.isPackaged` 必須にした（このリポジトリはパッケージ済み dev 版も配っているので、env だけを条件にすると実運用のパスワードが Keychain を使わない形式で保存されうる）。② 決定表 #19 を「キャッシュ破棄と `attempts` 全消しの両方」に更新し、集約点は `http-auth.ts` と `store/http-auth.ts` の相互 import を避けて両者の外側に置くことを明記。③ 資格情報編集後の再チャレンジ検査を「リロードせず同じ document から `fetch` を撃つ」に限定（リロードすると `did-start-navigation` が `attempts` を消すので、集約が片方しか実装されていなくても PASS する。空振り検査になるという指摘が正しい）。④ テスト backend の形式を「固定ヘッダ + checksum を持ち、改変されたら必ず復号エラーになる」決定的な形に定義（base64 や XOR だと壊しても例外にならず「1件だけ無効化」の検査が空振りする）。⑤ 人間の動作確認を「保存後に Nemo を完全終了・再起動してからアクセス」に変更（同セッションだと HttpAuthCache が答えて、保存ルールも実 safeStorage の復号も一度も通らない）。⑥ 再マスクの2経路（Settings を閉じたとき / 別ルールを表示したとき）は決定済みなので UI 検査に今すぐ追加し、時間経過だけを Q 待ちにした。

見送り: 「上限値を `MAX_RULES` 等の数値で定義する」は、具体値まで plan に書くと実装時に決めればよい細部でファイルが膨らむため、「名前付き定数に切り出し、上限ちょうど / 超過をテストする」までに留めた。

Q は2件（インポートで validator に拒否されるルールがあったときの原子性、再マスクまでの秒数）。どちらも人間の意思決定なので手を付けていない。
