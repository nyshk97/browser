review session: ed979408-5ddb-427d-bc10-04b7bdbc5167

# polish-impl review log

対象: ⌘1〜9 を tools 専用にし、サイドバーの並びを tools → messages に入れ替える変更

## 1回目

````text
レビューしました（`git diff` 全体 + 周辺の呼び出し元・ドキュメント・検証スクリプトを確認。テスト/型チェックはサンドボックスで実行が許可されず未実行です）。

## P0

## P1
- `src/shared/favorites.js:favoritesInShortcutOrder` — 「番号が付くのは `tools`」という事実が main 側（この関数の `filter(... === 'tools')`）と renderer 側（`Sidebar.tsx:FavoriteSections` の `numbered={section === 'tools'}`）に別々のリテラルで二重に書かれている / 片方だけ直すと「バッジが出ているタイルと ⌘N が開くタイルが食い違う」という、目視でしか気づけないズレになる（しかも `favoritesInShortcutOrder` は `FAVORITE_SECTIONS` を参照しなくなったので、セクションを増やしたときの取りこぼしにも気づけない） / `export const SHORTCUT_SECTION = 'tools'` を `favorites.js` に置き、この関数と `numbered` の両方をそれ経由にする
- `src/renderer/components/Sidebar.tsx:FavoriteSections` — `messages` が空だとラベルごと畳むため、**`messages` へ D&D で振り分ける入口が消える** / `messages` を一度も使っていないユーザー（＝畳まれている状態が常態）は、右クリックの「Messages へ移動」を発見するまで振り分け機能に到達できない。畳みの元の動機は「上段を空箱 2 つで重くしない」なので、`tools` に 1 件でもあれば空箱は 1 つで動機を満たさない / 畳む条件を「`messages` が空 **かつ** `tools` も空（＝初回）」に狭め、通常時は `messages` の空受け皿を出す
- `DESIGN.md:サイドバーの 3 層の並び / ⌘1〜9` — 「Favorites は `messages` → `tools` の順」「⌘1〜9 は通し番号」「`tools` が空のときラベルごと畳む」の 3 点がすべて実装と逆になった / このリポジトリの仕様の一次情報がコードと矛盾したままになり、次に触る人（人間・エージェントとも）が旧仕様を正として直しにくる / 並び・番号・畳む側を新仕様に書き換える
- `VERIFY.md:Favorites のセクション（messages / tools） / ⌘1〜9 = Favorites の通し番号` — `verify-pins.mjs` の検査内容だけ書き換えて、それを目視手順として写している VERIFY.md が旧記述のまま（「ラベルが messages → tools → bookmarks」「⌘1 で messages の 1 件目」など） / 検証スクリプトと手順書が食い違い、手動検証が誤った期待値で回る / 新しい check 名に合わせて更新する（`⌘5` 空振り・`messages` にバッジが出ないの 2 件も追記）
- `src/shared/types.ts:FavoriteSection` — 型の JSDoc が「`messages` → `tools` の順に描き、⌘1〜9 もこの順で通し番号」のまま / `FavoriteSection` を使う側が最初に読むコメントで、`favorites.js` 側の正しい記述と真っ向から矛盾する / 並び順と「番号は `tools` のみ」に書き換える

## P2
- `scripts/verify-pins.mjs:messages が空ならラベルごと畳まれる` — `tools` が 0 件でもラベルは `['tools','bookmarks']` になるので、この check は「まだ描画が追いついていない」状態でも PASS する（畳みが壊れた場合しか落ちない一方、描画自体が死んでいても気づけない） / 直前に `await settleUi()` を入れ、同じブロックで `.fav-grid[data-section="tools"] .fav` が 5 個あることも併せて見る
- `scripts/verify-pins.mjs:messages は ⌘N の対象にならない（tools 4 件で ⌘5 は空振り）` — 判定は `activeTabKey` なのに失敗時の detail に `favoriteId` を出しており、`messages のグリッドにバッジが無い` は detail 自体が無い / FAIL したときに「何が開いてしまったか」がログから読めず、原因追跡が 1 往復増える / detail を判定した値（`activeTabKey` / バッジ数）に合わせる
- `scripts/verify-pins.mjs:Favorites のセクション（tools / messages）ブロック` — 新しく宣言した「`tools` は空でも受け皿を出す」に対応する検査が無い（畳む側だけ検査している） / `tools` を空にしたときに `.fav-empty[data-section="tools"]` が出ることを 1 件足す
- `scripts/readme-shot.mjs:FAVORITES` — 見せ用データは `messages` 4 件が先頭のままで、並びが入れ替わったぶん `docs/images/readme.png` が実物と違う / 撮り直す（`node scripts/readme-shot.mjs`）
- `src/renderer/components/Sidebar.tsx:Sidebar` — 冒頭の docstring が「Favorites（messages / tools）」のままで、同ファイル内の他のコメントとだけ食い違っている

## Q
- `src/renderer/components/Sidebar.tsx:FavoriteSections` — `tools` が 10 件以上あるときに 10 件目以降を「番号なしで `tools` に置く」のを許すのか、それとも `messages` を「番号なし置き場」として運用に載せるのか（今回の変更で `messages` は番号と無関係な純粋な分類になった） / 決めないと、⌘N を使い切ったユーザーが `messages` を第 2 の `tools` として使い始め、次に UI 文言やラベル名（`messages`）を変えるときに後方互換の縛りになる

````

**対応**: P0 なしで収束。P1 は全件反映（SHORTCUT_SECTION 定数で番号対象の二重定義を解消 / messages の畳みを「Favorites が 1 件も無い初回だけ」に狭めて D&D の入口を維持 / DESIGN.md・VERIFY.md・types.ts の旧仕様記述を更新）。P2 も全件反映（verify-pins に settleUi + tools 5 件の実測・初回畳み検査・受け皿検査を追加、⌘5 とバッジ検査の detail を判定値に統一、readme.png 撮り直し、Sidebar docstring 修正）。Q（tools 10 件以上のときの messages の位置づけ）は実装を止めないため「今回の範囲に入れない」で閉じた。
