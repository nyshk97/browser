review session: 65d0607e-e7c8-4a03-87d3-a24fe96e931e

## 1回目

````text
`git diff` を全部読み、参照の残り（`call-btn` / `toggleCallDevice` / `callToggleMic` 等）が src に無いこと、`OWNERS` の逆引きが full に倒れることまで確認した。ビルド系コマンドはこのセッションでは実行が許可されなかったので、型検査・verify は未実行（静的読みのみ）。

## P0

## P1
- `src/main/call-coordinator.ts:probe`（`syncBackgroundThrottling` の削除） — 小窓が出ている状況＝会議タブの View が hidden の状況なので、rAF を止め直した結果**小窓が出ているあいだ Meet 側の DOM 更新が凍る**。削除したコメント自身が「hidden で 3 秒待っても `muted=false`、前面に戻して初めて `true`」という実測を残しており、これはクリック経路に限らず**ホストによるミュート・ホストが会議を終了したときのタイル消滅**にも同じく効く可能性が高い。前者なら「OFF なのに小窓は ON」、後者なら**終わった会議の小窓が出っぱなし＋そのタブが `isSleepExempt` で永久に寝ない**（`refreshCallCoordinator` の候補は URL でしか外れない）。直し方は 2 択で、**推奨は `state === 'joined'` の対象タブだけスロットリング除外を残すこと**（コストは元々 CPU だけ、今度は「押した結果の反映」ではなく「表示の鮮度と離脱検知」が理由になるのでコメントと DESIGN をその理由に書き換える）。受け入れるなら、最低限「ホストがミュート／会議を終了」を背面で起こして小窓がどうなるかを実測し、結果を DESIGN に明記する。
- `docs/CHANGELOG.md:[Unreleased] 変更` — 「合わせて、会議中のタブの背面での描画を止めない特別扱いも無くした（切り替えの反映のためだけに入れていたもの）」は事実として狭い。上の通り**表示の鮮度と離脱検知にも効いていた**ので、このまま出すとユーザー向けの説明が実挙動とずれる。上の判断が決まってから、残すなら 1 行削除、外すなら「小窓のマイク・カメラの表示は会議タブを離れた時点の状態」と読み手に分かる書き方にする。
- `test-pages/meet-fake.html:mic/cam の click ハンドラ` — rAF を外して同期書き込みにしたことで、**偽 Meet が本物より「良い」挙動になった**（ファイル冒頭の「本物と同じ目印を持たせてある」という前提が崩れる）。結果、`verify-call.mjs` の「ページ側で切ると小窓が追従する」は**背面では本物で成立しない性質を PASS させる**（偽の安心）。rAF 版に戻し、上の P1 でスロットリング除外を残すならそのまま PASS するはず、外すなら**この検査を「背面では追従しない」を固定する形に書き換える**のが筋。
- `scripts/verify-call.mjs:「マイク・カメラの表示は押せない」の check` — 3 つの判定のうち 2 つが空振りしうる。`el.onclick` は React（root 委譲）では常に `null` なので恒真、`getComputedStyle(el).webkitAppRegion` は非標準プロパティで computed style から読めない場合 `undefined` になり、そのときも `noDrag === 0` で PASS する（＝退行しても気づけない）。**陽性コントロールを足す**：同じ式で `.call-goto` を測って `no-drag` かつ `BUTTON` であることを assert し、それが取れない環境なら検査自体を FAIL させる。加えて形ではなく**振る舞い**で押さえる方が強い — `clickBar('[data-device="mic"]')` を撃って `meet.status()` の `clicks.mic === 0` かつ `micMuted` 不変を見る（`__clicks` は残っているので使える。`onClick` を戻す退行だけは形の検査を素通りする）。
- `VERIFY.md:会議の小窓（手動確認 4）` — 「Meet でミュートを切り替えて小窓の表示が 1〜2 秒で追従すること」は、Meet タブを前面にした瞬間に小窓が hide される（`applyWindow` / `tabOnScreen` はウィンドウのフォーカスも見る）ため**書いてある通りには観測できない**。手順を「Meet で切り替える → 別タブへ移る → 小窓の表示が新しい値になっている」に直し、今回退行した側（**別タブに居るあいだにホストからミュートされた／会議が終了した**）を独立した手順として足す。

## P2
- `src/renderer/components/CallBar.tsx:DeviceIndicator` — 押せなくしたのに 38px・角丸・`--nemo-surface` の面という**ボタンの見た目のまま**なので、押せると読める。唯一の説明である `title="（切り替えは会議タブで）"` は `-webkit-app-region: drag` の上に置かれていて、macOS ではポインタが窓のドラッグに吸われるため出ない見込み（要実機確認）。面を落としてアイコンだけにする／`--nemo-ink-dim` 寄りにするなど、掴みしろらしい見た目に寄せると齟齬が消える。
- `docs/plans/2026-08-25-1606-meet-call-mini-window.md` — `call:toggleMic` / `call:toggleCam` と `buildToggleSource` を現行 API として記載したまま。plan は日付つきの記録なので更新しない運用だと理解しているが、この plan は「直す場所は 2 か所だけ」と将来の作業者に読ませる意図の記述なので、末尾に「2026-09-08 に廃止」の 1 行があると次に読む人が迷わない。

## Q
- `src/main/call-coordinator.ts:isShowable` — 背面で会議が終了した（ホストが終了・回線断）ケースを、スロットリング除外を残して DOM 変化で拾うのか、それとも別の手段（一定時間ごとに強制的に読み直す等）で拾うのか。決めないと「終わった会議の小窓が出続け、そのタブが寝ない」が仕様なのか不具合なのか判定できず、P1 の直し方も選べない。

````

**対応**: P0 なしで収束。P1/P2 と Q は以下のとおり反映。
- Q `call-coordinator.ts:isShowable` / P1 `probe`（スロットリング解除の削除）: **解除を残す**と決定。根拠は削除したコメントに残っていた実測（背面では Meet の DOM 更新が rAF 待ちで凍る）で、表示だけ残す以上「小窓が出ているあいだ表示が古いまま・主催者の終了で小窓が出続ける」は避ける必要があり、コストは以前と同じ CPU のみ。`syncBackgroundThrottling` / `restoreBackgroundThrottling` / `throttlingDisabled` を復元し、コメントの理由を「表示の鮮度と離脱の検知」に書き換えた。DESIGN.md の節も同じ理由で復元
- P1 `docs/CHANGELOG.md`: スロットリング解除を無くした旨の文を削除
- P1 `test-pages/meet-fake.html`: 属性書き換えを rAF 越しに戻した（本物と同じ。解除が壊れると背面で追従せず落ちる）
- P1 `verify-call.mjs` の「押せない」検査: 陽性コントロール（`.call-goto` が BUTTON かつ no-drag と測れる）を足し、`clickBar('[data-device="mic"]')` → `clicks.mic === 0` かつ `micMuted` 不変の振る舞い検査を足した。`el.onclick` 判定は捨てた
- P1 `VERIFY.md` 手動確認 4: 「Meet で切り替える → 別タブへ移る → 小窓が新しい状態」「別タブに居るあいだに主催者からミュートされても 1〜2 秒で OFF」に書き換え。自走検証の項目にも背面追従を明記
- P2 `CallBar.tsx:DeviceIndicator` の見た目（面を落とす等）: **見送り**。色・形の変更はユーザーの目視で決める領域（グローバルルール「UI の見た目の相談」）で、今回の依頼範囲は切替の削除。title が drag 領域で出ない可能性は終了報告に載せる
- P2 plan への廃止追記: 末尾に 1 行追記した

