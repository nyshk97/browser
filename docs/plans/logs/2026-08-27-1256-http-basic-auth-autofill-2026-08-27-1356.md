codex session: 01a04193-e3fd-7a10-9c97-8780af7fa36b

## 1回目

````text
## P0

## P1

- Phase 4: ダイアログ（保存と prefill） > ステップ3 — 拒否された既存ルールでも `patternFromUrl(details.url)` から保存するため、ワイルドカードやインポート済みルールの資格情報を修正しても元ルールは上書きされない / 誤った資格情報のルールが残り、別URLでは再び送信されて自己修復仕様を満たさない / `rejected` に採用ルールIDを保持し、再保存時はそのルールの username/password を更新する。`patternFromUrl` で新規作成するのは `no-rule` の場合だけにする
- Phase 5: Settings の管理 UI > ステップ1 — `nemo:import-multipass` が資格情報変更後の共通後始末を呼ぶ指定になっていない / 同じパターンをインポートで上書きしても HttpAuthCache が古い資格情報を送り続け、インポート結果が同一セッションへ反映されない / インポート全体を1回の永続化トランザクションとして保存した後、共有 coordinator で auth cache と `attempts` / `denied` / `inflight` を消し、その結果もIPCへ返す
- Phase 1: 純粋ロジック + 単体テスト > ステップ1 — 件数・username・passwordの上限がMultiPassインポートにしか適用されず、ダイアログ保存・Settings保存・`normalizeRules`との共通不変条件になっていない / 同じ資格情報でも入口によって拒否・無制限保存・既存IPCの黙示的な切り詰めに分かれ、手入力した値と実際に送信／保存された値が食い違う / 上限と検証関数をsharedへ集約し、全保存入口と正規化で適用する。rendererには対応する`maxLength`を設定し、境界テストも各入口へ追加する
- Phase 5: Settings の管理 UI > ステップ3 — パスワードの「再マスク」とインポート後の平文JSONを破棄する要件がない / CSS上で隠すだけなら取得済みパスワードがrenderer stateに残り、インポートtextareaには全資格情報が表示されたままになる / 再マスク時は平文state自体を消し、インポート成功後はtextareaを直ちに空にする。Settings終了時にも両方を破棄する

## P2

## Q
````

**対応**: P0 は空なので収束。P1 4件はいずれも安く効くので全部反映した。①`rejected` に rule ID を載せ、再保存は**採用されたルールの username / password を更新**する形に変更（`patternFromUrl` で新規作成するのは `no-rule` のときだけ）。ワイルドカード／インポート済みルールを直しても元ルールが残る穴を塞いだ。②`nemo:import-multipass` を1トランザクションで永続化したあと #19 の共通後始末（auth cache / `attempts` / `denied` / `inflight`）を通し、その結果も IPC で返す。③件数と username / password / pattern の長さ上限を `shared` の名前付き定数に集約し、**全保存入口（ダイアログ・Settings・インポート）と `normalizeRules`** で共通適用。renderer にも同じ `maxLength` を入れ、境界テストを各入口に。④再マスクは表示の切り替えではなく **renderer の平文 state 自体を消す**ことと、インポート成功後に textarea を直ちに空にする（Settings を閉じるときも破棄）ことを明記。
見送りなし。
