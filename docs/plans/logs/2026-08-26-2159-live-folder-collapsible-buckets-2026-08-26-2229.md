## 1回目

````text
## P0
- 実装計画 > Phase 3 — `review` を開いた後に `.lf-sub .dot` が 0 件という期待は、未読のまま折りたたまれている `mine` のドットまで数えるため正しい実装でも失敗する / `mine` には PR_88 の未読ドットが残る / `.lf-sub[data-bucket="review"] .dot` が 0 件、`.lf-sub[data-bucket="mine"] .dot` が 1 件と個別に検査する

## P1
- 実装計画 > Phase 1 — state 更新を関数形式にする指定がない / `expandAll` は2ボタンを同一タスクで連続クリックするため、`setCollapsed({ ...collapsed, ... })` だと React のバッチ処理で片方の更新が失われ、ヘルパがタイムアウトする / `setCollapsed((prev) => ({ ...prev, [bucket]: !prev[bucket] }))` を明記する
- 実装計画 > Phase 3 — ホバー直後に背景色を読む手順では `.row` の `background 0.12s` transition が完了しておらず、正しい色でも中間値になり得る / 完全一致検査が環境依存で不安定になる / 両対象とも `--nemo-hover` の最終色になるまで `until` で待ってから比較する

## P2
- 実装計画 > Phase 3 — DESIGN.md への「追記」だけでは、既存の「右端は常に描画行数」「下に何行あるか」という説明と折りたたみ中の件数表示が矛盾する / 将来、件数を DOM 上の行数へ戻す誤修正を招く / 既存記述を「開閉状態にかかわらないバケット内の項目数」へ置き換えることまで計画に明記する

## Q
````

**対応**: P0 → 未読ドットの検査を bucket ごとに分離。P1-1 → setCollapsed の関数形式を明記。P1-2 → hover 色は transition 完了まで until で待つ。P2 は見送り

## 2回目

````text
## P0
- Phase 3: 自走検証の追従 > ステップ3 — 外観検査で `review` を開いた直後、次の検査が同じ見出しをクリックして「開く」ため、実際には閉じて件数検査が失敗する / 検査間で開閉状態が漏れており、記載順では正しい実装でも通らない / 各検査の冒頭で両方を閉じる状態正規化ヘルパを呼ぶか、外観検査で開いた状態を次の独立開閉検査にそのまま利用する

## P1
- Phase 3: 自走検証の追従 > ステップ2 — 展開対象として列挙されているのが `ROW_TITLES`・`STALE_ROWS`・`TRUNCATED` だけで、既存の `.lf-row .lf-sub-line` 直接取得が漏れている / 直前の追加検査が折りたたみ状態を残すと、2行目検査が空配列を読んで失敗する / `.lf-row` に依存するすべての既存取得を洗い出し、直前に `expandAll` する共通読取ヘルパへ寄せる
- Phase 1: 開閉 state と小見出しのトグル > ステップ2 — `data-bucket` を「ボタン自身に付ける」としながら外側も `<div data-bucket>` と記述し、行検査は `[data-bucket="review"] .lf-row` を使っている / ボタンだけに付けると行は兄弟なので一致せず、両方に付けると属性の役割が重複する / 外側を `.lf-bucket[data-bucket]` に統一し、ボタンと内容は子結合子を使ったセレクタで識別する
- Phase 3: 自走検証の追従 > ステップ3 — キーボード検査の `Input.dispatchKeyEvent` のイベント列が決まっていない / Enter は押下時、Space は解放時にボタンを発火するため、単発イベントでは片方だけ失敗し得る / `keyDown` と `keyUp` を正しい `key`・`code` 付きで送り、Enter とSpaceを別々に検査する

## P2
- Phase 3: 自走検証の追従 > ステップ5 — 「両方閉」と「両方閉で未読ドットが小見出し側」は、初回 `BASE` の全件が未読なので同じ画面になる / 同一状態のPNGが2枚でき、追加の視覚情報がない / 一方を「両方開」または「未読既読化後の両方閉」に置き換える

## Q
````

**対応**: P0 → collapseAll で各検査冒頭に状態正規化、順序を明記。P1-1 → lf-row 依存の取得を readExpanded ヘルパに寄せる。P1-2 → data-bucket はラッパーだけに付け子結合子で識別。P1-3 → keyDown/keyUp 対と Enter/Space 別検査。P2 は見送り

## 3回目

````text
## P0

## P1

- Phase 3: 自走検証の追従 > ステップ2 — `TRUNCATED` を一律 `readExpanded` 経由にすると、ステップ3の「畳むと `.lf-truncated` が0件」を読む直前に再展開してしまう / 正しい実装でも折りたたみ検査が失敗する / 展開前提の既存検査だけを `readExpanded` に寄せ、開閉状態を検査する取得は raw のまま分離する
- Phase 3: 自走検証の追従 > ステップ3 — 未読検査のセレクタが `.lf-sub[data-bucket="review"]` のままで、ステップ1の「`data-bucket` は外側だけ」と一致していない / 読み替え規則に依存すると検査コードへ誤ったセレクタを転記しやすい / 記載を `.lf-bucket[data-bucket="review"] > .lf-sub .dot` など実際に使うセレクタへ直接置き換える
- Phase 1: 開閉 state と小見出しのトグル > ステップ6 — 小見出しは `.lf-items` だけを `aria-controls` で参照する一方、開閉によって別位置の `.lf-truncated` も表示が変わる / 支援技術へ公開する制御対象と実際の挙動が一致しない / 打ち切り行にも安定したIDを付けて `aria-controls` の対象に含めるか、制御対象を一つの領域として表現できる構造にする

## P2

- Phase 3: 自走検証の追従 > ステップ5 — 「両方閉」と「両方閉で未読ドットが小見出し側」は、初回 `BASE` が全件未読なので同じ画面になる / 同一状態のPNGが2枚できて追加の視覚情報がない / 一方を「両方開」または「既読化後の両方閉」に置き換える
- Phase 3: 自走検証の追従 > ステップ7 — `DESIGN.md` の既存説明「下に何行あるか」は、折りたたみ中にDOM上の行が0件でも件数を表示する新仕様と矛盾する / 将来、件数を表示行数へ戻す誤修正を誘発する / 追記だけでなく「開閉状態にかかわらないバケット内の項目数」へ既存文言を置き換える

## Q
````

**対応**: P1-1 → readExpanded は既存検査だけに使い、開閉状態の検査は raw で読むと明記。P1-2 → セレクタを .lf-bucket[data-bucket] > .lf-sub に全置換。P1-3 → 打ち切り行に id を付け aria-controls を複数参照に。P2 は見送り

## 4回目

````text
## P0

- Phase 1: 開閉 state と小見出しのトグル > ステップ6 — 折りたたみ時に `.lf-truncated` を DOM から除去する一方、ボタンの `aria-controls` にはその ID を残すため、参照先実在検査が確実に失敗する / アクセシビリティ要件とも矛盾する / `.lf-truncated` は常時描画し、折りたたみ時だけ `hidden` にする

## P1

- Phase 3: 自走検証の追従 > ステップ7 — DESIGN.md に規則を追記するだけでは、既存の「件数は常に描画行数」「下に何行あるか」という定義と、折りたたみ時も全件数を出す新仕様が矛盾する / 実装・将来のレビューで件数の意味が割れる / 既存記述を「バケット内の総件数で、折りたたみ状態に依存しない」へ置き換え、LiveFolder.tsx と styles.css の同趣旨コメントも更新する

## P2

## Q
````

**対応**: P0 → 打ち切り行は常時描画し hidden で畳む、検査は :not([hidden]) で数える。P1 → DESIGN.md の既存定義を置き換え、tsx/css のコメントも更新するよう明記

## 5回目

````text
## P0
- Phase 3: 自走検証の追従 > ステップ7 — 件数を「バケット内の総件数」に変更するとしている一方、Phase 1 は `items.length` を表示し、打ち切り検査も `137` ではなく `100` を期待しており、仕様・実装・検査が矛盾している / このままでは実装後に DESIGN.md 逸脱か検査失敗のどちらかが確定する / 件数を「取得済み件数」と「GitHub 上の総件数」のどちらにするか統一し、表示値・`aria-label`・打ち切り検査・DESIGN.md・コードコメントを同じ定義に揃える

## P1
- Phase 3: 自走検証の追従 > ステップ3 — 右クリック検査で開いた `RowMenu` を閉じる手順がない / `RowMenu` は `mousedown`・`contextmenu`・キー操作などで閉じるため、後続ヘルパの `element.click()` だけではメニューが残り、検査間の状態独立が崩れる / 右クリック検査の末尾で Escape または外側クリックを送り、`.row-menu` が消えるまで待つ
- Phase 3: 自走検証の追従 > ステップ5 — 「両方閉」と「両方閉で未読ドットが小見出し側」のスクリーンショットが、初回 `BASE` では全件未読になるため同じ状態になる / 3枚撮っても未読なしの閉状態を確認できず、検証意図が重複する / 1枚目を未読なしの fixture で撮ることを明記するか、重複する場面を別の有用な状態に置き換える

## P2
- Phase 1: 開閉 state と小見出しのトグル > ステップ2 — 新しいバケットラッパーの `.lf-bucket` は、既存の打ち切り文中のバケット名 `<span className="lf-bucket">` と同名になる / 現在の属性付きセレクタなら直ちには壊れないが、同じクラスがコンテナとラベルの二役になり、将来の CSS・DOM 取得で誤爆しやすい / ラッパーを `.lf-pr-bucket` など別名にする

## Q
````

**対応**: P0 → 件数の定義を「取得済み件数 = items.length」に統一し前提・DESIGN.md 記述を揃えた。P1-1 → 右クリック検査末尾で Escape してメニュー消失を待つ。P1-2 → スクショ場面を「両方閉／review だけ開／両方開」に置換。P2 は見送り

## 6回目

````text
## P0

## P1

- Phase 3: 自走検証の追従 > ステップ5 — スクリーンショット直前に矢印の transition 完了と合成マウスの退避を待つ手順がない / 回転途中の矢印や意図しない hover 状態を撮影し、外観確認が不安定になる / 各場面を作った後、矢印が最終 transform になるまで待ち、マウスを対象外へ移動して hover が消えたことを確認してから撮影する
- Phase 3: 自走検証の追従 > ステップ7 — 件数の定義を揃える更新対象から、`LiveFolder.tsx` の打ち切り説明、`styles.css` の `.lf-truncated` コメント、`src/shared/types.ts`、`src/shared/github-pr.js`、既存検査名に残る「描画行数」が漏れている / 折りたたみ中は描画行数が0なので、実装後もコード内に新仕様と矛盾する説明が残る / `rg '描画行数|rendered'` の該当箇所を洗い出し、「取得済み項目数」と検索の `returned`・`total` を区別する文言へ更新する
- Phase 1: 開閉 state と小見出しのトグル > ステップ7 — 「トグルは左クリックのみ」が、Phase 3で必須としている Enter・Space によるトグルと文面上矛盾する / 実装者がポインター入力だけに制限するとキーボード操作を壊す / 「右クリックではトグルしない。通常の click activation（左クリック・Enter・Space）でトグルする」と書き換える

## P2

## Q
````

**対応**: P1-1 → 撮影前に transform 完了と hover 退避を待つ。P1-2 → 「描画行数」の語を git grep で洗い出して更新する対象を列挙。P1-3 → トグルは click activation（左クリック・Enter・Space）と書き換え

## 7回目

````text
## P0

## P1
- Phase 3: 自走検証の追従 > ステップ3 — `aria-label` の検査は全バケットに未読がある状態しか通らず、「未読あり」の固定表示でも合格する / 未読なし時のラベルとドット制御を検証できない / 片方のバケットを全件既読にした状態で、見出しドットがなく `aria-label` が「未読なし」を表すことも検査する
- Phase 3: 自走検証の追従 > ステップ6 — VERIFY.md に「行を数える前に `expandAll`」と無条件で書くと、同じ計画で追加する折りたたみ中の `.lf-row` 0件検査と矛盾する / 将来の検査追加で `readExpanded` を誤用し、開閉状態の不具合を見逃す / 「表示行を前提とする既存検査だけ `readExpanded` を使い、折りたたみ状態の検査は raw で読む」と条件付きで記載する

## P2

## Q
````

**対応**: P1-1 → 未読なし状態の aria-label / ドット検査を追加。P1-2 → VERIFY.md の記載を条件付きに。上限到達のため再レビューは未実施

