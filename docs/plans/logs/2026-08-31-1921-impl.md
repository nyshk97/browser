# polish-impl review log

対象: will-prevent-unload 修正（beforeunload ページでリロードが黙って握り潰されるバグ）

## 1回目

````text
コードとリポジトリの既存規約（`ipc.ts` / `timings.ts` の env ゲート、`verify-packaged.mjs` の裏口封鎖検査、`verify-peek.mjs` の `USER_DATA` ガード）を突き合わせて確認しました。`tsc` / `prettier` はサンドボックスで実行できず未確認です。

## P0
- `src/main/registry.ts:attachTabEvents`（`will-prevent-unload` ハンドラ） — `NEMO_VERIFY_UNLOAD_CHOICE` が `!app.isPackaged` で塞がれていない / 既存の裏口（`ipc.ts` の `NEMO_VERIFY_DIAGNOSTICS`、`timings.ts` の `NEMO_VERIFY_TIMINGS`）はすべて `!app.isPackaged` でゲートされ、`verify-packaged.mjs:200-240` が「パッケージ版では効かない」ことを検査している。このままだと配布版でも env を置くだけで離脱確認を無効化でき、`stay` を置けば**今回直したバグ（リロードが無反応）そのものを再現できる**裏口になる / `const verifyChoice = app.isPackaged ? undefined : process.env['NEMO_VERIFY_UNLOAD_CHOICE']` に揃え、可能なら `verify-packaged.mjs` に同種の封鎖検査を足す

## P1
- `src/main/registry.ts:attachTabEvents`（`will-prevent-unload` ハンドラ） — 破棄チェックが `current.isDestroyed` だけで `current.baseWindow.isDestroyed()` を見ていない / 同ファイルは `win.isDestroyed || win.baseWindow.isDestroyed()`（`registry.ts:629`, `2285` 等）で必ず対にしている。NemoWindow のフラグが立つ前に baseWindow が壊れている窓で `showMessageBoxSync(current.baseWindow, …)` が throw し、イベントハンドラ内なので main の未捕捉例外になる（verify-all は `app.uncaught_exception` を 0 で見ているので検証も落ちる） / 条件を `!current.isDestroyed && !current.baseWindow.isDestroyed()` にする
- `scripts/verify-all.mjs:runVerify` / `scripts/verify-phase1.mjs`（beforeunload ブロック） — `NEMO_VERIFY_UNLOAD_CHOICE` をアプリ側 env にしか載せておらず、検証スクリプト側は自分が「自動で離れる」モードかを知れない / `verify-phase1.mjs` のヘッダは単体実行（`node scripts/verify-phase1.mjs`）を使い方として書いており、env 無しで起動したアプリに対してこのブロックを撃つと**ネイティブ modal が出て main が同期的に固まり、検証が永久に止まる**（このリポジトリが `NEMO_HTTP_AUTH_TEST_CRYPTO` で明示的に避けている失敗モードと同じ） / `NEMO_VERIFY_TIMINGS` と同様に `runVerify` の env にも同じ値を載せ、phase1 側は未設定ならこのブロックを skip（または明示 FAIL）する
- `VERIFY.md`（Phase 1 で人が見る分） — 人間確認の項目が足されていない / 自走検証は常に `leave` を自動選択するため、**ダイアログが実際に出ること・文言・「キャンセル」で留まる分岐**は一度も検証されない。ネイティブ modal は原理的に CDP から押せないので、機械検証だけでは永久に穴が残る / 「未保存フォームで ⌘R → 確認が出る／キャンセルで入力が残る／離れるでリロードされる」を Phase 1 の人が見る分に追記する

## P2
- `src/main/registry.ts:attachTabEvents`（`will-prevent-unload` ハンドラ） — バックグラウンドタブが自分で遷移した場合もウィンドウ modal だけが出る / Chrome は該当タブを前面化してから確認を出すので、どのページの確認か分からない状態になりうる / ダイアログ前に `tab` を前面化するか、少なくともタイトルを `detail` に載せる
- `src/main/registry.ts:attachTabEvents`（`will-prevent-unload` ハンドラ） — 連続表示の抑制がない / ページが `location.reload()` を回すと同期 modal が連発し、main が固まり続けて操作不能になる（Chrome は同一ドキュメントでの繰り返し表示を抑制する） / 同一 WebContents で短時間に n 回を超えたら自動で `preventDefault()` する等の上限を置く
- `scripts/verify-phase1.mjs`（beforeunload ブロック） — `readLogLines(process.env.NEMO_USER_DATA_DIR ?? '')` が未設定時に相対パス `logs` を読みに行き、静かに空配列 → 原因の分かりにくい FAIL になる / `verify-peek.mjs:228` は `if (!USER_DATA) return null` でガードしている / 同じガードを入れる（P1 の skip 判定とまとめてよい）
- `scripts/verify-phase1.mjs`（beforeunload ブロック） — マーカー消失の待ちで毎周 CDP の WebSocket を張り直している / 12×400ms 固定で、遅い CI では 4.8 秒で打ち切られるうえ接続コストも乗る / `waitFor` と 1 本のセッションで回すか、`afterSessionSave` と同じく timings 由来の待ちに寄せる
- `src/main/registry.ts:attachTabEvents`（`will-prevent-unload` ハンドラ） — `NEMO_VERIFY_UNLOAD_CHOICE=stay` を通す経路がどこからも使われていない / 未使用の分岐は腐る（P1 の VERIFY.md 追記で人間が見るなら env 側は不要） / 使わないなら `stay` を落として `leave` の有無だけで判定する

## Q
- `src/main/registry.ts:NemoTab.sleep` / `closeTab` 経路 — タブを閉じる・ウィンドウを閉じる・アプリ終了でも同じ離脱確認を出すか / いまの `wc.close()` は既定で beforeunload を走らせない（`waitForBeforeUnload` 未指定）ので、**⌘R では確認が出るのに ⌘W や sleep では未保存のまま黙って消える**という非対称が残る。Chrome はタブ閉じでも確認するため、出さない方針なら意図的な差として決め、出す方針なら閉じる側の実装（と「寝かせるだけの sleep で確認を出すのか」）を決める必要がある
````

**対応**: P0: NEMO_VERIFY_UNLOAD_CHOICE を `!app.isPackaged` でゲート（規約どおり）。P1: baseWindow.isDestroyed() の対を追加 / runVerify の env にも UNLOAD_CHOICE を追加し、phase1 は leave と NEMO_USER_DATA_DIR が無い単体実行では SKIP（P2 の readLogLines ガードも兼ねる）/ VERIFY.md にダイアログ目視の項目を追記。P2: マーカー待ちを CDP 1 本・最大 10 秒に書き換え。見送り: verify-packaged への封鎖検査（ゲートが働くと本物のネイティブ modal が開いて packaged 検証ごと固まるため安全に自動化できない。実物の確認は VERIFY.md の目視へ）/ タブ前面化・タイトル表示（仕組みの追加）/ 連発抑制（仕組みの追加）/ stay の削除（scratchpad の分岐検証で使用済み・1 行のコストで両分岐を検査可能に保つ）。Q: ⌘W / sleep の離脱確認は範囲外として閉じた（sleep は自動の後始末なので silent が正しい。⌘W は独立した機能判断で本バグを止めない）

## 2回目

````text
前回の指摘の反映を差分で確認しました（`app.isPackaged` ゲートは `timings.ts` と同形、`baseWindow.isDestroyed()` の対も追加済み、`runVerify` への env 追加と skip 分岐、マーカー待ちは CDP 1 本・10 秒に集約）。plan ファイルは無し（`docs/plans/` に該当なし、未追跡ファイルも無し）。見送り理由はいずれも妥当なので再掲しません。

## P0

## P1
- `scripts/verify-phase1.mjs`（beforeunload ブロックの skip 分岐） — SKIP が集計されず、まとめ行が `verify-phase1: すべて PASS` のまま exit 0 になる / このリポジトリは「条件に到達できなかったものは黙って PASS にしない」を明文化していて、`verify-split.mjs:skip` / `verify-http-auth.mjs:skip` は `skipped` を数えて `（SKIP N 件 —— 見ていない検査がある）` をまとめ行に出す。今の実装は生の `console.log` だけなので、単体実行時に「見ていない検査がある」ことが最終行から消える / `verify-split.mjs:skip` と同じ 4 行のヘルパ＋カウンタを phase1 にも置き、`ui.close()` 後のまとめ行に SKIP 件数を出す
- `VERIFY.md:beforeunload の離脱確認（人が見る分）` — 節が `## 手で CDP を叩く` の下に置かれている / このファイルの「人が見る分」は機能セクション配下（`分割ビュー > 人が見る分`、`Live Folder > 人が見る分`）か `## Phase 1 で人が見る分` に集約されており、`手で CDP を叩く` は CDP 技法の説明が並ぶ場所。**CDP から押せないから人が見る**という項目がそこにあると、リリース前に「人が見る分」を順に潰す運用から漏れる / `## Phase 1 で人が見る分` の番号付きリストに項目として足す（技法側に残すなら 1 行の参照だけにする）
- `VERIFY.md:beforeunload の離脱確認（人が見る分）` — 手順に「ページを一度クリックする」と再現用ページが無い / sticky user activation が無いと Chromium は beforeunload のキャンセル自体を無視してダイアログが出ない（`verify-phase1.mjs` のコメントが「これが無いと空振りする」と明記している罠）。手順どおり「開いて ⌘R」だけを実行すると何も出ず、直っているのに壊れていると誤判定する。加えて `test-pages/` に beforeunload を出すページが無く、「入力途中の予約フロー等」を実サイト頼りにしているので再現性がない / 手順 1 に「ページを一度クリックしてから ⌘R」を明記し、`test-pages` に beforeunload 付きのページ（または `login.html?beforeunload=1`）を足して URL を書く

## P2
- `scripts/verify-phase1.mjs`（beforeunload ブロックの `prompted` 判定） — `tab.unload_prompt` をログ全体から探しており、タブを特定していない / 将来ほかの検査や別タブが同じイベントを出すと、当該リロードが確認を通っていなくても真になる（`marker === 'gone'` と組で「素通り」を弾く役目が弱まる） / `entry.key === key` を条件に足す
- `scripts/verify-all.mjs:startApp` / `scripts/verify-all.mjs:runVerify` — `'leave'` が 2 箇所にベタ書き / 同ファイルは同じ事情の `NEMO_VERIFY_TIMINGS` を `verifyTimings` 変数で共有しており、片方だけ変えるとアプリと検証側の前提が静かにズレる（ズレると検査は skip され、上の P1 を直すまで気づけない） / `const verifyUnloadChoice = 'leave'` を切って両方から渡す

## Q
````

**対応**: 収束（P0 なし）。採用した P1/P2: phase1 に skip ヘルパ＋カウンタを置き、まとめ行に SKIP 件数を出す（verify-split と同形）/ VERIFY.md の beforeunload 目視を「## Phase 1 で人が見る分」へ移設（技法節からは削除。ついでに重複していた項番 4 を 6 に直した）/ 手順に「ページを一度クリックしてから ⌘R」を明記し、test-pages/login.html に ?beforeunload=1 の離脱ガードを足して再現可能にした / prompted 判定に entry.key === key を追加 / verify-all の 'leave' を verifyUnloadChoice 定数に共有化
